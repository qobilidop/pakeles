/target
__pycache__/
.pytest_cache/
*.pyc
*.egg-info/
/python/build/
/python/dist/

# Generated conformance suites: they churn on every IR/testgen change,
# so they stay uncommitted — locally regenerate with
# ./dev.sh scripts/gen-examples.sh (or just `cargo run --bin
# gen_vectors`); CI regenerates them before testing, so their absence
# never weakens the gate.
examples/*/conformance/vectors.json
examples/*/conformance/vectors.pcap
benchmarks/*/*/conformance/vectors.json
benchmarks/*/*/conformance/vectors.pcap

# symex perf bench outputs (baselines, inventories)
/.bench/

# Factory/spike build outputs (never committed; see the third_party/
# licensing rule for why katran sources are fetch-only)
benchmarks/industry/*/factory/build/
benchmarks/industry/*/spike/build/
benchmarks/industry/*/factory/witnesses.txt
/benchmarks/industry/tls_clienthello/factory/target/
benchmarks/industry/tls_clienthello/factory/witnesses.golden.json
/benchmarks/industry/quic_initial/factory/target/
benchmarks/industry/quic_initial/factory/witnesses.golden.json
