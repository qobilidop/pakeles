/* Generated by pakeles from `quic_initial`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

static uint64_t pk_read_bits(const uint8_t *buf, uint64_t avail, uint64_t off, uint32_t n) {
  (void)avail;
  uint64_t v = 0;
  uint32_t i;
  for (i = 0; i < n; i++) {
    uint64_t pos = off + i;
    v = (v << 1) | (uint64_t)((buf[pos >> 3] >> (7 - (pos & 7))) & 1);
  }
  return v;
}

#define PK_S_PARSE_FIRST 0
#define PK_S_PARSE_VERSION 1
#define PK_S_PARSE_OTHER_CIDS 2
#define PK_S_PARSE_DCID_LEN 3
#define PK_S_PARSE_DCID 4
#define PK_S_PARSE_SCID_LEN 5
#define PK_S_PARSE_SCID 6
#define PK_S_PARSE_TOK_LEAD 7
#define PK_S_TOK_W1 8
#define PK_S_TOK_W2 9
#define PK_S_TOK_W4 10
#define PK_S_TOK_W8 11
#define PK_S_PARSE_LEN_LEAD 12
#define PK_S_LEN_W1 13
#define PK_S_LEN_W2 14
#define PK_S_LEN_W4 15
#define PK_S_LEN_W8 16
#define PK_S_PARSE_FIRST__SHORT 17
#define PK_S_PARSE_VERSION__NEGOTIATION 18
#define PK_S_PARSE_VERSION__DEFAULT 19
#define PK_S_PARSE_SCID__INITIAL 20
#define PK_S_PARSE_SCID__ZERO_RTT 21
#define PK_S_PARSE_SCID__HANDSHAKE 22
#define PK_S_PARSE_SCID__RETRY 23

static int pk_quic_initial_parse_core(const uint8_t *buf, uint64_t bit_len, pk_quic_initial_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_FIRST;
  uint32_t depth;
  out->m_kind = 0ULL;
  out->m_token_len = 0ULL;
  out->m_length = 0ULL;
  for (depth = 0; depth < 12u; depth++) {
    switch (state) {
    case PK_S_PARSE_FIRST: {
      out->first_byte_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->first_byte.form = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->first_byte.fixed = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->first_byte.ty = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->first_byte.low = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      uint64_t key0 = (uint64_t)out->first_byte.form;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_FIRST__SHORT;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_VERSION;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNREACHABLE__1_BIT_KEY;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_VERSION: {
      out->version_present = 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->version.v = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      uint64_t key0 = (uint64_t)out->version.v;
      if (key0 == 1ULL) {
        state = PK_S_PARSE_DCID_LEN;
        continue;
      } else if (key0 == 0ULL) {
        state = PK_S_PARSE_VERSION__NEGOTIATION;
        continue;
      } else {
        state = PK_S_PARSE_VERSION__DEFAULT;
        continue;
      }
    }
    case PK_S_PARSE_OTHER_CIDS: {
      out->other_cids_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->other_cids.dcid_len = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = (uint64_t)out->other_cids.dcid_len;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->other_cids.dcid_bit_off = off;
        out->other_cids.dcid_bit_len = vlen * 8;
        off += vlen * 8;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->other_cids.scid_len = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = (uint64_t)out->other_cids.scid_len;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->other_cids.scid_bit_off = off;
        out->other_cids.scid_bit_len = vlen * 8;
        off += vlen * 8;
      }
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_DCID_LEN: {
      out->dcid_len_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->dcid_len.ln = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->dcid_len.ln;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 2ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 3ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 5ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 7ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 8ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 9ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 10ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 11ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 12ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 13ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 14ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 15ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 16ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 18ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 19ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else if (key0 == 20ULL) {
        state = PK_S_PARSE_DCID;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_DCID_TOO_LONG_FOR_V1;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_DCID: {
      out->dcid_present = 1;
      {
        uint64_t vlen = (uint64_t)out->dcid_len.ln;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->dcid.cid_bit_off = off;
        out->dcid.cid_bit_len = vlen * 8;
        off += vlen * 8;
      }
      state = PK_S_PARSE_SCID_LEN;
      continue;
    }
    case PK_S_PARSE_SCID_LEN: {
      out->scid_len_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->scid_len.ln = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->scid_len.ln;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 2ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 3ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 5ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 7ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 8ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 9ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 10ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 11ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 12ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 13ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 14ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 15ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 16ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 18ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 19ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else if (key0 == 20ULL) {
        state = PK_S_PARSE_SCID;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_SCID_TOO_LONG_FOR_V1;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_SCID: {
      out->scid_present = 1;
      {
        uint64_t vlen = (uint64_t)out->scid_len.ln;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->scid.cid_bit_off = off;
        out->scid.cid_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->first_byte.ty;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_SCID__INITIAL;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_SCID__ZERO_RTT;
        continue;
      } else if (key0 == 2ULL) {
        state = PK_S_PARSE_SCID__HANDSHAKE;
        continue;
      } else if (key0 == 3ULL) {
        state = PK_S_PARSE_SCID__RETRY;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNREACHABLE__2_BIT_KEY;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_TOK_LEAD: {
      out->tok_lead_present = 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tok_lead.prefix = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tok_lead.v6 = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      uint64_t key0 = (uint64_t)out->tok_lead.prefix;
      if (key0 == 0ULL) {
        state = PK_S_TOK_W1;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_TOK_W2;
        continue;
      } else if (key0 == 2ULL) {
        state = PK_S_TOK_W4;
        continue;
      } else if (key0 == 3ULL) {
        state = PK_S_TOK_W8;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNREACHABLE__2_BIT_KEY;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_TOK_W1: {
      out->tok0_present = 1;
      {
        uint64_t vlen = (uint64_t)out->tok_lead.v6;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->tok0.body_bit_off = off;
        out->tok0.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      out->m_token_len = (uint64_t)out->tok_lead.v6;
      state = PK_S_PARSE_LEN_LEAD;
      continue;
    }
    case PK_S_TOK_W2: {
      out->tok1_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tok1.t = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = (((uint64_t)out->tok_lead.v6 << 8ULL) | (uint64_t)out->tok1.t);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->tok1.body_bit_off = off;
        out->tok1.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      out->m_token_len = (((uint64_t)out->tok_lead.v6 << 8ULL) | (uint64_t)out->tok1.t);
      state = PK_S_PARSE_LEN_LEAD;
      continue;
    }
    case PK_S_TOK_W4: {
      out->tok2_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tok2.t = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      {
        uint64_t vlen = (((uint64_t)out->tok_lead.v6 << 24ULL) | (uint64_t)out->tok2.t);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->tok2.body_bit_off = off;
        out->tok2.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      out->m_token_len = (((uint64_t)out->tok_lead.v6 << 24ULL) | (uint64_t)out->tok2.t);
      state = PK_S_PARSE_LEN_LEAD;
      continue;
    }
    case PK_S_TOK_W8: {
      out->tok3_present = 1;
      if (off + 56 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tok3.t = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 48) | ((uint64_t)buf[(off >> 3) + 1] << 40) | ((uint64_t)buf[(off >> 3) + 2] << 32) | ((uint64_t)buf[(off >> 3) + 3] << 24) | ((uint64_t)buf[(off >> 3) + 4] << 16) | ((uint64_t)buf[(off >> 3) + 5] << 8) | (uint64_t)buf[(off >> 3) + 6]);
      off += 56;
      {
        uint64_t vlen = (((uint64_t)out->tok_lead.v6 << 56ULL) | (uint64_t)out->tok3.t);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->tok3.body_bit_off = off;
        out->tok3.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      out->m_token_len = (((uint64_t)out->tok_lead.v6 << 56ULL) | (uint64_t)out->tok3.t);
      state = PK_S_PARSE_LEN_LEAD;
      continue;
    }
    case PK_S_PARSE_LEN_LEAD: {
      out->len_lead_present = 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->len_lead.prefix = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->len_lead.v6 = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      uint64_t key0 = (uint64_t)out->len_lead.prefix;
      if (key0 == 0ULL) {
        state = PK_S_LEN_W1;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_LEN_W2;
        continue;
      } else if (key0 == 2ULL) {
        state = PK_S_LEN_W4;
        continue;
      } else if (key0 == 3ULL) {
        state = PK_S_LEN_W8;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNREACHABLE__2_BIT_KEY;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_LEN_W1: {
      out->m_length = (uint64_t)out->len_lead.v6;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_LEN_W2: {
      out->len1_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->len1.t = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      out->m_length = (((uint64_t)out->len_lead.v6 << 8ULL) | (uint64_t)out->len1.t);
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_LEN_W4: {
      out->len2_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->len2.t = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      out->m_length = (((uint64_t)out->len_lead.v6 << 24ULL) | (uint64_t)out->len2.t);
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_LEN_W8: {
      out->len3_present = 1;
      if (off + 56 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->len3.t = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 48) | ((uint64_t)buf[(off >> 3) + 1] << 40) | ((uint64_t)buf[(off >> 3) + 2] << 32) | ((uint64_t)buf[(off >> 3) + 3] << 24) | ((uint64_t)buf[(off >> 3) + 4] << 16) | ((uint64_t)buf[(off >> 3) + 5] << 8) | (uint64_t)buf[(off >> 3) + 6]);
      off += 56;
      out->m_length = (((uint64_t)out->len_lead.v6 << 56ULL) | (uint64_t)out->len3.t);
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_FIRST__SHORT: {
      out->m_kind = (7ULL) & 0x7ULL;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_VERSION__NEGOTIATION: {
      out->m_kind = (5ULL) & 0x7ULL;
      state = PK_S_PARSE_OTHER_CIDS;
      continue;
    }
    case PK_S_PARSE_VERSION__DEFAULT: {
      out->m_kind = (6ULL) & 0x7ULL;
      state = PK_S_PARSE_OTHER_CIDS;
      continue;
    }
    case PK_S_PARSE_SCID__INITIAL: {
      out->m_kind = (1ULL) & 0x7ULL;
      state = PK_S_PARSE_TOK_LEAD;
      continue;
    }
    case PK_S_PARSE_SCID__ZERO_RTT: {
      out->m_kind = (2ULL) & 0x7ULL;
      state = PK_S_PARSE_LEN_LEAD;
      continue;
    }
    case PK_S_PARSE_SCID__HANDSHAKE: {
      out->m_kind = (3ULL) & 0x7ULL;
      state = PK_S_PARSE_LEN_LEAD;
      continue;
    }
    case PK_S_PARSE_SCID__RETRY: {
      out->m_kind = (4ULL) & 0x7ULL;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_quic_initial_parse(const uint8_t *buf, uint64_t bit_len, pk_quic_initial_result_t *out) {
  pk_quic_initial_result_t zero = {0};
  *out = zero;
  return pk_quic_initial_parse_core(buf, bit_len, out);
}

const char *pk_quic_initial_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  case 4: return "out of region bounds";
  case 5: return "region out of bounds";
  case 6: return "region not exhausted";
  case 16: return "dcid too long for v1";
  case 17: return "scid too long for v1";
  case 18: return "unreachable: 1-bit key";
  case 19: return "unreachable: 2-bit key";
  default: return "";
  }
}
