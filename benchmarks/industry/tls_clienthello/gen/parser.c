/* Generated by pakeles from `tls_clienthello`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

#define PK_S_S_RECORD 0
#define PK_S_S_RECVER 1
#define PK_S_S_HS 2
#define PK_S_S_FIXED 3
#define PK_S_S_SID_LEN 4
#define PK_S_S_SID 5
#define PK_S_S_CS_LEN 6
#define PK_S_S_CS_PARITY 7
#define PK_S_S_CS 8
#define PK_S_S_COMP_LEN 9
#define PK_S_S_COMP 10
#define PK_S_S_EXT_CHECK 11
#define PK_S_S_EXT_LEN 12
#define PK_S_S_TLV 13
#define PK_S_S_EXT 14
#define PK_S_S_SKIP 15
#define PK_S_S_SNI 16
#define PK_S_S_SNI_LIST 17
#define PK_S_S_SNI_ENTRY 18
#define PK_S_S_HOST 19
#define PK_S_S_DONE 20
#define PK_S_S_DONE_NOEXT 21

static int pk_tls_clienthello_parse_core(const uint8_t *buf, uint64_t bit_len, pk_tls_clienthello_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_S_RECORD;
  uint32_t depth;
#define PK_RMASK 7u
  uint64_t pk_region_end[8];
  uint32_t pk_rsp = 0;
  out->m_seen_sni = 0ULL;
  out->m_cs_odd = 0ULL;
  for (depth = 0; depth < 96u; depth++) {
    switch (state) {
    case PK_S_S_RECORD: {
      out->record_hdr_present = 1;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->record_hdr.ctype = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->record_hdr.ver_major = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->record_hdr.ver_minor = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->record_hdr.rlen = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      {
        uint64_t rlen = ((uint64_t)out->record_hdr.rlen * 8ULL);
        if (pk_rsp) {
          if (rlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
            out->outcome = 1;
            out->reason = PK_R_REGION_OUT_OF_BOUNDS;
            out->consumed_bits = off;
            return 1;
          }
        } else if (rlen > 0xffffffffffffffffULL - off) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (pk_rsp > PK_RMASK) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        pk_region_end[pk_rsp & PK_RMASK] = off + rlen;
        pk_rsp++;
      }
      uint64_t key0 = (uint64_t)out->record_hdr.ctype;
      if (key0 == 22ULL) {
        state = PK_S_S_RECVER;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_NOT_A_HANDSHAKE_RECORD;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_S_RECVER: {
      uint64_t key0 = (uint64_t)out->record_hdr.ver_major;
      if (key0 == 3ULL) {
        state = PK_S_S_HS;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNSUPPORTED_RECORD_VERSION;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_S_HS: {
      out->handshake_hdr_present = 1;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->handshake_hdr.typ = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (pk_rsp && off + 24 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->handshake_hdr.hlen = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      {
        uint64_t rlen = ((uint64_t)out->handshake_hdr.hlen * 8ULL);
        if (pk_rsp) {
          if (rlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
            out->outcome = 1;
            out->reason = PK_R_REGION_OUT_OF_BOUNDS;
            out->consumed_bits = off;
            return 1;
          }
        } else if (rlen > 0xffffffffffffffffULL - off) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (pk_rsp > PK_RMASK) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        pk_region_end[pk_rsp & PK_RMASK] = off + rlen;
        pk_rsp++;
      }
      uint64_t key0 = (uint64_t)out->handshake_hdr.typ;
      if (key0 == 1ULL) {
        state = PK_S_S_FIXED;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_NOT_A_CLIENT_HELLO;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_S_FIXED: {
      out->body_fixed_present = 1;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->body_fixed.ver = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      {
        uint64_t vlen = (32ULL * 8ULL);
        if (pk_rsp && vlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_REGION_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->body_fixed.random_bit_off = off;
        out->body_fixed.random_bit_len = vlen;
        off += vlen;
      }
      state = PK_S_S_SID_LEN;
      continue;
    }
    case PK_S_S_SID_LEN: {
      out->sid_len_present = 1;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sid_len.slen = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->sid_len.slen;
      if (key0 <= 32ULL) {
        state = PK_S_S_SID;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_SESSION_ID_TOO_LONG;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_S_SID: {
      out->sid_present = 1;
      {
        uint64_t vlen = ((uint64_t)out->sid_len.slen * 8ULL);
        if (pk_rsp && vlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_REGION_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->sid.body_bit_off = off;
        out->sid.body_bit_len = vlen;
        off += vlen;
      }
      state = PK_S_S_CS_LEN;
      continue;
    }
    case PK_S_S_CS_LEN: {
      out->cs_len_present = 1;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->cs_len.clen = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->m_cs_odd = (((uint64_t)out->cs_len.clen & 1ULL)) & 0x1ULL;
      uint64_t key0 = (uint64_t)out->cs_len.clen;
      if (key0 == 0ULL) {
        out->outcome = 1;
        out->reason = PK_R_EMPTY_CIPHER_SUITES;
        out->consumed_bits = off;
        return 1;
      } else {
        state = PK_S_S_CS_PARITY;
        continue;
      }
    }
    case PK_S_S_CS_PARITY: {
      uint64_t key0 = out->m_cs_odd;
      if (key0 == 1ULL) {
        out->outcome = 1;
        out->reason = PK_R_ODD_CIPHER_SUITES_LENGTH;
        out->consumed_bits = off;
        return 1;
      } else {
        state = PK_S_S_CS;
        continue;
      }
    }
    case PK_S_S_CS: {
      out->cs_present = 1;
      {
        uint64_t vlen = ((uint64_t)out->cs_len.clen * 8ULL);
        if (pk_rsp && vlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_REGION_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->cs.body_bit_off = off;
        out->cs.body_bit_len = vlen;
        off += vlen;
      }
      state = PK_S_S_COMP_LEN;
      continue;
    }
    case PK_S_S_COMP_LEN: {
      out->comp_len_present = 1;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->comp_len.plen = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->comp_len.plen;
      if (key0 == 0ULL) {
        out->outcome = 1;
        out->reason = PK_R_EMPTY_COMPRESSIONS;
        out->consumed_bits = off;
        return 1;
      } else {
        state = PK_S_S_COMP;
        continue;
      }
    }
    case PK_S_S_COMP: {
      out->comp_present = 1;
      {
        uint64_t vlen = ((uint64_t)out->comp_len.plen * 8ULL);
        if (pk_rsp && vlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_REGION_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->comp.body_bit_off = off;
        out->comp.body_bit_len = vlen;
        off += vlen;
      }
      state = PK_S_S_EXT_CHECK;
      continue;
    }
    case PK_S_S_EXT_CHECK: {
      uint64_t key0 = ((pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) >> 3ULL);
      if (key0 == 0ULL) {
        state = PK_S_S_DONE_NOEXT;
        continue;
      } else if (key0 == 1ULL) {
        out->outcome = 1;
        out->reason = PK_R_PARTIAL_EXTENSIONS_LENGTH;
        out->consumed_bits = off;
        return 1;
      } else {
        state = PK_S_S_EXT_LEN;
        continue;
      }
    }
    case PK_S_S_EXT_LEN: {
      out->ext_len_present = 1;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ext_len.total = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      {
        uint64_t rlen = ((uint64_t)out->ext_len.total * 8ULL);
        if (pk_rsp) {
          if (rlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
            out->outcome = 1;
            out->reason = PK_R_REGION_OUT_OF_BOUNDS;
            out->consumed_bits = off;
            return 1;
          }
        } else if (rlen > 0xffffffffffffffffULL - off) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (pk_rsp > PK_RMASK) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        pk_region_end[pk_rsp & PK_RMASK] = off + rlen;
        pk_rsp++;
      }
      state = PK_S_S_TLV;
      continue;
    }
    case PK_S_S_TLV: {
      uint64_t key0 = ((pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) >> 3ULL);
      if (key0 == 0ULL) {
        state = PK_S_S_DONE;
        continue;
      } else if (key0 == 1ULL) {
        out->outcome = 1;
        out->reason = PK_R_PARTIAL_EXTENSION_HEADER;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 2ULL) {
        out->outcome = 1;
        out->reason = PK_R_PARTIAL_EXTENSION_HEADER;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 3ULL) {
        out->outcome = 1;
        out->reason = PK_R_PARTIAL_EXTENSION_HEADER;
        out->consumed_bits = off;
        return 1;
      } else {
        state = PK_S_S_EXT;
        continue;
      }
    }
    case PK_S_S_EXT: {
      out->ext_present = 1;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ext.typ = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ext.elen = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->ext.typ;
      uint64_t key1 = out->m_seen_sni;
      if (key0 == 0ULL && key1 == 0ULL) {
        state = PK_S_S_SNI;
        continue;
      } else if (key0 == 0ULL && key1 == 1ULL) {
        out->outcome = 1;
        out->reason = PK_R_DUPLICATE_SNI;
        out->consumed_bits = off;
        return 1;
      } else {
        state = PK_S_S_SKIP;
        continue;
      }
    }
    case PK_S_S_SKIP: {
      out->skip_present = 1;
      {
        uint64_t vlen = ((uint64_t)out->ext.elen * 8ULL);
        if (pk_rsp && vlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_REGION_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->skip.body_bit_off = off;
        out->skip.body_bit_len = vlen;
        off += vlen;
      }
      state = PK_S_S_TLV;
      continue;
    }
    case PK_S_S_SNI: {
      out->m_seen_sni = (1ULL) & 0x1ULL;
      {
        uint64_t rlen = ((uint64_t)out->ext.elen * 8ULL);
        if (pk_rsp) {
          if (rlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
            out->outcome = 1;
            out->reason = PK_R_REGION_OUT_OF_BOUNDS;
            out->consumed_bits = off;
            return 1;
          }
        } else if (rlen > 0xffffffffffffffffULL - off) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (pk_rsp > PK_RMASK) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        pk_region_end[pk_rsp & PK_RMASK] = off + rlen;
        pk_rsp++;
      }
      state = PK_S_S_SNI_LIST;
      continue;
    }
    case PK_S_S_SNI_LIST: {
      out->sni_list_present = 1;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sni_list.list_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      {
        uint64_t rlen = ((uint64_t)out->sni_list.list_len * 8ULL);
        if (pk_rsp) {
          if (rlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
            out->outcome = 1;
            out->reason = PK_R_REGION_OUT_OF_BOUNDS;
            out->consumed_bits = off;
            return 1;
          }
        } else if (rlen > 0xffffffffffffffffULL - off) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (pk_rsp > PK_RMASK) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        pk_region_end[pk_rsp & PK_RMASK] = off + rlen;
        pk_rsp++;
      }
      state = PK_S_S_SNI_ENTRY;
      continue;
    }
    case PK_S_S_SNI_ENTRY: {
      out->sni_entry_present = 1;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sni_entry.ntype = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (pk_rsp && off + 16 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sni_entry.hlen = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->sni_entry.ntype;
      if (key0 == 0ULL) {
        state = PK_S_S_HOST;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_UNSUPPORTED_SNI_NAME_TYPE;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_S_HOST: {
      out->host_present = 1;
      {
        uint64_t vlen = ((uint64_t)out->sni_entry.hlen * 8ULL);
        if (pk_rsp && vlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_REGION_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->host.name_bit_off = off;
        out->host.name_bit_len = vlen;
        off += vlen;
      }
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      state = PK_S_S_TLV;
      continue;
    }
    case PK_S_S_DONE: {
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_S_DONE_NOEXT: {
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_tls_clienthello_parse(const uint8_t *buf, uint64_t bit_len, pk_tls_clienthello_result_t *out) {
  pk_tls_clienthello_result_t zero = {0};
  *out = zero;
  return pk_tls_clienthello_parse_core(buf, bit_len, out);
}

const char *pk_tls_clienthello_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  case 4: return "out of region bounds";
  case 5: return "region out of bounds";
  case 6: return "region not exhausted";
  case 16: return "duplicate sni";
  case 17: return "empty cipher suites";
  case 18: return "empty compressions";
  case 19: return "not a client hello";
  case 20: return "not a handshake record";
  case 21: return "odd cipher suites length";
  case 22: return "partial extension header";
  case 23: return "partial extensions length";
  case 24: return "session id too long";
  case 25: return "unsupported record version";
  case 26: return "unsupported sni name type";
  default: return "";
  }
}
