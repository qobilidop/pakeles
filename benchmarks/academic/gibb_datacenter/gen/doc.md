# `gibb_datacenter`

Generated by `pakeles doc` from IR version 0.2.0. Parse depth bound: 10.

## Header `ethernet`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `dst_addr` | 48 | MAC | Destination |  |  |
| `src_addr` | 48 | MAC | Source |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 37120=802.1Q (legacy 0x9100), 37376=802.1Q (legacy 0x9200), 37632=802.1Q (legacy 0x9300) |  |

## Header `vlan`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 |  | PCP |  |  |
| `cfi` | 1 |  | CFI |  |  |
| `vid` | 12 | dec | VLAN ID |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 37120=802.1Q (legacy 0x9100), 37376=802.1Q (legacy 0x9200), 37632=802.1Q (legacy 0x9300) |  |

## Header `ipv4`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `ihl` | 4 | dec | IHL |  |  |
| `diffserv` | 8 | hex | DiffServ |  |  |
| `total_len` | 16 | dec | Total Length |  |  |
| `identification` | 16 | hex | Identification |  |  |
| `flags` | 3 |  | Flags |  |  |
| `frag_offset` | 13 | dec | Fragment Offset |  |  |
| `ttl` | 8 | dec | TTL |  |  |
| `protocol` | 8 | dec | Protocol | 1=ICMP, 6=TCP, 17=UDP, 47=GRE |  |
| `hdr_checksum` | 16 | hex | Header Checksum |  |  |
| `src_addr` | 32 | hex | Source |  |  |
| `dst_addr` | 32 | hex | Destination |  |  |
| `options` | `((ipv4.ihl * 4) - 20)` bytes |  |  |  |  |

## Header `icmp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `type` | 8 | dec | Type |  |  |
| `code` | 8 | dec | Code |  |  |
| `hdr_checksum` | 16 | hex | Checksum |  |  |

## Header `tcp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port |  |  |
| `seq_no` | 32 | dec | Sequence Number |  |  |
| `ack_no` | 32 | dec | Ack Number |  |  |
| `data_offset` | 4 | dec | Data Offset |  |  |
| `res` | 3 |  | Reserved |  |  |
| `ecn` | 3 |  | ECN |  |  |
| `ctrl` | 6 |  | Control Bits |  |  |
| `window` | 16 | dec | Window |  |  |
| `checksum` | 16 | hex | Checksum |  |  |
| `urgent_ptr` | 16 | dec | Urgent Pointer |  |  |
| `options` | `((tcp.data_offset * 4) - 20)` bytes |  |  |  |  |

## Header `udp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port | 65535=VXLAN (pre-assignment placeholder port) |  |
| `length` | 16 | dec | Length |  |  |
| `checksum` | 16 | hex | Checksum |  |  |

## Header `gre`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `c` | 1 |  | C (Checksum Present) |  |  |
| `r` | 1 |  | R (Routing Present) |  |  |
| `k` | 1 |  | K (Key Present) |  |  |
| `s` | 1 |  | S (Sequence Present) |  |  |
| `strict` | 1 |  | s (Strict Source Route) |  |  |
| `recurse` | 3 |  | Recursion Control |  |  |
| `flags` | 5 |  | Flags |  |  |
| `ver` | 3 | dec | Version |  |  |
| `proto` | 16 | hex | Protocol | 25944=NVGRE inner (Transparent Ethernet Bridging), 25945=GRE-in-GRE |  |

## Header `nv_gre_inner`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `tni` | 24 | hex | TNI |  |  |
| `reserved` | 8 |  | Reserved |  |  |

## Header `vxlan`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `flags` | 8 | hex | Flags |  |  |
| `reserved` | 24 |  | Reserved |  |  |
| `vni` | 24 | hex | VNI |  |  |
| `reserved2` | 8 |  | Reserved |  |  |

## Header `arp_rarp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `hw_type` | 16 | dec | Hardware Type |  |  |
| `proto_type` | 16 | hex | Protocol Type | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 37120=802.1Q (legacy 0x9100), 37376=802.1Q (legacy 0x9200), 37632=802.1Q (legacy 0x9300) |  |
| `hw_addr_len` | 8 | dec | Hardware Address Length |  |  |
| `proto_addr_len` | 8 | dec | Protocol Address Length |  |  |
| `opcode` | 16 | dec | Opcode |  |  |

## Header `arp_rarp_ipv4`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_hw_addr` | 48 | MAC | Sender Hardware Address |  |  |
| `src_proto_addr` | 32 | hex | Sender Protocol Address |  |  |
| `dst_hw_addr` | 48 | MAC | Target Hardware Address |  |  |
| `dst_proto_addr` | 32 | hex | Target Protocol Address |  |  |

## Parse graph

Start state: `parse_ethernet`.

- **`parse_ethernet`** — extracts ethernet; selects on `ethernet.ether_type`:
  - ethernet.ether_type == 0x8100 → `parse_vlan1`
  - ethernet.ether_type == 0x9100 → `parse_vlan1`
  - ethernet.ether_type == 0x9200 → `parse_vlan1`
  - ethernet.ether_type == 0x9300 → `parse_vlan1`
  - ethernet.ether_type == 0x800 → `parse_ipv4`
  - ethernet.ether_type == 0x806 → `parse_arp_rarp`
  - ethernet.ether_type == 0x8035 → `parse_arp_rarp`
  - otherwise → **accept**
- **`parse_vlan1`** — extracts vlan; selects on `vlan.ether_type`:
  - vlan.ether_type == 0x8100 → `parse_vlan2`
  - vlan.ether_type == 0x9100 → `parse_vlan2`
  - vlan.ether_type == 0x9200 → `parse_vlan2`
  - vlan.ether_type == 0x9300 → `parse_vlan2`
  - vlan.ether_type == 0x800 → `parse_ipv4`
  - vlan.ether_type == 0x806 → `parse_arp_rarp`
  - vlan.ether_type == 0x8035 → `parse_arp_rarp`
  - otherwise → **accept**
  > First of the two bounded VLAN tags (`max = 2` in the
  > source, drawn as two nodes in the paper's graphs); the tag
  > dispatches exactly as Ethernet does.
- **`parse_vlan2`** — extracts vlan; selects on `vlan.ether_type`:
  - vlan.ether_type == 0x800 → `parse_ipv4`
  - vlan.ether_type == 0x806 → `parse_arp_rarp`
  - vlan.ether_type == 0x8035 → `parse_arp_rarp`
  - otherwise → **accept**
  > Second VLAN tag; a third falls to the default - beyond the
  > graph's bound is simply not a recognized sequence.
- **`parse_ipv4`** — extracts ipv4; selects on `ipv4.frag_offset`, `ipv4.protocol`:
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x1 → `parse_icmp`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x6 → `parse_tcp`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x11 → `parse_udp`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x2f → `parse_gre1`
  - otherwise → **accept**
  > L4 dispatch keys on (frag_offset, protocol) concatenated -
  > the source's `map(fragOffset, protocol)`, so only first
  > fragments dispatch.
- **`parse_icmp`** — extracts icmp; then **accept**
- **`parse_tcp`** — extracts tcp; then **accept**
- **`parse_udp`** — extracts udp; selects on `udp.dst_port`:
  - udp.dst_port == 0xffff → `parse_vxlan`
  - otherwise → **accept**
  > The VXLAN port is the source's 65535 [sic] - a made-up
  > placeholder that predates the IANA 4789 assignment.
- **`parse_gre1`** — extracts gre; selects on `gre.k`, `gre.proto`:
  - gre.k == 0x1 && gre.proto == 0x6558 → `parse_nv_gre_inner`
  - gre.k == 0x1 && gre.proto == 0x6559 → `parse_gre2`
  - otherwise → **accept**
  > First of the three bounded GRE headers (`max = 3` in the
  > source). Dispatch keys on (K, proto) concatenated - the
  > source's map values 0x16558/0x16559 are (K=1, EtherType).
- **`parse_gre2`** — extracts gre; selects on `gre.k`, `gre.proto`:
  - gre.k == 0x1 && gre.proto == 0x6558 → `parse_nv_gre_inner`
  - gre.k == 0x1 && gre.proto == 0x6559 → `parse_gre3`
  - otherwise → **accept**
- **`parse_gre3`** — extracts gre; selects on `gre.k`, `gre.proto`:
  - gre.k == 0x1 && gre.proto == 0x6558 → `parse_nv_gre_inner`
  - otherwise → **accept**
  > Third GRE header; a fourth nesting falls to the default -
  > beyond the graph's bound is simply not a recognized
  > sequence. The NVGRE arm stays.
- **`parse_nv_gre_inner`** — extracts nv_gre_inner; then `parse_ethernet2`
- **`parse_vxlan`** — extracts vxlan; then `parse_ethernet2`
- **`parse_ethernet2`** — extracts ethernet2; then **accept**
  > The source's `ethernet2`: the inner (tunneled) Ethernet
  > header, terminal. Same layout as `Ethernet` - reused as a
  > second instance.
- **`parse_arp_rarp`** — extracts arp_rarp; selects on `arp_rarp.proto_type`:
  - arp_rarp.proto_type == 0x800 → `parse_arp_rarp_ipv4`
  - otherwise → **accept**
  > Only IPv4-protocol ARP/RARP has a recognized body.
- **`parse_arp_rarp_ipv4`** — extracts arp_rarp_ipv4; then **accept**
