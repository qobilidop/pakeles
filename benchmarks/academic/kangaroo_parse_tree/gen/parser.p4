/* Generated by pakeles from `kangaroo_parse_tree`. Do not edit:
 * regenerate with `pakeles gen p4`. */
#include <core.p4>
#include <v1model.p4>

header ethernet_s0_t {
    bit<48> dst_addr;
    bit<48> src_addr;
    bit<16> ether_type;
}

header shim_tag_s0_t {
    bit<3> pcp;
    bit<1> cfi;
    bit<12> vid;
    bit<16> ether_type;
}

header ieee8021_ah_s0_t {
    bit<3> pcp;
    bit<1> dei;
    bit<1> uca;
    bit<3> reserved;
    bit<24> i_sid;
    bit<16> ether_type;
}

header mpls_s0_t {
    bit<20> label;
    bit<3> tc;
    bit<1> bos;
    bit<8> ttl;
}

header mpls_payload_nibble_s0_t {
    bit<4> v;
    bit<4> _pk_pad;
}

header ethernet2_s0_t {
    bit<48> dst_addr;
    bit<48> src_addr;
    bit<16> ether_type;
}

header ipv4_s0_t {
    bit<4> version;
    bit<4> ihl;
    bit<8> diffserv;
    bit<16> total_len;
    bit<16> identification;
    bit<3> flags;
    bit<13> frag_offset;
    bit<8> ttl;
    bit<8> protocol;
    bit<16> hdr_checksum;
    bit<32> src_addr;
    bit<32> dst_addr;
}

header ipv4_v1_t {
    varbit<320> options;
}

header ipv6_s0_t {
    bit<4> version;
    bit<8> traffic_class;
    bit<20> flow_label;
    bit<16> payload_len;
    bit<8> next_hdr;
    bit<8> hop_limit;
}

header ipv6_v1_t {
    varbit<128> src_addr;
}

header ipv6_v2_t {
    varbit<128> dst_addr;
}

header ipv6_ext_hdr_s0_t {
    bit<8> next_hdr;
    bit<8> hdr_ext_len;
}

header ipv6_ext_hdr_v1_t {
    varbit<16368> body;
}

header gre_s0_t {
    bit<1> c;
    bit<1> r;
    bit<1> k;
    bit<1> s;
    bit<1> strict;
    bit<3> recurse;
    bit<5> flags;
    bit<3> ver;
    bit<16> proto;
}

header gre_v1_t {
    varbit<96> opt;
}

header tcp_s0_t {
    bit<16> src_port;
    bit<16> dst_port;
    bit<32> seq_no;
    bit<32> ack_no;
    bit<4> data_offset;
    bit<3> res;
    bit<3> ecn;
    bit<6> ctrl;
    bit<16> window;
    bit<16> checksum;
    bit<16> urgent_ptr;
}

header tcp_v1_t {
    varbit<320> options;
}

header udp_s0_t {
    bit<16> src_port;
    bit<16> dst_port;
    bit<16> length;
    bit<16> checksum;
}

header icmp_s0_t {
    bit<8> type;
    bit<8> code;
    bit<16> hdr_checksum;
}

header icmpv6_s0_t {
    bit<8> type;
    bit<8> code;
    bit<16> hdr_checksum;
}

header ipsec_esp_s0_t {
    bit<32> spi;
    bit<32> seq_no;
}

header arp_rarp_s0_t {
    bit<16> hw_type;
    bit<16> proto_type;
    bit<8> hw_addr_len;
    bit<8> proto_addr_len;
    bit<16> opcode;
}

header verdict_t {
    bit<16> bitmap;
    bit<8> err;
}

struct headers {
    verdict_t verdict;
    ethernet_s0_t ethernet_s0;
    shim_tag_s0_t[13] shim_tag_s0;
    ieee8021_ah_s0_t ieee8021_ah_s0;
    mpls_s0_t[13] mpls_s0;
    mpls_payload_nibble_s0_t mpls_payload_nibble_s0;
    ethernet2_s0_t ethernet2_s0;
    ipv4_s0_t[13] ipv4_s0;
    ipv4_v1_t[13] ipv4_v1;
    ipv6_s0_t[13] ipv6_s0;
    ipv6_v1_t[13] ipv6_v1;
    ipv6_v2_t[13] ipv6_v2;
    ipv6_ext_hdr_s0_t ipv6_ext_hdr_s0;
    ipv6_ext_hdr_v1_t ipv6_ext_hdr_v1;
    gre_s0_t gre_s0;
    gre_v1_t gre_v1;
    tcp_s0_t tcp_s0;
    tcp_v1_t tcp_v1;
    udp_s0_t udp_s0;
    icmp_s0_t icmp_s0;
    icmpv6_s0_t icmpv6_s0;
    ipsec_esp_s0_t ipsec_esp_s0;
    arp_rarp_s0_t arp_rarp_s0;
}

struct metadata {
}

parser PkParser(packet_in pkt, out headers hdr, inout metadata meta,
                inout standard_metadata_t smeta) {
    state start {
        transition st_parse_ethernet;
    }
    state st_parse_ethernet {
        pkt.extract(hdr.ethernet_s0);
        transition select((bit<64>)hdr.ethernet_s0.ether_type) {
            64w33024: st_parse_vlan_q1;
            64w61440: st_parse_recirc;
            64w61696: st_parse_service;
            64w35047: st_parse_ieee8021ah;
            64w34984: st_parse_vlan_ad;
            64w34887: st_parse_mpls1;
            64w34888: st_parse_mpls1;
            64w2054: st_parse_arp_rarp;
            64w32821: st_parse_arp_rarp;
            64w2048: st_parse_ipv4;
            64w34525: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_vlan_q1 {
        pkt.extract(hdr.shim_tag_s0.next);
        transition select((bit<64>)hdr.shim_tag_s0.last.ether_type) {
            64w33024: st_parse_vlan_q2;
            64w34887: st_parse_mpls1;
            64w34888: st_parse_mpls1;
            64w2054: st_parse_arp_rarp;
            64w32821: st_parse_arp_rarp;
            64w2048: st_parse_ipv4;
            64w34525: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_vlan_q2 {
        pkt.extract(hdr.shim_tag_s0.next);
        transition select((bit<64>)hdr.shim_tag_s0.last.ether_type) {
            64w34887: st_parse_mpls1;
            64w34888: st_parse_mpls1;
            64w2054: st_parse_arp_rarp;
            64w32821: st_parse_arp_rarp;
            64w2048: st_parse_ipv4;
            64w34525: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_recirc {
        pkt.extract(hdr.shim_tag_s0.next);
        transition select((bit<64>)hdr.shim_tag_s0.last.ether_type) {
            64w34887: st_parse_mpls1;
            64w34888: st_parse_mpls1;
            64w2054: st_parse_arp_rarp;
            64w32821: st_parse_arp_rarp;
            64w2048: st_parse_ipv4;
            64w34525: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_service {
        pkt.extract(hdr.shim_tag_s0.next);
        transition select((bit<64>)hdr.shim_tag_s0.last.ether_type) {
            64w34887: st_parse_mpls1;
            64w34888: st_parse_mpls1;
            64w2054: st_parse_arp_rarp;
            64w32821: st_parse_arp_rarp;
            64w2048: st_parse_ipv4;
            64w34525: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_vlan_ad {
        pkt.extract(hdr.shim_tag_s0.next);
        transition select((bit<64>)hdr.shim_tag_s0.last.ether_type) {
            64w34887: st_parse_mpls1;
            64w34888: st_parse_mpls1;
            64w2054: st_parse_arp_rarp;
            64w32821: st_parse_arp_rarp;
            64w2048: st_parse_ipv4;
            64w34525: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_ieee8021ah {
        pkt.extract(hdr.ieee8021_ah_s0);
        transition select((bit<64>)hdr.ieee8021_ah_s0.ether_type) {
            64w34887: st_parse_mpls1;
            64w34888: st_parse_mpls1;
            64w2054: st_parse_arp_rarp;
            64w32821: st_parse_arp_rarp;
            64w2048: st_parse_ipv4;
            64w34525: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_mpls1 {
        pkt.extract(hdr.mpls_s0.next);
        transition select((bit<64>)hdr.mpls_s0.last.bos) {
            64w0: st_parse_mpls2;
            64w1: st_parse_mpls_payload;
            default: accept;
        }
    }
    state st_parse_mpls2 {
        pkt.extract(hdr.mpls_s0.next);
        transition select((bit<64>)hdr.mpls_s0.last.bos) {
            64w0: st_parse_mpls3;
            64w1: st_parse_mpls_payload;
            default: accept;
        }
    }
    state st_parse_mpls3 {
        pkt.extract(hdr.mpls_s0.next);
        transition select((bit<64>)hdr.mpls_s0.last.bos) {
            64w0: st_parse_mpls4;
            64w1: st_parse_mpls_payload;
            default: accept;
        }
    }
    state st_parse_mpls4 {
        pkt.extract(hdr.mpls_s0.next);
        transition select((bit<64>)hdr.mpls_s0.last.bos) {
            64w1: st_parse_mpls_payload;
            default: accept;
        }
    }
    state st_parse_mpls_payload {
        bit<4> pk_la_mpls_payload_nibble = pkt.lookahead<bit<4>>();
        hdr.mpls_payload_nibble_s0.setValid();
        hdr.mpls_payload_nibble_s0.v = pk_la_mpls_payload_nibble[3:0];
        hdr.mpls_payload_nibble_s0._pk_pad = 0;
        transition select((bit<64>)hdr.mpls_payload_nibble_s0.v) {
            64w0: st_parse_ethernet2;
            64w4: st_parse_ipv4;
            64w6: st_parse_ipv6;
            default: accept;
        }
    }
    state st_parse_ethernet2 {
        pkt.extract(hdr.ethernet2_s0);
        transition accept;
    }
    state st_parse_ipv4 {
        pkt.extract(hdr.ipv4_s0.next);
        pkt.extract(hdr.ipv4_v1.next, (bit<32>)((((bit<64>)hdr.ipv4_s0.last.ihl * 64w4) - 64w20) * 64w8));
        transition select((bit<64>)hdr.ipv4_s0.last.protocol) {
            64w1: st_parse_icmp;
            64w4: st_parse_ipv4_inner;
            64w6: st_parse_tcp;
            64w17: st_parse_udp;
            64w47: st_parse_gre;
            64w50: st_parse_ipsec_esp;
            default: accept;
        }
    }
    state st_parse_ipv6 {
        pkt.extract(hdr.ipv6_s0.next);
        pkt.extract(hdr.ipv6_v1.next, (bit<32>)(64w16 * 64w8));
        pkt.extract(hdr.ipv6_v2.next, (bit<32>)(64w16 * 64w8));
        transition select((bit<64>)hdr.ipv6_s0.last.next_hdr) {
            64w0: st_parse_ipv6_ext;
            64w1: st_parse_icmp;
            64w4: st_parse_ipv4_inner;
            64w6: st_parse_tcp;
            64w17: st_parse_udp;
            64w47: st_parse_gre;
            64w50: st_parse_ipsec_esp;
            default: accept;
        }
    }
    state st_parse_ipv6_ext {
        pkt.extract(hdr.ipv6_ext_hdr_s0);
        pkt.extract(hdr.ipv6_ext_hdr_v1, (bit<32>)((((64w1 + (bit<64>)hdr.ipv6_ext_hdr_s0.hdr_ext_len) << (bit<8>)(64w3)) - 64w2) * 64w8));
        transition select((bit<64>)hdr.ipv6_ext_hdr_s0.next_hdr) {
            64w6: st_parse_tcp;
            64w17: st_parse_udp;
            64w50: st_parse_ipsec_esp;
            64w58: st_parse_icmpv6;
            default: accept;
        }
    }
    state st_parse_gre {
        pkt.extract(hdr.gre_s0);
        pkt.extract(hdr.gre_v1, (bit<32>)(((((bit<64>)hdr.gre_s0.c * 64w4) + ((bit<64>)hdr.gre_s0.k * 64w4)) + ((bit<64>)hdr.gre_s0.s * 64w4)) * 64w8));
        transition select((bit<64>)hdr.gre_s0.proto) {
            64w2048: st_parse_ipv4_inner;
            64w34525: st_parse_ipv6_inner;
            default: accept;
        }
    }
    state st_parse_ipv4_inner {
        pkt.extract(hdr.ipv4_s0.next);
        pkt.extract(hdr.ipv4_v1.next, (bit<32>)((((bit<64>)hdr.ipv4_s0.last.ihl * 64w4) - 64w20) * 64w8));
        transition select((bit<64>)hdr.ipv4_s0.last.protocol) {
            64w1: st_parse_icmp;
            64w6: st_parse_tcp;
            64w17: st_parse_udp;
            64w50: st_parse_ipsec_esp;
            default: accept;
        }
    }
    state st_parse_ipv6_inner {
        pkt.extract(hdr.ipv6_s0.next);
        pkt.extract(hdr.ipv6_v1.next, (bit<32>)(64w16 * 64w8));
        pkt.extract(hdr.ipv6_v2.next, (bit<32>)(64w16 * 64w8));
        transition select((bit<64>)hdr.ipv6_s0.last.next_hdr) {
            64w1: st_parse_icmp;
            64w6: st_parse_tcp;
            64w17: st_parse_udp;
            64w50: st_parse_ipsec_esp;
            default: accept;
        }
    }
    state st_parse_tcp {
        pkt.extract(hdr.tcp_s0);
        pkt.extract(hdr.tcp_v1, (bit<32>)((((bit<64>)hdr.tcp_s0.data_offset * 64w4) - 64w20) * 64w8));
        transition accept;
    }
    state st_parse_udp {
        pkt.extract(hdr.udp_s0);
        transition accept;
    }
    state st_parse_icmp {
        pkt.extract(hdr.icmp_s0);
        transition accept;
    }
    state st_parse_icmpv6 {
        pkt.extract(hdr.icmpv6_s0);
        transition accept;
    }
    state st_parse_ipsec_esp {
        pkt.extract(hdr.ipsec_esp_s0);
        transition accept;
    }
    state st_parse_arp_rarp {
        pkt.extract(hdr.arp_rarp_s0);
        transition accept;
    }
}

control PkVerifyChecksum(inout headers hdr, inout metadata meta) {
    apply { }
}

control PkIngress(inout headers hdr, inout metadata meta,
                  inout standard_metadata_t smeta) {
    apply {
        hdr.verdict.setValid();
        bit<16> bm = 16w0;
        if (hdr.ethernet_s0.isValid()) { bm = bm | 16w1; }
        if (hdr.shim_tag_s0[0].isValid()) { bm = bm | 16w2; }
        if (hdr.ieee8021_ah_s0.isValid()) { bm = bm | 16w4; }
        if (hdr.mpls_s0[0].isValid()) { bm = bm | 16w8; }
        if (hdr.mpls_payload_nibble_s0.isValid()) { bm = bm | 16w16; }
        if (hdr.ethernet2_s0.isValid()) { bm = bm | 16w32; }
        if (hdr.ipv4_v1[0].isValid()) { bm = bm | 16w64; }
        if (hdr.ipv6_v2[0].isValid()) { bm = bm | 16w128; }
        if (hdr.ipv6_ext_hdr_v1.isValid()) { bm = bm | 16w256; }
        if (hdr.gre_v1.isValid()) { bm = bm | 16w512; }
        if (hdr.tcp_v1.isValid()) { bm = bm | 16w1024; }
        if (hdr.udp_s0.isValid()) { bm = bm | 16w2048; }
        if (hdr.icmp_s0.isValid()) { bm = bm | 16w4096; }
        if (hdr.icmpv6_s0.isValid()) { bm = bm | 16w8192; }
        if (hdr.ipsec_esp_s0.isValid()) { bm = bm | 16w16384; }
        if (hdr.arp_rarp_s0.isValid()) { bm = bm | 16w32768; }
        hdr.verdict.bitmap = bm;
        bit<8> err = 8w255;
        if (smeta.parser_error == error.NoError) { err = 8w0; }
        else if (smeta.parser_error == error.PacketTooShort) { err = 8w1; }
        else if (smeta.parser_error == error.NoMatch) { err = 8w2; }
        else if (smeta.parser_error == error.StackOutOfBounds) { err = 8w3; }
        else if (smeta.parser_error == error.HeaderTooShort) { err = 8w4; }
        else if (smeta.parser_error == error.ParserTimeout) { err = 8w5; }
        else if (smeta.parser_error == error.ParserInvalidArgument) { err = 8w6; }
        hdr.verdict.err = err;
        smeta.egress_spec = 9w1;
    }
}

control PkEgress(inout headers hdr, inout metadata meta,
                 inout standard_metadata_t smeta) {
    apply { }
}

control PkComputeChecksum(inout headers hdr, inout metadata meta) {
    apply { }
}

control PkDeparser(packet_out pkt, in headers hdr) {
    apply {
        pkt.emit(hdr.verdict);
    }
}

V1Switch(PkParser(), PkVerifyChecksum(), PkIngress(), PkEgress(),
         PkComputeChecksum(), PkDeparser()) main;
