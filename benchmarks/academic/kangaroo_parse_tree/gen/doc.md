# `kangaroo_parse_tree`

Generated by `pakeles doc` from IR version 0.2.0. Parse depth bound: 13.

## Header `ethernet`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `dst_addr` | 48 | MAC | Destination |  |  |
| `src_addr` | 48 | MAC | Source |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 34525=IPv6, 34887=MPLS (unicast), 34888=MPLS (multicast), 34984=802.1ad, 35047=802.1ah, 61440=recirc tag (PLACEHOLDER - Cisco-internal, unpublished), 61696=service tag (PLACEHOLDER - Cisco-internal, unpublished) |  |

## Header `shim_tag`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 |  | PCP |  |  |
| `cfi` | 1 |  | CFI |  |  |
| `vid` | 12 | dec | VLAN ID |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 34525=IPv6, 34887=MPLS (unicast), 34888=MPLS (multicast), 34984=802.1ad, 35047=802.1ah, 61440=recirc tag (PLACEHOLDER - Cisco-internal, unpublished), 61696=service tag (PLACEHOLDER - Cisco-internal, unpublished) |  |

## Header `ieee8021_ah`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 |  | PCP |  |  |
| `dei` | 1 |  | DEI |  |  |
| `uca` | 1 |  | UCA |  |  |
| `reserved` | 3 |  | Reserved |  |  |
| `i_sid` | 24 | dec | I-SID |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 34525=IPv6, 34887=MPLS (unicast), 34888=MPLS (multicast), 34984=802.1ad, 35047=802.1ah, 61440=recirc tag (PLACEHOLDER - Cisco-internal, unpublished), 61696=service tag (PLACEHOLDER - Cisco-internal, unpublished) |  |

## Header `mpls`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `label` | 20 | dec | Label |  |  |
| `tc` | 3 |  | Traffic Class |  |  |
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `ttl` | 8 | dec | TTL |  |  |

## Header `mpls_payload_nibble`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `v` | 4 | dec | Version Nibble | 0=Ethernet (EoMPLS), 4=IPv4, 6=IPv6 |  |

## Header `ipv4`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `ihl` | 4 | dec | IHL |  |  |
| `diffserv` | 8 | hex | DiffServ |  |  |
| `total_len` | 16 | dec | Total Length |  |  |
| `identification` | 16 | hex | Identification |  |  |
| `flags` | 3 |  | Flags |  |  |
| `frag_offset` | 13 | dec | Fragment Offset |  |  |
| `ttl` | 8 | dec | TTL |  |  |
| `protocol` | 8 | dec | Protocol | 0=IPv6 Hop-by-Hop Options, 1=ICMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 58=ICMPv6 |  |
| `hdr_checksum` | 16 | hex | Header Checksum |  |  |
| `src_addr` | 32 | hex | Source |  |  |
| `dst_addr` | 32 | hex | Destination |  |  |
| `options` | `((ipv4.ihl * 4) - 20)` bytes |  |  |  |  |

## Header `ipv6`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `traffic_class` | 8 | hex | Traffic Class |  |  |
| `flow_label` | 20 | hex | Flow Label |  |  |
| `payload_len` | 16 | dec | Payload Length |  |  |
| `next_hdr` | 8 | dec | Next Header | 0=IPv6 Hop-by-Hop Options, 1=ICMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 58=ICMPv6 |  |
| `hop_limit` | 8 | dec | Hop Limit |  |  |
| `src_addr` | `16` bytes |  |  |  |  |
| `dst_addr` | `16` bytes |  |  |  |  |

## Header `ipv6_ext_hdr`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `next_hdr` | 8 | dec | Next Header | 0=IPv6 Hop-by-Hop Options, 1=ICMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 58=ICMPv6 |  |
| `hdr_ext_len` | 8 | dec | Hdr Ext Len |  | in 8-octet units, excl. first 8 |
| `body` | `(((1 + ipv6_ext_hdr.hdr_ext_len) << 3) - 2)` bytes |  |  |  |  |

## Header `gre`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `c` | 1 |  | C (Checksum Present) |  |  |
| `r` | 1 |  | R (Routing Present) |  |  |
| `k` | 1 |  | K (Key Present) |  |  |
| `s` | 1 |  | S (Sequence Present) |  |  |
| `strict` | 1 |  | s (Strict Source Route) |  |  |
| `recurse` | 3 |  | Recursion Control |  |  |
| `flags` | 5 |  | Flags |  |  |
| `ver` | 3 |  | Version |  |  |
| `proto` | 16 | hex | Protocol Type | 2048=IPv4, 34525=IPv6 |  |
| `opt` | `(((gre.c * 4) + (gre.k * 4)) + (gre.s * 4))` bytes |  |  |  |  |

## Header `tcp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port |  |  |
| `seq_no` | 32 | dec | Sequence Number |  |  |
| `ack_no` | 32 | dec | Ack Number |  |  |
| `data_offset` | 4 | dec | Data Offset |  |  |
| `res` | 3 |  | Reserved |  |  |
| `ecn` | 3 |  | ECN |  |  |
| `ctrl` | 6 |  | Control Bits |  |  |
| `window` | 16 | dec | Window |  |  |
| `checksum` | 16 | hex | Checksum |  |  |
| `urgent_ptr` | 16 | dec | Urgent Pointer |  |  |
| `options` | `((tcp.data_offset * 4) - 20)` bytes |  |  |  |  |

## Header `udp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port |  |  |
| `length` | 16 | dec | Length |  |  |
| `checksum` | 16 | hex | Checksum |  |  |

## Header `icmp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `type` | 8 | dec | Type |  |  |
| `code` | 8 | dec | Code |  |  |
| `hdr_checksum` | 16 | hex | Checksum |  |  |

## Header `icmpv6`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `type` | 8 | dec | Type |  |  |
| `code` | 8 | dec | Code |  |  |
| `hdr_checksum` | 16 | hex | Checksum |  |  |

## Header `ipsec_esp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `spi` | 32 | hex | SPI |  |  |
| `seq_no` | 32 | dec | Sequence Number |  |  |

## Header `arp_rarp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `hw_type` | 16 | dec | Hardware Type |  |  |
| `proto_type` | 16 | hex | Protocol Type |  |  |
| `hw_addr_len` | 8 | dec | Hardware Address Length |  |  |
| `proto_addr_len` | 8 | dec | Protocol Address Length |  |  |
| `opcode` | 16 | dec | Opcode |  |  |

## Parse graph

Start state: `parse_ethernet`.

- **`parse_ethernet`** — extracts ethernet; selects on `ethernet.ether_type`:
  - ethernet.ether_type == 0x8100 → `parse_vlan_q1`
  - ethernet.ether_type == 0xf000 → `parse_recirc`
  - ethernet.ether_type == 0xf100 → `parse_service`
  - ethernet.ether_type == 0x88e7 → `parse_ieee8021ah`
  - ethernet.ether_type == 0x88a8 → `parse_vlan_ad`
  - ethernet.ether_type == 0x8847 → `parse_mpls1`
  - ethernet.ether_type == 0x8848 → `parse_mpls1`
  - ethernet.ether_type == 0x806 → `parse_arp_rarp`
  - ethernet.ether_type == 0x8035 → `parse_arp_rarp`
  - ethernet.ether_type == 0x800 → `parse_ipv4`
  - ethernet.ether_type == 0x86dd → `parse_ipv6`
  - otherwise → **accept**
- **`parse_vlan_q1`** — extracts shim_tag; selects on `shim_tag.ether_type`:
  - shim_tag.ether_type == 0x8100 → `parse_vlan_q2`
  - shim_tag.ether_type == 0x8847 → `parse_mpls1`
  - shim_tag.ether_type == 0x8848 → `parse_mpls1`
  - shim_tag.ether_type == 0x806 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x8035 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x800 → `parse_ipv4`
  - shim_tag.ether_type == 0x86dd → `parse_ipv6`
  - otherwise → **accept**
  > First 802.1Q tag; "nested 802.1q" is its own shim in the
  > prose, so exactly one more 802.1Q may follow.
- **`parse_vlan_q2`** — extracts shim_tag; selects on `shim_tag.ether_type`:
  - shim_tag.ether_type == 0x8847 → `parse_mpls1`
  - shim_tag.ether_type == 0x8848 → `parse_mpls1`
  - shim_tag.ether_type == 0x806 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x8035 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x800 → `parse_ipv4`
  - shim_tag.ether_type == 0x86dd → `parse_ipv6`
  - otherwise → **accept**
  > The nested 802.1Q tag; a third is not in the prose's shim
  > list and falls to the default.
- **`parse_recirc`** — extracts shim_tag; selects on `shim_tag.ether_type`:
  - shim_tag.ether_type == 0x8847 → `parse_mpls1`
  - shim_tag.ether_type == 0x8848 → `parse_mpls1`
  - shim_tag.ether_type == 0x806 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x8035 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x800 → `parse_ipv4`
  - shim_tag.ether_type == 0x86dd → `parse_ipv6`
  - otherwise → **accept**
  > Cisco-internal recirculation tag ("resirc" in the paper
  > [sic]); reached via a placeholder EtherType.
- **`parse_service`** — extracts shim_tag; selects on `shim_tag.ether_type`:
  - shim_tag.ether_type == 0x8847 → `parse_mpls1`
  - shim_tag.ether_type == 0x8848 → `parse_mpls1`
  - shim_tag.ether_type == 0x806 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x8035 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x800 → `parse_ipv4`
  - shim_tag.ether_type == 0x86dd → `parse_ipv6`
  - otherwise → **accept**
  > Cisco-internal service tag; placeholder EtherType.
- **`parse_vlan_ad`** — extracts shim_tag; selects on `shim_tag.ether_type`:
  - shim_tag.ether_type == 0x8847 → `parse_mpls1`
  - shim_tag.ether_type == 0x8848 → `parse_mpls1`
  - shim_tag.ether_type == 0x806 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x8035 → `parse_arp_rarp`
  - shim_tag.ether_type == 0x800 → `parse_ipv4`
  - shim_tag.ether_type == 0x86dd → `parse_ipv6`
  - otherwise → **accept**
- **`parse_ieee8021ah`** — extracts ieee8021_ah; selects on `ieee8021_ah.ether_type`:
  - ieee8021_ah.ether_type == 0x8847 → `parse_mpls1`
  - ieee8021_ah.ether_type == 0x8848 → `parse_mpls1`
  - ieee8021_ah.ether_type == 0x806 → `parse_arp_rarp`
  - ieee8021_ah.ether_type == 0x8035 → `parse_arp_rarp`
  - ieee8021_ah.ether_type == 0x800 → `parse_ipv4`
  - ieee8021_ah.ether_type == 0x86dd → `parse_ipv6`
  - otherwise → **accept**
- **`parse_mpls1`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls2`
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
  > First of the four bounded MPLS labels ("up to 4 MPLS
  > headers"); dispatch mechanism borrowed from the Gibb suite
  > (bos select + payload-nibble header).
- **`parse_mpls2`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls3`
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
- **`parse_mpls3`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls4`
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
- **`parse_mpls4`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
  > Fourth label: the bound is exhausted, so bos=0 (a fifth
  > label) falls to the default - not a recognized sequence.
- **`parse_mpls_payload`** — extracts mpls_payload_nibble (lookahead); selects on `mpls_payload_nibble.v`:
  - mpls_payload_nibble.v == 0x0 → `parse_ethernet2`
  - mpls_payload_nibble.v == 0x4 → `parse_ipv4`
  - mpls_payload_nibble.v == 0x6 → `parse_ipv6`
  - otherwise → **accept**
  > Bottom of stack: "MPLS is followed by Ethernet, IPv4, or
  > IPv6". Nibble 0 is the EoMPLS discriminator here (no control
  > word in this tree, unlike Gibb - see README).
- **`parse_ethernet2`** — extracts ethernet2; then **accept**
  > The inner Ethernet after MPLS - terminal (the prose gives
  > it no successors of its own).
- **`parse_ipv4`** — extracts ipv4; selects on `ipv4.protocol`:
  - ipv4.protocol == 0x1 → `parse_icmp`
  - ipv4.protocol == 0x4 → `parse_ipv4_inner`
  - ipv4.protocol == 0x6 → `parse_tcp`
  - ipv4.protocol == 0x11 → `parse_udp`
  - ipv4.protocol == 0x2f → `parse_gre`
  - ipv4.protocol == 0x32 → `parse_ipsec_esp`
  - otherwise → **accept**
  > Single-key dispatch on protocol - the prose never mentions
  > fragments (unlike the Gibb graphs' (fragOffset, protocol)).
- **`parse_ipv6`** — extracts ipv6; selects on `ipv6.next_hdr`:
  - ipv6.next_hdr == 0x0 → `parse_ipv6_ext`
  - ipv6.next_hdr == 0x1 → `parse_icmp`
  - ipv6.next_hdr == 0x4 → `parse_ipv4_inner`
  - ipv6.next_hdr == 0x6 → `parse_tcp`
  - ipv6.next_hdr == 0x11 → `parse_udp`
  - ipv6.next_hdr == 0x2f → `parse_gre`
  - ipv6.next_hdr == 0x32 → `parse_ipsec_esp`
  - otherwise → **accept**
- **`parse_ipv6_ext`** — extracts ipv6_ext_hdr; selects on `ipv6_ext_hdr.next_hdr`:
  - ipv6_ext_hdr.next_hdr == 0x6 → `parse_tcp`
  - ipv6_ext_hdr.next_hdr == 0x11 → `parse_udp`
  - ipv6_ext_hdr.next_hdr == 0x32 → `parse_ipsec_esp`
  - ipv6_ext_hdr.next_hdr == 0x3a → `parse_icmpv6`
  - otherwise → **accept**
  > "an IPv6 extension header which, in turn, is followed by
  > TCP, UDP, ESP or ICMPv6" - one level.
- **`parse_gre`** — extracts gre; selects on `gre.proto`:
  - gre.proto == 0x800 → `parse_ipv4_inner`
  - gre.proto == 0x86dd → `parse_ipv6_inner`
  - otherwise → **accept**
  > "GRE can also be followed by IPv4/IPv6" - a full inner IP
  > header, one tunnel level.
- **`parse_ipv4_inner`** — extracts ipv4; selects on `ipv4.protocol`:
  - ipv4.protocol == 0x1 → `parse_icmp`
  - ipv4.protocol == 0x6 → `parse_tcp`
  - ipv4.protocol == 0x11 → `parse_udp`
  - ipv4.protocol == 0x32 → `parse_ipsec_esp`
  - otherwise → **accept**
  > The "second IPv4 header" (and GRE's inner IPv4): one
  > tunnel level, so it dispatches L4 only - no further GRE or
  > IP-in-IP.
- **`parse_ipv6_inner`** — extracts ipv6; selects on `ipv6.next_hdr`:
  - ipv6.next_hdr == 0x1 → `parse_icmp`
  - ipv6.next_hdr == 0x6 → `parse_tcp`
  - ipv6.next_hdr == 0x11 → `parse_udp`
  - ipv6.next_hdr == 0x32 → `parse_ipsec_esp`
  - otherwise → **accept**
  > GRE's inner IPv6: L4 only, and no re-entry into the
  > extension-header chain (one-level tunnel reading).
- **`parse_tcp`** — extracts tcp; then **accept**
- **`parse_udp`** — extracts udp; then **accept**
- **`parse_icmp`** — extracts icmp; then **accept**
- **`parse_icmpv6`** — extracts icmpv6; then **accept**
- **`parse_ipsec_esp`** — extracts ipsec_esp; then **accept**
- **`parse_arp_rarp`** — extracts arp_rarp; then **accept**
  > Terminal - the prose lists ARP/RARP among the possible
  > followers but gives them no successors (and no ARP body node,
  > unlike the Gibb graphs).
