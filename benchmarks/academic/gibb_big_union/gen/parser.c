/* Generated by pakeles from `gibb_big_union`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

static uint64_t pk_read_bits(const uint8_t *buf, uint64_t avail, uint64_t off, uint32_t n) {
  (void)avail;
  uint64_t v = 0;
  uint32_t i;
  for (i = 0; i < n; i++) {
    uint64_t pos = off + i;
    v = (v << 1) | (uint64_t)((buf[pos >> 3] >> (7 - (pos & 7))) & 1);
  }
  return v;
}

#define PK_S_PARSE_ETHERNET 0
#define PK_S_PARSE_VLAN_Q1 1
#define PK_S_PARSE_VLAN_AD 2
#define PK_S_PARSE_VLAN_Q2 3
#define PK_S_PARSE_IEEE8021AH 4
#define PK_S_PARSE_MPLS1 5
#define PK_S_PARSE_MPLS2 6
#define PK_S_PARSE_MPLS3 7
#define PK_S_PARSE_MPLS4 8
#define PK_S_PARSE_MPLS5 9
#define PK_S_PARSE_MPLS_PAYLOAD 10
#define PK_S_PARSE_EOMPLS 11
#define PK_S_PARSE_IPV4 12
#define PK_S_PARSE_IPV6 13
#define PK_S_PARSE_ICMP 14
#define PK_S_PARSE_ICMPV6 15
#define PK_S_PARSE_TCP 16
#define PK_S_PARSE_UDP 17
#define PK_S_PARSE_VXLAN 18
#define PK_S_PARSE_SCTP 19
#define PK_S_PARSE_GRE1 20
#define PK_S_PARSE_GRE2 21
#define PK_S_PARSE_GRE3 22
#define PK_S_PARSE_NV_GRE_INNER 23
#define PK_S_PARSE_IPSEC_ESP 24
#define PK_S_PARSE_IPSEC_AH 25
#define PK_S_PARSE_ARP_RARP 26
#define PK_S_PARSE_ARP_RARP_IPV4 27
#define PK_S_PARSE_ETHERNET2 28

static int pk_gibb_big_union_parse_core(const uint8_t *buf, uint64_t bit_len, pk_gibb_big_union_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_ETHERNET;
  uint32_t depth;
  for (depth = 0; depth < 17u; depth++) {
    switch (state) {
    case PK_S_PARSE_ETHERNET: {
      out->ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.dst_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.src_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->ethernet.ether_type;
      if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN_Q1;
        continue;
      } else if (key0 == 37120ULL) {
        state = PK_S_PARSE_VLAN_Q1;
        continue;
      } else if (key0 == 37376ULL) {
        state = PK_S_PARSE_VLAN_Q1;
        continue;
      } else if (key0 == 37632ULL) {
        state = PK_S_PARSE_VLAN_Q1;
        continue;
      } else if (key0 == 34984ULL) {
        state = PK_S_PARSE_VLAN_AD;
        continue;
      } else if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS1;
        continue;
      } else if (key0 == 34888ULL) {
        state = PK_S_PARSE_MPLS1;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 32821ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VLAN_Q1: {
      out->vlan_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.cfi = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.vid = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan.ether_type;
      if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 37120ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 37376ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 37632ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS1;
        continue;
      } else if (key0 == 34888ULL) {
        state = PK_S_PARSE_MPLS1;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 32821ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VLAN_AD: {
      out->ieee8021_ad_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ad.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ad.cfi = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ad.vid = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ad.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->ieee8021_ad.ether_type;
      if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 37120ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 37376ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 37632ULL) {
        state = PK_S_PARSE_VLAN_Q2;
        continue;
      } else if (key0 == 35047ULL) {
        state = PK_S_PARSE_IEEE8021AH;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VLAN_Q2: {
      out->vlan_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.cfi = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.vid = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan.ether_type;
      if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS1;
        continue;
      } else if (key0 == 34888ULL) {
        state = PK_S_PARSE_MPLS1;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 32821ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IEEE8021AH: {
      out->ieee8021_ah_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ah.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ah.dei = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ah.uca = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ah.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ieee8021_ah.i_sid = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      state = PK_S_PARSE_ETHERNET2;
      continue;
    }
    case PK_S_PARSE_MPLS1: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.tc = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_MPLS2;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_PAYLOAD;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS2: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.tc = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_MPLS3;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_PAYLOAD;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS3: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.tc = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_MPLS4;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_PAYLOAD;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS4: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.tc = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_MPLS5;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_PAYLOAD;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS5: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.tc = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_PAYLOAD;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS_PAYLOAD: {
      out->mpls_payload_nibble_present = 1;
      { /* lookahead: cursor does not advance */
      uint64_t poff = off;
      if (poff + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls_payload_nibble.v = (uint8_t)pk_read_bits(buf, bit_len, poff, 4);
      poff += 4;
      }
      uint64_t key0 = (uint64_t)out->mpls_payload_nibble.v;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_EOMPLS;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_EOMPLS: {
      out->eompls_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->eompls.zero = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->eompls.reserved = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->eompls.seq_no = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      state = PK_S_PARSE_ETHERNET2;
      continue;
    }
    case PK_S_PARSE_IPV4: {
      out->ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ihl = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.diffserv = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.total_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.identification = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.frag_offset = (uint16_t)pk_read_bits(buf, bit_len, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.protocol = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.src_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dst_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      {
        uint64_t vlen = ((((uint64_t)out->ipv4.ihl * 4ULL) - 20ULL) * 8ULL);
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv4.options_bit_off = off;
        out->ipv4.options_bit_len = vlen;
        off += vlen;
      }
      uint64_t key0 = (uint64_t)out->ipv4.frag_offset;
      uint64_t key1 = (uint64_t)out->ipv4.protocol;
      if (key0 == 0ULL && key1 == 1ULL) {
        state = PK_S_PARSE_ICMP;
        continue;
      } else if (key0 == 0ULL && key1 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 0ULL && key1 == 17ULL) {
        state = PK_S_PARSE_UDP;
        continue;
      } else if (key0 == 0ULL && key1 == 47ULL) {
        state = PK_S_PARSE_GRE1;
        continue;
      } else if (key0 == 0ULL && key1 == 50ULL) {
        state = PK_S_PARSE_IPSEC_ESP;
        continue;
      } else if (key0 == 0ULL && key1 == 51ULL) {
        state = PK_S_PARSE_IPSEC_AH;
        continue;
      } else if (key0 == 0ULL && key1 == 132ULL) {
        state = PK_S_PARSE_SCTP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IPV6: {
      out->ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.traffic_class = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.flow_label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.payload_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.next_hdr = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.hop_limit = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = (16ULL * 8ULL);
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.src_addr_bit_off = off;
        out->ipv6.src_addr_bit_len = vlen;
        off += vlen;
      }
      {
        uint64_t vlen = (16ULL * 8ULL);
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.dst_addr_bit_off = off;
        out->ipv6.dst_addr_bit_len = vlen;
        off += vlen;
      }
      uint64_t key0 = (uint64_t)out->ipv6.next_hdr;
      if (key0 == 58ULL) {
        state = PK_S_PARSE_ICMPV6;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_UDP;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE1;
        continue;
      } else if (key0 == 50ULL) {
        state = PK_S_PARSE_IPSEC_ESP;
        continue;
      } else if (key0 == 51ULL) {
        state = PK_S_PARSE_IPSEC_AH;
        continue;
      } else if (key0 == 132ULL) {
        state = PK_S_PARSE_SCTP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ICMP: {
      out->icmp_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.type = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.code = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_ICMPV6: {
      out->icmpv6_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmpv6.type = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmpv6.code = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmpv6.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_TCP: {
      out->tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.seq_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ack_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.data_offset = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.res = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ecn = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ctrl = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.window = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.urgent_ptr = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      {
        uint64_t vlen = ((((uint64_t)out->tcp.data_offset * 4ULL) - 20ULL) * 8ULL);
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->tcp.options_bit_off = off;
        out->tcp.options_bit_len = vlen;
        off += vlen;
      }
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_UDP: {
      out->udp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.length = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->udp.dst_port;
      if (key0 == 65535ULL) {
        state = PK_S_PARSE_VXLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VXLAN: {
      out->vxlan_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.flags = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.reserved = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.vni = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.reserved2 = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      state = PK_S_PARSE_ETHERNET2;
      continue;
    }
    case PK_S_PARSE_SCTP: {
      out->sctp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.verif_tag = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.checksum = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_GRE1: {
      out->gre_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.c = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.r = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.k = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.s = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.strict = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.recurse = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.ver = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.proto = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->gre.k;
      uint64_t key1 = (uint64_t)out->gre.proto;
      if (key0 == 1ULL && key1 == 25944ULL) {
        state = PK_S_PARSE_NV_GRE_INNER;
        continue;
      } else if (key0 == 1ULL && key1 == 25945ULL) {
        state = PK_S_PARSE_GRE2;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_GRE2: {
      out->gre_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.c = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.r = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.k = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.s = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.strict = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.recurse = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.ver = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.proto = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->gre.k;
      uint64_t key1 = (uint64_t)out->gre.proto;
      if (key0 == 1ULL && key1 == 25944ULL) {
        state = PK_S_PARSE_NV_GRE_INNER;
        continue;
      } else if (key0 == 1ULL && key1 == 25945ULL) {
        state = PK_S_PARSE_GRE3;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_GRE3: {
      out->gre_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.c = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.r = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.k = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.s = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.strict = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.recurse = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.ver = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.proto = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->gre.k;
      uint64_t key1 = (uint64_t)out->gre.proto;
      if (key0 == 1ULL && key1 == 25944ULL) {
        state = PK_S_PARSE_NV_GRE_INNER;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_NV_GRE_INNER: {
      out->nv_gre_inner_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nv_gre_inner.tni = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nv_gre_inner.reserved = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      state = PK_S_PARSE_ETHERNET2;
      continue;
    }
    case PK_S_PARSE_IPSEC_ESP: {
      out->ipsec_esp_present = 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipsec_esp.spi = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipsec_esp.seq_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_IPSEC_AH: {
      out->ipsec_ah_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipsec_ah.next_hdr = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipsec_ah.length = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipsec_ah.zero = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipsec_ah.spi = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipsec_ah.seq_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      uint64_t key0 = (uint64_t)out->ipsec_ah.next_hdr;
      if (key0 == 1ULL) {
        state = PK_S_PARSE_ICMP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_UDP;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE1;
        continue;
      } else if (key0 == 58ULL) {
        state = PK_S_PARSE_ICMPV6;
        continue;
      } else if (key0 == 132ULL) {
        state = PK_S_PARSE_SCTP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ARP_RARP: {
      out->arp_rarp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp.hw_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp.proto_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp.hw_addr_len = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp.proto_addr_len = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp.opcode = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->arp_rarp.proto_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_ARP_RARP_IPV4;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ARP_RARP_IPV4: {
      out->arp_rarp_ipv4_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp_ipv4.src_hw_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp_ipv4.src_proto_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp_ipv4.dst_hw_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp_rarp_ipv4.dst_proto_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_ETHERNET2: {
      out->ethernet2_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet2.dst_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet2.src_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet2.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_gibb_big_union_parse(const uint8_t *buf, uint64_t bit_len, pk_gibb_big_union_result_t *out) {
  pk_gibb_big_union_result_t zero = {0};
  *out = zero;
  return pk_gibb_big_union_parse_core(buf, bit_len, out);
}

const char *pk_gibb_big_union_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  case 4: return "out of region bounds";
  case 5: return "region out of bounds";
  case 6: return "region not exhausted";
  default: return "";
  }
}
