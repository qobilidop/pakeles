# `gibb_big_union`

Generated by `pakeles doc` from IR version 0.1.0. Parse depth bound: 17.

## Header `ethernet`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `dst_addr` | 48 | MAC | Destination |  |  |
| `src_addr` | 48 | MAC | Source |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 34525=IPv6, 34887=MPLS (unicast), 34888=MPLS (multicast), 34984=802.1ad, 35047=802.1ah (PBB), 37120=802.1Q (legacy 0x9100), 37376=802.1Q (legacy 0x9200), 37632=802.1Q (legacy 0x9300) |  |

## Header `vlan`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 |  | PCP |  |  |
| `cfi` | 1 |  | CFI |  |  |
| `vid` | 12 | dec | VLAN ID |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 34525=IPv6, 34887=MPLS (unicast), 34888=MPLS (multicast), 34984=802.1ad, 35047=802.1ah (PBB), 37120=802.1Q (legacy 0x9100), 37376=802.1Q (legacy 0x9200), 37632=802.1Q (legacy 0x9300) |  |

## Header `ieee8021_ad`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 |  | PCP |  |  |
| `cfi` | 1 |  | CFI |  |  |
| `vid` | 12 | dec | VLAN ID |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 32821=RARP, 33024=802.1Q, 34525=IPv6, 34887=MPLS (unicast), 34888=MPLS (multicast), 34984=802.1ad, 35047=802.1ah (PBB), 37120=802.1Q (legacy 0x9100), 37376=802.1Q (legacy 0x9200), 37632=802.1Q (legacy 0x9300) |  |

## Header `ieee8021_ah`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 |  | PCP |  |  |
| `dei` | 1 |  | DEI |  |  |
| `uca` | 1 |  | UCA |  |  |
| `reserved` | 3 |  | Reserved |  |  |
| `i_sid` | 24 | dec | I-SID |  |  |

## Header `mpls`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `label` | 20 | dec | Label |  |  |
| `tc` | 3 |  | Traffic Class |  |  |
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `ttl` | 8 | dec | TTL |  |  |

## Header `mpls_payload_nibble`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `v` | 4 | dec | Version Nibble | 0=EoMPLS, 4=IPv4, 6=IPv6 |  |

## Header `eompls_rest`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `reserved` | 12 |  | Reserved |  |  |
| `seq_no` | 16 | dec | Sequence Number |  |  |

## Header `ipv4`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `ihl` | 4 | dec | IHL |  |  |
| `diffserv` | 8 | hex | DiffServ |  |  |
| `total_len` | 16 | dec | Total Length |  |  |
| `identification` | 16 | hex | Identification |  |  |
| `flags` | 3 |  | Flags |  |  |
| `frag_offset` | 13 | dec | Fragment Offset |  |  |
| `ttl` | 8 | dec | TTL |  |  |
| `protocol` | 8 | dec | Protocol | 1=ICMP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 51=IPsec AH, 58=ICMPv6, 132=SCTP |  |
| `hdr_checksum` | 16 | hex | Header Checksum |  |  |
| `src_addr` | 32 | hex | Source |  |  |
| `dst_addr` | 32 | hex | Destination |  |  |
| `options` | `((ipv4.ihl * 4) - 20)` bytes |  |  |  |  |

## Header `ipv4_rest`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `ihl` | 4 | dec | IHL |  |  |
| `diffserv` | 8 | hex | DiffServ |  |  |
| `total_len` | 16 | dec | Total Length |  |  |
| `identification` | 16 | hex | Identification |  |  |
| `flags` | 3 |  | Flags |  |  |
| `frag_offset` | 13 | dec | Fragment Offset |  |  |
| `ttl` | 8 | dec | TTL |  |  |
| `protocol` | 8 | dec | Protocol | 1=ICMP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 51=IPsec AH, 58=ICMPv6, 132=SCTP |  |
| `hdr_checksum` | 16 | hex | Header Checksum |  |  |
| `src_addr` | 32 | hex | Source |  |  |
| `dst_addr` | 32 | hex | Destination |  |  |
| `options` | `((ipv4_rest.ihl * 4) - 20)` bytes |  |  |  |  |

## Header `ipv6`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `traffic_class` | 8 | hex | Traffic Class |  |  |
| `flow_label` | 20 | hex | Flow Label |  |  |
| `payload_len` | 16 | dec | Payload Length |  |  |
| `next_hdr` | 8 | dec | Next Header | 1=ICMP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 51=IPsec AH, 58=ICMPv6, 132=SCTP |  |
| `hop_limit` | 8 | dec | Hop Limit |  |  |
| `src_addr` | `16` bytes |  |  |  |  |
| `dst_addr` | `16` bytes |  |  |  |  |

## Header `ipv6_rest`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `traffic_class` | 8 | hex | Traffic Class |  |  |
| `flow_label` | 20 | hex | Flow Label |  |  |
| `payload_len` | 16 | dec | Payload Length |  |  |
| `next_hdr` | 8 | dec | Next Header | 1=ICMP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 51=IPsec AH, 58=ICMPv6, 132=SCTP |  |
| `hop_limit` | 8 | dec | Hop Limit |  |  |
| `src_addr` | `16` bytes |  |  |  |  |
| `dst_addr` | `16` bytes |  |  |  |  |

## Header `icmp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `type` | 8 | dec | Type |  |  |
| `code` | 8 | dec | Code |  |  |
| `hdr_checksum` | 16 | hex | Checksum |  |  |

## Header `icmpv6`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `type` | 8 | dec | Type |  |  |
| `code` | 8 | dec | Code |  |  |
| `hdr_checksum` | 16 | hex | Checksum |  |  |

## Header `tcp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port |  |  |
| `seq_no` | 32 | dec | Sequence Number |  |  |
| `ack_no` | 32 | dec | Ack Number |  |  |
| `data_offset` | 4 | dec | Data Offset |  |  |
| `res` | 3 |  | Reserved |  |  |
| `ecn` | 3 |  | ECN |  |  |
| `ctrl` | 6 |  | Control Bits |  |  |
| `window` | 16 | dec | Window |  |  |
| `checksum` | 16 | hex | Checksum |  |  |
| `urgent_ptr` | 16 | dec | Urgent Pointer |  |  |
| `options` | `((tcp.data_offset * 4) - 20)` bytes |  |  |  |  |

## Header `udp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port | 65535=VXLAN (the source's made-up pre-assignment port) |  |
| `length` | 16 | dec | Length |  |  |
| `checksum` | 16 | hex | Checksum |  |  |

## Header `vxlan`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `flags` | 8 | hex | Flags |  |  |
| `reserved` | 24 |  | Reserved |  |  |
| `vni` | 24 | dec | VNI |  |  |
| `reserved2` | 8 |  | Reserved |  |  |

## Header `sctp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port |  |  |
| `verif_tag` | 32 | hex | Verification Tag |  |  |
| `checksum` | 32 | hex | Checksum |  |  |

## Header `gre`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `c` | 1 |  | C (Checksum Present) |  |  |
| `r` | 1 |  | R (Routing Present) |  |  |
| `k` | 1 |  | K (Key Present) |  |  |
| `s` | 1 |  | S (Sequence Present) |  |  |
| `strict` | 1 |  | s (Strict Source Route) |  |  |
| `recurse` | 3 |  | Recursion Control |  |  |
| `flags` | 5 |  | Flags |  |  |
| `ver` | 3 |  | Version |  |  |
| `proto` | 16 | hex | Protocol Type | 25944=Transparent Ethernet Bridging (NVGRE), 25945=nested GRE (the source's GRE-in-GRE arm) |  |

## Header `nv_gre_inner`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `tni` | 24 | dec | TNI |  |  |
| `reserved` | 8 |  | Reserved |  |  |

## Header `ipsec_esp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `spi` | 32 | hex | SPI |  |  |
| `seq_no` | 32 | dec | Sequence Number |  |  |

## Header `ipsec_ah`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `next_hdr` | 8 | dec | Next Header | 1=ICMP, 6=TCP, 17=UDP, 47=GRE, 50=IPsec ESP, 51=IPsec AH, 58=ICMPv6, 132=SCTP |  |
| `length` | 8 | dec | Length |  |  |
| `zero` | 16 |  | Reserved |  |  |
| `spi` | 32 | hex | SPI |  |  |
| `seq_no` | 32 | dec | Sequence Number |  |  |

## Header `arp_rarp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `hw_type` | 16 | dec | Hardware Type |  |  |
| `proto_type` | 16 | hex | Protocol Type |  |  |
| `hw_addr_len` | 8 | dec | Hardware Address Length |  |  |
| `proto_addr_len` | 8 | dec | Protocol Address Length |  |  |
| `opcode` | 16 | dec | Opcode |  |  |

## Header `arp_rarp_ipv4`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_hw_addr` | 48 | MAC | Sender Hardware Address |  |  |
| `src_proto_addr` | 32 | hex | Sender Protocol Address |  |  |
| `dst_hw_addr` | 48 | MAC | Target Hardware Address |  |  |
| `dst_proto_addr` | 32 | hex | Target Protocol Address |  |  |

## Parse graph

Start state: `parse_ethernet`.

- **`parse_ethernet`** — extracts ethernet; selects on `ethernet.ether_type`:
  - ethernet.ether_type == 0x8100 → `parse_vlan_q1`
  - ethernet.ether_type == 0x9100 → `parse_vlan_q1`
  - ethernet.ether_type == 0x9200 → `parse_vlan_q1`
  - ethernet.ether_type == 0x9300 → `parse_vlan_q1`
  - ethernet.ether_type == 0x88a8 → `parse_vlan_ad`
  - ethernet.ether_type == 0x8847 → `parse_mpls1`
  - ethernet.ether_type == 0x8848 → `parse_mpls1`
  - ethernet.ether_type == 0x800 → `parse_ipv4`
  - ethernet.ether_type == 0x86dd → `parse_ipv6`
  - ethernet.ether_type == 0x806 → `parse_arp_rarp`
  - ethernet.ether_type == 0x8035 → `parse_arp_rarp`
  - otherwise → **accept**
- **`parse_vlan_q1`** — extracts vlan; selects on `vlan.ether_type`:
  - vlan.ether_type == 0x8100 → `parse_vlan_q2`
  - vlan.ether_type == 0x9100 → `parse_vlan_q2`
  - vlan.ether_type == 0x9200 → `parse_vlan_q2`
  - vlan.ether_type == 0x9300 → `parse_vlan_q2`
  - vlan.ether_type == 0x8847 → `parse_mpls1`
  - vlan.ether_type == 0x8848 → `parse_mpls1`
  - vlan.ether_type == 0x800 → `parse_ipv4`
  - vlan.ether_type == 0x86dd → `parse_ipv6`
  - vlan.ether_type == 0x806 → `parse_arp_rarp`
  - vlan.ether_type == 0x8035 → `parse_arp_rarp`
  - otherwise → **accept**
  > First 802.1Q tag. 802.1Q and 802.1ad share one two-tag
  > budget (the source's shared `max_var = vlan` counter), so a
  > second tag of either entry continues at `parse_vlan_q2`.
- **`parse_vlan_ad`** — extracts ieee8021_ad; selects on `ieee8021_ad.ether_type`:
  - ieee8021_ad.ether_type == 0x8100 → `parse_vlan_q2`
  - ieee8021_ad.ether_type == 0x9100 → `parse_vlan_q2`
  - ieee8021_ad.ether_type == 0x9200 → `parse_vlan_q2`
  - ieee8021_ad.ether_type == 0x9300 → `parse_vlan_q2`
  - ieee8021_ad.ether_type == 0x88e7 → `parse_ieee8021ah`
  - otherwise → **accept**
  > First tag, 802.1ad entry: followed only by an 802.1Q tag
  > (spending the shared budget's second slot) or 802.1ah/PBB.
- **`parse_vlan_q2`** — extracts vlan; selects on `vlan.ether_type`:
  - vlan.ether_type == 0x8847 → `parse_mpls1`
  - vlan.ether_type == 0x8848 → `parse_mpls1`
  - vlan.ether_type == 0x800 → `parse_ipv4`
  - vlan.ether_type == 0x86dd → `parse_ipv6`
  - vlan.ether_type == 0x806 → `parse_arp_rarp`
  - vlan.ether_type == 0x8035 → `parse_arp_rarp`
  - otherwise → **accept**
  > Second tag (either entry): the shared budget is exhausted,
  > so further VLAN TPIDs fall to the default.
- **`parse_ieee8021ah`** — extracts ieee8021_ah; then `parse_ethernet2`
- **`parse_mpls1`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls2`
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
  > First of the five bounded MPLS labels (`max = 5`).
- **`parse_mpls2`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls3`
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
- **`parse_mpls3`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls4`
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
- **`parse_mpls4`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls5`
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
- **`parse_mpls5`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x1 → `parse_mpls_payload`
  - otherwise → **accept**
  > Fifth label: the bound is exhausted, so bos=0 (a sixth
  > label) falls to the default - not a recognized sequence.
- **`parse_mpls_payload`** — extracts mpls_payload_nibble; selects on `mpls_payload_nibble.v`:
  - mpls_payload_nibble.v == 0x0 → `parse_eompls`
  - mpls_payload_nibble.v == 0x4 → `parse_ipv4_rest`
  - mpls_payload_nibble.v == 0x6 → `parse_ipv6_rest`
  - otherwise → **accept**
  > Bottom of stack: the source's 4-bit pseudo-field, extracted
  > here as a real header (see the module docstring).
- **`parse_eompls`** — extracts eompls_rest; then `parse_ethernet2`
- **`parse_ipv4`** — extracts ipv4; selects on `ipv4.frag_offset`, `ipv4.protocol`:
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x1 → `parse_icmp`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x6 → `parse_tcp`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x11 → `parse_udp`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x2f → `parse_gre1`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x32 → `parse_ipsec_esp`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x33 → `parse_ipsec_ah`
  - ipv4.frag_offset == 0x0 && ipv4.protocol == 0x84 → `parse_sctp`
  - otherwise → **accept**
- **`parse_ipv4_rest`** — extracts ipv4_rest; selects on `ipv4_rest.frag_offset`, `ipv4_rest.protocol`:
  - ipv4_rest.frag_offset == 0x0 && ipv4_rest.protocol == 0x1 → `parse_icmp`
  - ipv4_rest.frag_offset == 0x0 && ipv4_rest.protocol == 0x6 → `parse_tcp`
  - ipv4_rest.frag_offset == 0x0 && ipv4_rest.protocol == 0x11 → `parse_udp`
  - ipv4_rest.frag_offset == 0x0 && ipv4_rest.protocol == 0x2f → `parse_gre1`
  - ipv4_rest.frag_offset == 0x0 && ipv4_rest.protocol == 0x32 → `parse_ipsec_esp`
  - ipv4_rest.frag_offset == 0x0 && ipv4_rest.protocol == 0x33 → `parse_ipsec_ah`
  - ipv4_rest.frag_offset == 0x0 && ipv4_rest.protocol == 0x84 → `parse_sctp`
  - otherwise → **accept**
- **`parse_ipv6`** — extracts ipv6; selects on `ipv6.next_hdr`:
  - ipv6.next_hdr == 0x3a → `parse_icmpv6`
  - ipv6.next_hdr == 0x6 → `parse_tcp`
  - ipv6.next_hdr == 0x11 → `parse_udp`
  - ipv6.next_hdr == 0x2f → `parse_gre1`
  - ipv6.next_hdr == 0x32 → `parse_ipsec_esp`
  - ipv6.next_hdr == 0x33 → `parse_ipsec_ah`
  - ipv6.next_hdr == 0x84 → `parse_sctp`
  - otherwise → **accept**
- **`parse_ipv6_rest`** — extracts ipv6_rest; selects on `ipv6_rest.next_hdr`:
  - ipv6_rest.next_hdr == 0x3a → `parse_icmpv6`
  - ipv6_rest.next_hdr == 0x6 → `parse_tcp`
  - ipv6_rest.next_hdr == 0x11 → `parse_udp`
  - ipv6_rest.next_hdr == 0x2f → `parse_gre1`
  - ipv6_rest.next_hdr == 0x32 → `parse_ipsec_esp`
  - ipv6_rest.next_hdr == 0x33 → `parse_ipsec_ah`
  - ipv6_rest.next_hdr == 0x84 → `parse_sctp`
  - otherwise → **accept**
- **`parse_icmp`** — extracts icmp; then **accept**
- **`parse_icmpv6`** — extracts icmpv6; then **accept**
- **`parse_tcp`** — extracts tcp; then **accept**
- **`parse_udp`** — extracts udp; selects on `udp.dst_port`:
  - udp.dst_port == 0xffff → `parse_vxlan`
  - otherwise → **accept**
  > UDP dispatches VXLAN on dstPort 65535 - the source's
  > made-up value (it predates the 4789 assignment) [sic].
- **`parse_vxlan`** — extracts vxlan; then `parse_ethernet2`
- **`parse_sctp`** — extracts sctp; then **accept**
- **`parse_gre1`** — extracts gre; selects on `gre.k`, `gre.proto`:
  - gre.k == 0x1 && gre.proto == 0x6558 → `parse_nv_gre_inner`
  - gre.k == 0x1 && gre.proto == 0x6559 → `parse_gre2`
  - otherwise → **accept**
  > First of the three bounded GREs (`max = 3`); dispatch keys
  > on (K, proto) concatenated, so both arms require K=1.
- **`parse_gre2`** — extracts gre; selects on `gre.k`, `gre.proto`:
  - gre.k == 0x1 && gre.proto == 0x6558 → `parse_nv_gre_inner`
  - gre.k == 0x1 && gre.proto == 0x6559 → `parse_gre3`
  - otherwise → **accept**
- **`parse_gre3`** — extracts gre; selects on `gre.k`, `gre.proto`:
  - gre.k == 0x1 && gre.proto == 0x6558 → `parse_nv_gre_inner`
  - otherwise → **accept**
  > Third GRE: the bound is exhausted, so the nested-GRE arm
  > falls to the default.
- **`parse_nv_gre_inner`** — extracts nv_gre_inner; then `parse_ethernet2`
- **`parse_ipsec_esp`** — extracts ipsec_esp; then **accept**
- **`parse_ipsec_ah`** — extracts ipsec_ah; selects on `ipsec_ah.next_hdr`:
  - ipsec_ah.next_hdr == 0x1 → `parse_icmp`
  - ipsec_ah.next_hdr == 0x6 → `parse_tcp`
  - ipsec_ah.next_hdr == 0x11 → `parse_udp`
  - ipsec_ah.next_hdr == 0x2f → `parse_gre1`
  - ipsec_ah.next_hdr == 0x3a → `parse_icmpv6`
  - ipsec_ah.next_hdr == 0x84 → `parse_sctp`
  - otherwise → **accept**
  > The source's AH map, transcribed as-is - it includes
  > ICMPv6 even when AH was reached off the IPv4 path.
- **`parse_arp_rarp`** — extracts arp_rarp; selects on `arp_rarp.proto_type`:
  - arp_rarp.proto_type == 0x800 → `parse_arp_rarp_ipv4`
  - otherwise → **accept**
- **`parse_arp_rarp_ipv4`** — extracts arp_rarp_ipv4; then **accept**
- **`parse_ethernet2`** — extracts ethernet2; then **accept**
  > The inner Ethernet every tunnel bottoms out in - terminal
  > in the source (its `ethernet2` node has no dispatch map).
