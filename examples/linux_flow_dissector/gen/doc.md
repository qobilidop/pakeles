# `linux_flow_dissector`

Generated by `pakeles doc` from IR version 0.1.0. Parse depth bound: 10.

## Header `ethernet`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `dst` | 48 | MAC | Destination |  |  |
| `src` | 48 | MAC | Source |  |  |
| `ethertype` | 16 | hex | Type | 2048=IPv4, 2054=ARP, 33024=802.1Q VLAN, 34525=IPv6, 34984=802.1AD (QinQ), 34887=MPLS unicast, 34888=MPLS multicast |  |

## Header `vlan`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 | dec | Priority |  |  |
| `dei` | 1 | dec | DEI |  |  |
| `vid` | 12 | dec | VLAN ID |  |  |
| `encapsulated_proto` | 16 | hex | Type | 2048=IPv4, 34525=IPv6, 34887=MPLS unicast, 34888=MPLS multicast |  |

## Header `ipv4`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 | dec | Version |  |  |
| `ihl` | 4 | dec | Header Length |  | in 32-bit words |
| `dscp` | 6 | dec | DSCP |  |  |
| `ecn` | 2 | dec | ECN |  |  |
| `total_len` | 16 | dec | Total Length |  |  |
| `id` | 16 | hex | Identification |  |  |
| `flags` | 3 | hex | Flags |  |  |
| `frag_offset` | 13 | dec | Fragment Offset |  |  |
| `ttl` | 8 | dec | Time to Live |  |  |
| `protocol` | 8 | dec | Protocol | 1=ICMP, 6=TCP, 17=UDP |  |
| `checksum` | 16 | hex | Header Checksum |  |  |
| `src` | 32 | IPv4 | Source Address |  |  |
| `dst` | 32 | IPv4 | Destination Address |  |  |
| `options` | `((ipv4.ihl * 4) - 20)` bytes |  |  |  |  |

## Header `ipv6`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 | dec | Version |  |  |
| `traffic_class` | 8 | hex | Traffic Class |  |  |
| `flow_label` | 20 | hex | Flow Label |  |  |
| `payload_length` | 16 | dec | Payload Length |  |  |
| `next_header` | 8 | dec | Next Header | 1=ICMP, 6=TCP, 17=UDP |  |
| `hop_limit` | 8 | dec | Hop Limit |  |  |
| `src` | `16` bytes |  |  |  |  |
| `dst` | `16` bytes |  |  |  |  |

## Header `ipv6_ext_opt`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `next_header` | 8 | dec | Next Header |  |  |
| `hdr_ext_len` | 8 | dec | Hdr Ext Len |  | in 8-octet units, excl. first 8 |
| `body` | `(((1 + ext_opt.hdr_ext_len) << 3) - 2)` bytes |  |  |  |  |

## Header `ipv6_frag`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `next_header` | 8 | dec | Next Header |  |  |
| `reserved` | 8 | hex | Reserved |  |  |
| `frag_off` | 13 | dec | Fragment Offset |  | in 8-octet units |
| `res2` | 2 | hex | Res |  |  |
| `m_flag` | 1 | dec | More Fragments |  |  |
| `identification` | 32 | hex | Identification |  |  |

## Header `mpls`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `label` | 20 | dec | Label |  |  |
| `tc` | 3 | dec | Traffic Class |  |  |
| `s` | 1 | dec | Bottom of Stack |  |  |
| `ttl` | 8 | dec | TTL |  |  |

## Header `tcp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `sport` | 16 | dec | Source Port |  |  |
| `dport` | 16 | dec | Destination Port |  |  |
| `seq` | 32 | dec | Sequence Number |  |  |
| `ack` | 32 | dec | Acknowledgment Number |  |  |
| `data_offset` | 4 | dec | Data Offset |  | in 32-bit words |
| `reserved` | 4 | hex | Reserved |  |  |
| `flags` | 8 | hex | Flags |  |  |
| `window` | 16 | dec | Window |  |  |
| `checksum` | 16 | hex | Checksum |  |  |
| `urgent` | 16 | dec | Urgent Pointer |  |  |
| `options` | `((tcp.data_offset * 4) - 20)` bytes |  |  |  |  |

## Header `udp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `sport` | 16 | dec | Source Port |  |  |
| `dport` | 16 | dec | Destination Port |  |  |
| `length` | 16 | dec | Length |  |  |
| `checksum` | 16 | hex | Checksum |  |  |

## Parse graph

Start state: `parse_ethernet`.

- **`parse_ethernet`** — extracts ethernet; selects on `ethernet.ethertype`:
  - ethernet.ethertype == 0x800 → `parse_ipv4`
  - ethernet.ethertype == 0x86dd → `parse_ipv6`
  - ethernet.ethertype == 0x8100 → `parse_vlan_q`
  - ethernet.ethertype == 0x88a8 → `parse_vlan_ad`
  - ethernet.ethertype == 0x8847 → `parse_mpls`
  - ethernet.ethertype == 0x8848 → `parse_mpls`
  - otherwise → payload boundary (*unsupported ethertype*)
- **`parse_vlan_ad`** — extracts vlan_ad; selects on `vlan_ad.encapsulated_proto`:
  - vlan_ad.encapsulated_proto == 0x8100 → `parse_vlan_q`
  - otherwise → **reject** (*802.1AD must be followed by 802.1Q*)
- **`parse_vlan_q`** — extracts vlan_q; selects on `vlan_q.encapsulated_proto`:
  - vlan_q.encapsulated_proto == 0x800 → `parse_ipv4`
  - vlan_q.encapsulated_proto == 0x86dd → `parse_ipv6`
  - vlan_q.encapsulated_proto == 0x8847 → `parse_mpls`
  - vlan_q.encapsulated_proto == 0x8848 → `parse_mpls`
  - vlan_q.encapsulated_proto == 0x8100 → **reject** (*vlan stacking beyond kernel depth*)
  - vlan_q.encapsulated_proto == 0x88a8 → **reject** (*vlan stacking beyond kernel depth*)
  - otherwise → payload boundary (*unsupported ethertype*)
- **`parse_ipv4`** — extracts ipv4; selects on `ipv4.protocol`:
  - ipv4.protocol == 0x6 → `parse_tcp`
  - ipv4.protocol == 0x11 → `parse_udp`
  - otherwise → payload boundary (*unsupported ip protocol*)
- **`parse_ipv6`** — extracts ipv6; selects on `ipv6.next_header`:
  - ipv6.next_header == 0x0 → `parse_ipv6_opt`
  - ipv6.next_header == 0x3c → `parse_ipv6_opt`
  - ipv6.next_header == 0x2c → `parse_ipv6_frag`
  - ipv6.next_header == 0x6 → `parse_tcp`
  - ipv6.next_header == 0x11 → `parse_udp`
  - otherwise → payload boundary (*unsupported ip protocol*)
- **`parse_ipv6_opt`** — extracts ext_opt; selects on `ext_opt.next_header`:
  - ext_opt.next_header == 0x0 → `parse_ipv6_opt`
  - ext_opt.next_header == 0x3c → `parse_ipv6_opt`
  - ext_opt.next_header == 0x2c → `parse_ipv6_frag`
  - ext_opt.next_header == 0x6 → `parse_tcp`
  - ext_opt.next_header == 0x11 → `parse_udp`
  - otherwise → payload boundary (*unsupported ip protocol*)
- **`parse_ipv6_frag`** — extracts ext_frag; then **accept**
- **`parse_mpls`** — extracts mpls; then **accept**
- **`parse_tcp`** — extracts tcp; then **accept**
- **`parse_udp`** — extracts udp; then **accept**
