/* Generated by pakeles from `dpdk_ptype`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

static uint64_t pk_read_bits(const uint8_t *buf, uint64_t off, uint32_t n) {
  uint64_t v = 0;
  uint32_t i;
  for (i = 0; i < n; i++) {
    uint64_t pos = off + i;
    v = (v << 1) | (uint64_t)((buf[pos >> 3] >> (7 - (pos & 7))) & 1);
  }
  return v;
}

#define PK_S_PARSE_ETHERNET 0
#define PK_S_PARSE_VLAN 1
#define PK_S_PARSE_QINQ 2
#define PK_S_PARSE_IPV4 3
#define PK_S_PARSE_IPV6 4
#define PK_S_PARSE_EXT_OPT1 5
#define PK_S_PARSE_EXT_OPT2 6
#define PK_S_PARSE_EXT_OPT3 7
#define PK_S_PARSE_EXT_OPT4 8
#define PK_S_PARSE_EXT_OPT5 9
#define PK_S_PARSE_EXT_FRAG 10
#define PK_S_PARSE_GRE 11
#define PK_S_PARSE_GRE_OPT 12
#define PK_S_PARSE_TCP 13
#define PK_S_PARSE_INNER_ETHERNET 14
#define PK_S_PARSE_INNER_VLAN 15
#define PK_S_PARSE_INNER_QINQ 16
#define PK_S_PARSE_INNER_IPV4 17
#define PK_S_PARSE_INNER_IPV6 18
#define PK_S_PARSE_INNER_EXT_OPT1 19
#define PK_S_PARSE_INNER_EXT_OPT2 20
#define PK_S_PARSE_INNER_EXT_OPT3 21
#define PK_S_PARSE_INNER_EXT_OPT4 22
#define PK_S_PARSE_INNER_EXT_OPT5 23
#define PK_S_PARSE_INNER_EXT_FRAG 24
#define PK_S_PARSE_INNER_TCP 25

static int pk_dpdk_ptype_parse_core(const uint8_t *buf, uint64_t bit_len, pk_dpdk_ptype_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_ETHERNET;
  uint32_t depth;
  for (depth = 0; depth < 20u; depth++) {
    switch (state) {
    case PK_S_PARSE_ETHERNET: {
      out->ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.dst = (uint64_t)pk_read_bits(buf, off, 48);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.src = (uint64_t)pk_read_bits(buf, off, 48);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.ethertype = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->ethernet.ethertype;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN;
        continue;
      } else if (key0 == 34984ULL) {
        state = PK_S_PARSE_QINQ;
        continue;
      } else if (key0 == 34887ULL) {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      } else if (key0 == 34888ULL) {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      } else if (key0 == 25944ULL) {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      } else if (key0 == 1024ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 10496ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 12032ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VLAN: {
      out->vlan_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.pcp = (uint8_t)pk_read_bits(buf, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.dei = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.vid = (uint16_t)pk_read_bits(buf, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.proto = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan.proto;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 25944ULL) {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else if (key0 == 34984ULL) {
        state = PK_S_PARSE_INNER_QINQ;
        continue;
      } else if (key0 == 1024ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 10496ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 12032ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_QINQ: {
      out->qinq_present = 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->qinq.first_tag = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->qinq.tci = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->qinq.proto = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->qinq.proto;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 25944ULL) {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else if (key0 == 34984ULL) {
        state = PK_S_PARSE_INNER_QINQ;
        continue;
      } else if (key0 == 1024ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 10496ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 12032ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IPV4: {
      out->ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.version = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ihl = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dscp = (uint8_t)pk_read_bits(buf, off, 6);
      off += 6;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ecn = (uint8_t)pk_read_bits(buf, off, 2);
      off += 2;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.total_len = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.id = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.flags_res_df = (uint8_t)pk_read_bits(buf, off, 2);
      off += 2;
      if (off + 14 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.mf_frag_off = (uint16_t)pk_read_bits(buf, off, 14);
      off += 14;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ttl = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.protocol = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.checksum = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.src = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dst = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      {
        uint64_t vlen = (((uint64_t)out->ipv4.ihl * 4ULL) - 20ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv4.options_bit_off = off;
        out->ipv4.options_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv4.mf_frag_off;
      uint64_t key1 = (uint64_t)out->ipv4.protocol;
      if (key0 == 0ULL && key1 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 0ULL && key1 == 4ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 0ULL && key1 == 41ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 0ULL && key1 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 0ULL && key1 == 8ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 0ULL && key1 == 129ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IPV6: {
      out->ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.version = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.traffic_class = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.flow_label = (uint32_t)pk_read_bits(buf, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.payload_length = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.hop_limit = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.src_bit_off = off;
        out->ipv6.src_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.dst_bit_off = off;
        out->ipv6.dst_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_EXT_OPT1;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_EXT_OPT1;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_EXT_OPT1;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 41ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 8ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 129ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_EXT_OPT1: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_EXT_OPT2;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_EXT_OPT2;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_EXT_OPT2;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 41ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 8ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 129ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_EXT_OPT2: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_EXT_OPT3;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_EXT_OPT3;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_EXT_OPT3;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 41ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 8ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 129ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_EXT_OPT3: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_EXT_OPT4;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_EXT_OPT4;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_EXT_OPT4;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 41ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 8ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 129ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_EXT_OPT4: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_EXT_OPT5;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_EXT_OPT5;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_EXT_OPT5;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 41ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 8ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 129ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_EXT_OPT5: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_EXT_FRAG: {
      out->ipv6_frag_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.reserved = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.frag_off = (uint16_t)pk_read_bits(buf, off, 13);
      off += 13;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.res2 = (uint8_t)pk_read_bits(buf, off, 2);
      off += 2;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.m_flag = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.identification = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_GRE: {
      out->gre_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.c = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.r = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.k = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.s = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 9 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.reserved = (uint16_t)pk_read_bits(buf, off, 9);
      off += 9;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.version = (uint8_t)pk_read_bits(buf, off, 3);
      off += 3;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.proto = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->gre.r;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_GRE_OPT;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_GRE_OPT: {
      out->gre_opt_present = 1;
      {
        uint64_t vlen = ((((uint64_t)out->gre.c * 4ULL) + ((uint64_t)out->gre.k * 4ULL)) + ((uint64_t)out->gre.s * 4ULL));
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->gre_opt.body_bit_off = off;
        out->gre_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->gre.proto;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 25944ULL) {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else if (key0 == 34984ULL) {
        state = PK_S_PARSE_INNER_QINQ;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_TCP: {
      out->tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.sport = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.dport = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.seq = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ack = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.data_offset = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.reserved = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.flags = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.window = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.checksum = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.urgent = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_INNER_ETHERNET: {
      out->ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.dst = (uint64_t)pk_read_bits(buf, off, 48);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.src = (uint64_t)pk_read_bits(buf, off, 48);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.ethertype = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->ethernet.ethertype;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_INNER_VLAN;
        continue;
      } else if (key0 == 34984ULL) {
        state = PK_S_PARSE_INNER_QINQ;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_VLAN: {
      out->vlan_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.pcp = (uint8_t)pk_read_bits(buf, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.dei = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.vid = (uint16_t)pk_read_bits(buf, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.proto = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan.proto;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_QINQ: {
      out->qinq_present = 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->qinq.first_tag = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->qinq.tci = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->qinq.proto = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->qinq.proto;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_IPV4: {
      out->ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.version = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ihl = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dscp = (uint8_t)pk_read_bits(buf, off, 6);
      off += 6;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ecn = (uint8_t)pk_read_bits(buf, off, 2);
      off += 2;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.total_len = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.id = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.flags_res_df = (uint8_t)pk_read_bits(buf, off, 2);
      off += 2;
      if (off + 14 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.mf_frag_off = (uint16_t)pk_read_bits(buf, off, 14);
      off += 14;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ttl = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.protocol = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.checksum = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.src = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dst = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      {
        uint64_t vlen = (((uint64_t)out->ipv4.ihl * 4ULL) - 20ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv4.options_bit_off = off;
        out->ipv4.options_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv4.mf_frag_off;
      uint64_t key1 = (uint64_t)out->ipv4.protocol;
      if (key0 == 0ULL && key1 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_IPV6: {
      out->ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.version = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.traffic_class = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.flow_label = (uint32_t)pk_read_bits(buf, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.payload_length = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.hop_limit = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.src_bit_off = off;
        out->ipv6.src_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.dst_bit_off = off;
        out->ipv6.dst_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT1;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT1;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT1;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_INNER_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_EXT_OPT1: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT2;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT2;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT2;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_INNER_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_EXT_OPT2: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT3;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT3;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT3;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_INNER_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_EXT_OPT3: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT4;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT4;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT4;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_INNER_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_EXT_OPT4: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6_ext_opt.next_header;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT5;
        continue;
      } else if (key0 == 43ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT5;
        continue;
      } else if (key0 == 60ULL) {
        state = PK_S_PARSE_INNER_EXT_OPT5;
        continue;
      } else if (key0 == 44ULL) {
        state = PK_S_PARSE_INNER_EXT_FRAG;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_EXT_OPT5: {
      out->ipv6_ext_opt_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_ext_opt.hdr_ext_len = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      {
        uint64_t vlen = (((1ULL + (uint64_t)out->ipv6_ext_opt.hdr_ext_len) << 3ULL) - 2ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6_ext_opt.body_bit_off = off;
        out->ipv6_ext_opt.body_bit_len = vlen * 8;
        off += vlen * 8;
      }
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_INNER_EXT_FRAG: {
      out->ipv6_frag_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.next_header = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.reserved = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.frag_off = (uint16_t)pk_read_bits(buf, off, 13);
      off += 13;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.res2 = (uint8_t)pk_read_bits(buf, off, 2);
      off += 2;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.m_flag = (uint8_t)pk_read_bits(buf, off, 1);
      off += 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6_frag.identification = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_INNER_TCP: {
      out->tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.sport = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.dport = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.seq = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ack = (uint32_t)pk_read_bits(buf, off, 32);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.data_offset = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.reserved = (uint8_t)pk_read_bits(buf, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.flags = (uint8_t)pk_read_bits(buf, off, 8);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.window = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.checksum = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.urgent = (uint16_t)pk_read_bits(buf, off, 16);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_dpdk_ptype_parse(const uint8_t *buf, uint64_t bit_len, pk_dpdk_ptype_result_t *out) {
  pk_dpdk_ptype_result_t zero = {0};
  *out = zero;
  return pk_dpdk_ptype_parse_core(buf, bit_len, out);
}

const char *pk_dpdk_ptype_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  default: return "";
  }
}
