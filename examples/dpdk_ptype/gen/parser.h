/* Generated by pakeles from `dpdk_ptype`. Do not edit:
 * regenerate with `pakeles gen c`. */
#ifndef PK_DPDK_PTYPE_H
#define PK_DPDK_PTYPE_H

#include <stdint.h>
#include <stddef.h>

enum { PK_DPDK_PTYPE_ACCEPT = 0, PK_DPDK_PTYPE_REJECT = 1 };

typedef enum {
  PK_R_NONE = 0,
  PK_R_OUT_OF_BOUNDS = 1, /* "out of bounds" */
  PK_R_MAX_DEPTH_EXCEEDED = 2, /* "max depth exceeded" */
  PK_R_NO_MATCHING_SELECT_ARM = 3, /* "no matching select arm" */
} pk_dpdk_ptype_reason_t;

typedef struct {
  uint64_t dst;
  uint64_t src;
  uint16_t ethertype;
} pk_dpdk_ptype_ethernet_t;

typedef struct {
  uint8_t pcp;
  uint8_t dei;
  uint16_t vid;
  uint16_t proto;
} pk_dpdk_ptype_vlan_t;

typedef struct {
  uint32_t first_tag;
  uint16_t tci;
  uint16_t proto;
} pk_dpdk_ptype_qinq_t;

typedef struct {
  uint8_t version;
  uint8_t ihl;
  uint8_t dscp;
  uint8_t ecn;
  uint16_t total_len;
  uint16_t id;
  uint8_t flags_res_df;
  uint16_t mf_frag_off;
  uint8_t ttl;
  uint8_t protocol;
  uint16_t checksum;
  uint32_t src;
  uint32_t dst;
  uint64_t options_bit_off;
  uint64_t options_bit_len;
} pk_dpdk_ptype_ipv4_t;

typedef struct {
  uint8_t version;
  uint8_t traffic_class;
  uint32_t flow_label;
  uint16_t payload_length;
  uint8_t next_header;
  uint8_t hop_limit;
  uint64_t src_bit_off;
  uint64_t src_bit_len;
  uint64_t dst_bit_off;
  uint64_t dst_bit_len;
} pk_dpdk_ptype_ipv6_t;

typedef struct {
  uint8_t next_header;
  uint8_t hdr_ext_len;
  uint64_t body_bit_off;
  uint64_t body_bit_len;
} pk_dpdk_ptype_ipv6_ext_opt_t;

typedef struct {
  uint8_t next_header;
  uint8_t reserved;
  uint16_t frag_off;
  uint8_t res2;
  uint8_t m_flag;
  uint32_t identification;
} pk_dpdk_ptype_ipv6_frag_t;

typedef struct {
  uint8_t c;
  uint8_t r;
  uint8_t k;
  uint8_t s;
  uint16_t reserved;
  uint8_t version;
  uint16_t proto;
} pk_dpdk_ptype_gre_t;

typedef struct {
  uint64_t body_bit_off;
  uint64_t body_bit_len;
} pk_dpdk_ptype_gre_opt_t;

typedef struct {
  uint16_t sport;
  uint16_t dport;
  uint32_t seq;
  uint32_t ack;
  uint8_t data_offset;
  uint8_t reserved;
  uint8_t flags;
  uint16_t window;
  uint16_t checksum;
  uint16_t urgent;
} pk_dpdk_ptype_tcp_t;

typedef struct {
  uint8_t outcome; /* 0 = accept, 1 = reject */
  uint16_t reason; /* pk_dpdk_ptype_reason */
  uint64_t consumed_bits;
  uint8_t ethernet_present;
  pk_dpdk_ptype_ethernet_t ethernet;
  uint8_t vlan_present;
  pk_dpdk_ptype_vlan_t vlan;
  uint8_t qinq_present;
  pk_dpdk_ptype_qinq_t qinq;
  uint8_t ipv4_present;
  pk_dpdk_ptype_ipv4_t ipv4;
  uint8_t ipv6_present;
  pk_dpdk_ptype_ipv6_t ipv6;
  uint8_t ipv6_ext_opt_present;
  pk_dpdk_ptype_ipv6_ext_opt_t ipv6_ext_opt;
  uint8_t ipv6_frag_present;
  pk_dpdk_ptype_ipv6_frag_t ipv6_frag;
  uint8_t gre_present;
  pk_dpdk_ptype_gre_t gre;
  uint8_t gre_opt_present;
  pk_dpdk_ptype_gre_opt_t gre_opt;
  uint8_t tcp_present;
  pk_dpdk_ptype_tcp_t tcp;
} pk_dpdk_ptype_result_t;

/* Parse `bit_len` bits of `buf` (reject mode). Returns outcome. */
int pk_dpdk_ptype_parse(const uint8_t *buf, uint64_t bit_len, pk_dpdk_ptype_result_t *out);
const char *pk_dpdk_ptype_reason_str(uint16_t reason);

#endif /* PK_DPDK_PTYPE_H */
