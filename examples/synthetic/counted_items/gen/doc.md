# `counted_items`

Generated by `pakeles doc` from IR version 0.1.0. Parse depth bound: 8.

## Header `count`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `n` | 8 | dec | Item Count |  |  |

## Header `item`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `v` | 8 | hex | Value |  |  |

## Metadata

| Field | Bits | Init | Format | Name | Notes |
|---|---|---|---|---|---|
| `done` | 1 | 0 | dec | Done | set on the pass-through completion state |
| `remaining` | 8 | 0 | dec | Remaining | items left to read |

## Parse graph

Start state: `parse_count`.

- **`parse_count`** — extracts count; sets meta.remaining = count.n; selects on `meta.remaining`:
  - meta.remaining == 0x0 → `mark_done`
  - otherwise → `parse_item`
- **`parse_item`** — extracts item; sets meta.remaining = (meta.remaining - 1); selects on `meta.remaining`:
  - meta.remaining == 0x0 → `mark_done`
  - otherwise → `parse_item`
- **`mark_done`**; sets meta.done = 1; then **accept**
