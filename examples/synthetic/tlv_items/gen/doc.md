# `tlv_items`

Generated by `pakeles doc` from IR version 0.1.0. Parse depth bound: 8.

## Header `total_len`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `total` | 8 | dec | Total Length |  | bytes of TLV items that follow |

## Header `item`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `kind` | 8 | hex | Kind |  |  |
| `ln` | 8 | dec | Length |  |  |
| `val` | `item.ln` bytes |  |  |  |  |

## Parse graph

Start state: `parse_total`.

- **`parse_total`** — extracts total_len; then `tlv_loop`
- **`tlv_loop`**; selects on `remaining()`:
  - remaining() == 0x0 → `close`
  - otherwise → `parse_item`
- **`parse_item`** — extracts item; then `tlv_loop`
- **`close`**; then **accept**
