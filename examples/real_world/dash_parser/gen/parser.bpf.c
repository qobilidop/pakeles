/* Generated by pakeles from `dash_parser` (eBPF variant). Do not edit:
 * regenerate with `pakeles gen bpf`.
 * Compile: clang -O2 -target bpf -c this_file.c
 * Entry contract (rbpf raw VM): r1 = mem = 8-byte LE bit_len ++ packet;
 * the length prefix is the harness framing (rbpf passes no length).
 * Returns outcome(8b) << 56 | reason(8b) << 48 | consumed_bits(48b).
 * Note: the result struct lives on the (512-byte) BPF stack;
 * large parsers will need a redesign. */

/* Freestanding: no libc for the bpf target. */
typedef __UINT8_TYPE__ uint8_t;
typedef __UINT16_TYPE__ uint16_t;
typedef __UINT32_TYPE__ uint32_t;
typedef __UINT64_TYPE__ uint64_t;

typedef struct {
  uint64_t dst_addr;
  uint64_t src_addr;
  uint16_t ether_type;
} pk_dash_parser_u0_ethernet_t;

typedef struct {
  uint8_t packet_source;
  uint8_t packet_type;
  uint8_t packet_subtype;
  uint16_t length;
} pk_dash_parser_packet_meta_t;

typedef struct {
  uint64_t eni_mac;
  uint16_t vnet_id;
  uint64_t src_ip_bit_off;
  uint64_t src_ip_bit_len;
  uint64_t dst_ip_bit_off;
  uint64_t dst_ip_bit_len;
  uint16_t src_port;
  uint16_t dst_port;
  uint8_t ip_proto;
  uint8_t reserved;
  uint8_t is_ip_v6;
} pk_dash_parser_flow_key_t;

typedef struct {
  uint8_t reserved;
  uint8_t is_unidirectional;
  uint16_t direction;
  uint32_t version;
  uint32_t actions;
  uint32_t meter_class;
  uint32_t idle_timeout_in_ms;
} pk_dash_parser_flow_data_t;

typedef struct {
  uint64_t dmac;
  uint64_t sip_bit_off;
  uint64_t sip_bit_len;
  uint64_t dip_bit_off;
  uint64_t dip_bit_len;
  uint64_t sip_mask_bit_off;
  uint64_t sip_mask_bit_len;
  uint64_t dip_mask_bit_off;
  uint64_t dip_mask_bit_len;
  uint16_t sport;
  uint16_t dport;
  uint8_t reserved;
  uint8_t is_ipv6;
} pk_dash_parser_flow_overlay_data_t;

typedef struct {
  uint32_t vni;
  uint8_t reserved;
  uint32_t underlay_sip;
  uint32_t underlay_dip;
  uint64_t underlay_smac;
  uint64_t underlay_dmac;
  uint16_t dash_encapsulation;
} pk_dash_parser_flow_u0_encap_data_t;

typedef struct {
  uint32_t vni;
  uint8_t reserved;
  uint32_t underlay_sip;
  uint32_t underlay_dip;
  uint64_t underlay_smac;
  uint64_t underlay_dmac;
  uint16_t dash_encapsulation;
} pk_dash_parser_flow_u1_encap_data_t;

typedef struct {
  uint8_t version;
  uint8_t ihl;
  uint8_t diffserv;
  uint16_t total_len;
  uint16_t identification;
  uint8_t flags;
  uint16_t frag_offset;
  uint8_t ttl;
  uint8_t protocol;
  uint16_t hdr_checksum;
  uint32_t src_addr;
  uint32_t dst_addr;
} pk_dash_parser_u0_ipv4_t;

typedef struct {
  uint64_t options_bit_off;
  uint64_t options_bit_len;
} pk_dash_parser_u0_ipv4options_t;

typedef struct {
  uint8_t version;
  uint8_t traffic_class;
  uint32_t flow_label;
  uint16_t payload_length;
  uint8_t next_header;
  uint8_t hop_limit;
  uint64_t src_addr_hi;
  uint64_t src_addr_lo;
  uint64_t dst_addr_hi;
  uint64_t dst_addr_lo;
} pk_dash_parser_u0_ipv6_t;

typedef struct {
  uint16_t src_port;
  uint16_t dst_port;
  uint16_t length;
  uint16_t checksum;
} pk_dash_parser_u0_udp_t;

typedef struct {
  uint16_t src_port;
  uint16_t dst_port;
  uint32_t seq_no;
  uint32_t ack_no;
  uint8_t data_offset;
  uint8_t res;
  uint8_t ecn;
  uint8_t flags;
  uint16_t window;
  uint16_t checksum;
  uint16_t urgent_ptr;
} pk_dash_parser_u0_tcp_t;

typedef struct {
  uint8_t flags;
  uint32_t reserved;
  uint32_t vni;
  uint8_t reserved_2;
} pk_dash_parser_u0_vxlan_t;

typedef struct {
  uint64_t dst_addr;
  uint64_t src_addr;
  uint16_t ether_type;
} pk_dash_parser_customer_ethernet_t;

typedef struct {
  uint8_t version;
  uint8_t ihl;
  uint8_t diffserv;
  uint16_t total_len;
  uint16_t identification;
  uint8_t flags;
  uint16_t frag_offset;
  uint8_t ttl;
  uint8_t protocol;
  uint16_t hdr_checksum;
  uint32_t src_addr;
  uint32_t dst_addr;
} pk_dash_parser_customer_ipv4_t;

typedef struct {
  uint8_t version;
  uint8_t traffic_class;
  uint32_t flow_label;
  uint16_t payload_length;
  uint8_t next_header;
  uint8_t hop_limit;
  uint64_t src_addr_hi;
  uint64_t src_addr_lo;
  uint64_t dst_addr_hi;
  uint64_t dst_addr_lo;
} pk_dash_parser_customer_ipv6_t;

typedef struct {
  uint16_t src_port;
  uint16_t dst_port;
  uint16_t length;
  uint16_t checksum;
} pk_dash_parser_customer_udp_t;

typedef struct {
  uint16_t src_port;
  uint16_t dst_port;
  uint32_t seq_no;
  uint32_t ack_no;
  uint8_t data_offset;
  uint8_t res;
  uint8_t ecn;
  uint8_t flags;
  uint16_t window;
  uint16_t checksum;
  uint16_t urgent_ptr;
} pk_dash_parser_customer_tcp_t;

typedef struct {
  uint8_t outcome; /* 0 = accept, 1 = reject */
  uint16_t reason; /* pk_dash_parser_reason */
  uint64_t consumed_bits;
  uint8_t u0_ethernet_present;
  pk_dash_parser_u0_ethernet_t u0_ethernet;
  uint8_t packet_meta_present;
  pk_dash_parser_packet_meta_t packet_meta;
  uint8_t flow_key_present;
  pk_dash_parser_flow_key_t flow_key;
  uint8_t flow_data_present;
  pk_dash_parser_flow_data_t flow_data;
  uint8_t flow_overlay_data_present;
  pk_dash_parser_flow_overlay_data_t flow_overlay_data;
  uint8_t flow_u0_encap_data_present;
  pk_dash_parser_flow_u0_encap_data_t flow_u0_encap_data;
  uint8_t flow_u1_encap_data_present;
  pk_dash_parser_flow_u1_encap_data_t flow_u1_encap_data;
  uint8_t u0_ipv4_present;
  pk_dash_parser_u0_ipv4_t u0_ipv4;
  uint8_t u0_ipv4options_present;
  pk_dash_parser_u0_ipv4options_t u0_ipv4options;
  uint8_t u0_ipv6_present;
  pk_dash_parser_u0_ipv6_t u0_ipv6;
  uint8_t u0_udp_present;
  pk_dash_parser_u0_udp_t u0_udp;
  uint8_t u0_tcp_present;
  pk_dash_parser_u0_tcp_t u0_tcp;
  uint8_t u0_vxlan_present;
  pk_dash_parser_u0_vxlan_t u0_vxlan;
  uint8_t customer_ethernet_present;
  pk_dash_parser_customer_ethernet_t customer_ethernet;
  uint8_t customer_ipv4_present;
  pk_dash_parser_customer_ipv4_t customer_ipv4;
  uint8_t customer_ipv6_present;
  pk_dash_parser_customer_ipv6_t customer_ipv6;
  uint8_t customer_udp_present;
  pk_dash_parser_customer_udp_t customer_udp;
  uint8_t customer_tcp_present;
  pk_dash_parser_customer_tcp_t customer_tcp;
} pk_dash_parser_result_t;

typedef enum {
  PK_R_NONE = 0,
  PK_R_OUT_OF_BOUNDS = 1,
  PK_R_MAX_DEPTH_EXCEEDED = 2,
  PK_R_NO_MATCHING_SELECT_ARM = 3,
  PK_R_OUT_OF_REGION_BOUNDS = 4,
  PK_R_REGION_OUT_OF_BOUNDS = 5,
  PK_R_REGION_NOT_EXHAUSTED = 6,
  PK_R_IPV4INCORRECTVERSION = 16,
  PK_R_IPV4OPTIONSNOTSUPPORTED = 17,
  PK_R_INVALIDIPV4HEADER = 18,
} pk_dash_parser_reason_t;

/* Buffer contract: `buf` must hold at least PK_BUF_MASK + 1 bytes.
 * Every packet index is masked with it — dead at runtime (the
 * length guards already bound each access), but it bounds the index
 * register directly, which is what the kernel verifier tracks. */
#ifndef PK_BUF_MASK
#define PK_BUF_MASK 4095u
#endif

static __attribute__((always_inline)) uint64_t pk_read_bits(const uint8_t *buf, uint64_t avail, uint64_t off, uint32_t n) {
  (void)avail;
  uint64_t v = 0;
  uint32_t i;
  for (i = 0; i < n; i++) {
    uint64_t pos = off + i;
    v = (v << 1) | (uint64_t)((buf[(pos >> 3) & PK_BUF_MASK] >> (7 - (pos & 7))) & 1);
  }
  return v;
}

#define PK_S_PARSE_U0_ETHERNET 0
#define PK_S_PARSE_DASH_HDR 1
#define PK_S_PARSE_FLOW_KEY 2
#define PK_S_PARSE_FLOW_DATA 3
#define PK_S_PARSE_FLOW_OVERLAY_DATA 4
#define PK_S_PARSE_FLOW_ENCAP_U0 5
#define PK_S_PARSE_FLOW_ENCAP_U1 6
#define PK_S_PARSE_FLOW_ENCAP_U0_U1 7
#define PK_S_PARSE_U0_IPV4 8
#define PK_S_PARSE_U0_IPV4_IHL 9
#define PK_S_PARSE_U0_IPV4OPTIONS 10
#define PK_S_DISPATCH_ON_U0_PROTOCOL 11
#define PK_S_PARSE_U0_IPV6 12
#define PK_S_PARSE_U0_UDP 13
#define PK_S_PARSE_U0_TCP 14
#define PK_S_PARSE_U0_VXLAN 15
#define PK_S_PARSE_CUSTOMER_ETHERNET 16
#define PK_S_PARSE_CUSTOMER_IPV4 17
#define PK_S_PARSE_CUSTOMER_IPV4_IHL 18
#define PK_S_DISPATCH_ON_CUSTOMER_PROTOCOL 19
#define PK_S_PARSE_CUSTOMER_IPV6 20
#define PK_S_PARSE_CUSTOMER_UDP 21
#define PK_S_PARSE_CUSTOMER_TCP 22

static __attribute__((always_inline)) int pk_dash_parser_parse_core(const uint8_t *buf, uint64_t bit_len, pk_dash_parser_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_U0_ETHERNET;
  uint32_t depth;
  for (depth = 0; depth < 13u; depth++) {
    switch (state) {
    case PK_S_PARSE_U0_ETHERNET: {
      out->u0_ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ethernet.dst_addr = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ethernet.src_addr = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ethernet.ether_type = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      uint64_t key0 = (uint64_t)out->u0_ethernet.ether_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_U0_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_U0_IPV6;
        continue;
      } else if (key0 == 34669ULL) {
        state = PK_S_PARSE_DASH_HDR;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_DASH_HDR: {
      out->packet_meta_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->packet_meta.packet_source = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->packet_meta.packet_type = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->packet_meta.packet_subtype = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->packet_meta.length = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      uint64_t key0 = (uint64_t)out->packet_meta.packet_subtype;
      if (key0 == 1ULL) {
        state = PK_S_PARSE_FLOW_KEY;
        continue;
      } else if (key0 == 2ULL) {
        state = PK_S_PARSE_FLOW_KEY;
        continue;
      } else if (key0 == 3ULL) {
        state = PK_S_PARSE_FLOW_KEY;
        continue;
      } else {
        state = PK_S_PARSE_CUSTOMER_ETHERNET;
        continue;
      }
    }
    case PK_S_PARSE_FLOW_KEY: {
      out->flow_key_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_key.eni_mac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_key.vnet_id = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_key.src_ip_bit_off = off;
        out->flow_key.src_ip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_key.dst_ip_bit_off = off;
        out->flow_key.dst_ip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_key.src_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_key.dst_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_key.ip_proto = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 7 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_key.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 7);
      off += 7;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_key.is_ip_v6 = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      uint64_t key0 = (uint64_t)out->packet_meta.packet_subtype;
      if (key0 == 3ULL) {
        state = PK_S_PARSE_FLOW_DATA;
        continue;
      } else {
        state = PK_S_PARSE_CUSTOMER_ETHERNET;
        continue;
      }
    }
    case PK_S_PARSE_FLOW_DATA: {
      out->flow_data_present = 1;
      if (off + 7 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_data.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 7);
      off += 7;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_data.is_unidirectional = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_data.direction = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_data.version = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_data.actions = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_data.meter_class = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_data.idle_timeout_in_ms = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      uint64_t key0 = (uint64_t)out->flow_data.actions;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_CUSTOMER_ETHERNET;
        continue;
      } else if ((key0 & 3ULL) == 3ULL) {
        state = PK_S_PARSE_FLOW_ENCAP_U0_U1;
        continue;
      } else if ((key0 & 3ULL) == 1ULL) {
        state = PK_S_PARSE_FLOW_ENCAP_U0;
        continue;
      } else if ((key0 & 3ULL) == 2ULL) {
        state = PK_S_PARSE_FLOW_ENCAP_U1;
        continue;
      } else {
        state = PK_S_PARSE_FLOW_OVERLAY_DATA;
        continue;
      }
    }
    case PK_S_PARSE_FLOW_OVERLAY_DATA: {
      out->flow_overlay_data_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_bit_off = off;
        out->flow_overlay_data.sip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_bit_off = off;
        out->flow_overlay_data.dip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_mask_bit_off = off;
        out->flow_overlay_data.sip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_mask_bit_off = off;
        out->flow_overlay_data.dip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.sport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 7 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 7);
      off += 7;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.is_ipv6 = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      state = PK_S_PARSE_CUSTOMER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_FLOW_ENCAP_U0: {
      out->flow_overlay_data_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_bit_off = off;
        out->flow_overlay_data.sip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_bit_off = off;
        out->flow_overlay_data.dip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_mask_bit_off = off;
        out->flow_overlay_data.sip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_mask_bit_off = off;
        out->flow_overlay_data.dip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.sport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 7 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 7);
      off += 7;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.is_ipv6 = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      out->flow_u0_encap_data_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.vni = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.reserved = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_sip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_dip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_smac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.dash_encapsulation = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      state = PK_S_PARSE_CUSTOMER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_FLOW_ENCAP_U1: {
      out->flow_overlay_data_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_bit_off = off;
        out->flow_overlay_data.sip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_bit_off = off;
        out->flow_overlay_data.dip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_mask_bit_off = off;
        out->flow_overlay_data.sip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_mask_bit_off = off;
        out->flow_overlay_data.dip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.sport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 7 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 7);
      off += 7;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.is_ipv6 = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      out->flow_u1_encap_data_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.vni = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.reserved = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_sip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_dip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_smac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.dash_encapsulation = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      state = PK_S_PARSE_CUSTOMER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_FLOW_ENCAP_U0_U1: {
      out->flow_overlay_data_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_bit_off = off;
        out->flow_overlay_data.sip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_bit_off = off;
        out->flow_overlay_data.dip_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.sip_mask_bit_off = off;
        out->flow_overlay_data.sip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->flow_overlay_data.dip_mask_bit_off = off;
        out->flow_overlay_data.dip_mask_bit_len = vlen * 8;
        off += vlen * 8;
      }
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.sport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.dport = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 7 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 7);
      off += 7;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_overlay_data.is_ipv6 = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      out->flow_u0_encap_data_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.vni = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.reserved = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_sip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_dip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_smac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.underlay_dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u0_encap_data.dash_encapsulation = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      out->flow_u1_encap_data_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.vni = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.reserved = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_sip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_dip = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_smac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.underlay_dmac = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->flow_u1_encap_data.dash_encapsulation = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      state = PK_S_PARSE_CUSTOMER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_U0_IPV4: {
      out->u0_ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.ihl = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.diffserv = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.total_len = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.identification = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.frag_offset = (uint16_t)pk_read_bits(buf, bit_len, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.ttl = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.protocol = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.hdr_checksum = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.src_addr = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv4.dst_addr = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      uint64_t key0 = (uint64_t)out->u0_ipv4.version;
      if (key0 == 4ULL) {
        state = PK_S_PARSE_U0_IPV4_IHL;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_IPV4INCORRECTVERSION;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_U0_IPV4_IHL: {
      uint64_t key0 = (uint64_t)out->u0_ipv4.ihl;
      if (key0 == 0ULL) {
        out->outcome = 1;
        out->reason = PK_R_INVALIDIPV4HEADER;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 1ULL) {
        out->outcome = 1;
        out->reason = PK_R_INVALIDIPV4HEADER;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 2ULL) {
        out->outcome = 1;
        out->reason = PK_R_INVALIDIPV4HEADER;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 3ULL) {
        out->outcome = 1;
        out->reason = PK_R_INVALIDIPV4HEADER;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 4ULL) {
        out->outcome = 1;
        out->reason = PK_R_INVALIDIPV4HEADER;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 5ULL) {
        state = PK_S_DISPATCH_ON_U0_PROTOCOL;
        continue;
      } else {
        state = PK_S_PARSE_U0_IPV4OPTIONS;
        continue;
      }
    }
    case PK_S_PARSE_U0_IPV4OPTIONS: {
      out->u0_ipv4options_present = 1;
      {
        uint64_t vlen = (((uint64_t)out->u0_ipv4.ihl - 5ULL) * 4ULL);
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->u0_ipv4options.options_bit_off = off;
        out->u0_ipv4options.options_bit_len = vlen * 8;
        off += vlen * 8;
      }
      state = PK_S_DISPATCH_ON_U0_PROTOCOL;
      continue;
    }
    case PK_S_DISPATCH_ON_U0_PROTOCOL: {
      uint64_t key0 = (uint64_t)out->u0_ipv4.protocol;
      if (key0 == 17ULL) {
        state = PK_S_PARSE_U0_UDP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_U0_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_U0_IPV6: {
      out->u0_ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.traffic_class = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.flow_label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.payload_length = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.next_header = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.hop_limit = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.src_addr_hi = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.src_addr_lo = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.dst_addr_hi = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_ipv6.dst_addr_lo = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      uint64_t key0 = (uint64_t)out->u0_ipv6.next_header;
      if (key0 == 17ULL) {
        state = PK_S_PARSE_U0_UDP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_U0_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_U0_UDP: {
      out->u0_udp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_udp.src_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_udp.dst_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_udp.length = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_udp.checksum = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      uint64_t key0 = (uint64_t)out->u0_udp.dst_port;
      if (key0 == 4789ULL) {
        state = PK_S_PARSE_U0_VXLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_U0_TCP: {
      out->u0_tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.src_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.dst_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.seq_no = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.ack_no = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.data_offset = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.res = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.ecn = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.window = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.checksum = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_tcp.urgent_ptr = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_U0_VXLAN: {
      out->u0_vxlan_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_vxlan.flags = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_vxlan.reserved = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK]);
      off += 24;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_vxlan.vni = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->u0_vxlan.reserved_2 = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      state = PK_S_PARSE_CUSTOMER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_CUSTOMER_ETHERNET: {
      out->customer_ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ethernet.dst_addr = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ethernet.src_addr = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ethernet.ether_type = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      uint64_t key0 = (uint64_t)out->customer_ethernet.ether_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_CUSTOMER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_CUSTOMER_IPV6;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_CUSTOMER_IPV4: {
      out->customer_ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.ihl = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.diffserv = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.total_len = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.identification = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.frag_offset = (uint16_t)pk_read_bits(buf, bit_len, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.ttl = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.protocol = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.hdr_checksum = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.src_addr = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv4.dst_addr = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      uint64_t key0 = (uint64_t)out->customer_ipv4.version;
      if (key0 == 4ULL) {
        state = PK_S_PARSE_CUSTOMER_IPV4_IHL;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_IPV4INCORRECTVERSION;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_CUSTOMER_IPV4_IHL: {
      uint64_t key0 = (uint64_t)out->customer_ipv4.ihl;
      if (key0 == 5ULL) {
        state = PK_S_DISPATCH_ON_CUSTOMER_PROTOCOL;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_IPV4OPTIONSNOTSUPPORTED;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_DISPATCH_ON_CUSTOMER_PROTOCOL: {
      uint64_t key0 = (uint64_t)out->customer_ipv4.protocol;
      if (key0 == 17ULL) {
        state = PK_S_PARSE_CUSTOMER_UDP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_CUSTOMER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_CUSTOMER_IPV6: {
      out->customer_ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.traffic_class = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.flow_label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.payload_length = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.next_header = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.hop_limit = (uint8_t)((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK]);
      off += 8;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.src_addr_hi = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.src_addr_lo = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.dst_addr_hi = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_ipv6.dst_addr_lo = (uint64_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 56) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 48) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 40) | ((uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK] << 32) | ((uint64_t)buf[((off >> 3) + 4) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 5) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 6) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 7) & PK_BUF_MASK]);
      off += 64;
      uint64_t key0 = (uint64_t)out->customer_ipv6.next_header;
      if (key0 == 17ULL) {
        state = PK_S_PARSE_CUSTOMER_UDP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_CUSTOMER_TCP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_CUSTOMER_UDP: {
      out->customer_udp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_udp.src_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_udp.dst_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_udp.length = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_udp.checksum = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_CUSTOMER_TCP: {
      out->customer_tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.src_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.dst_port = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.seq_no = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.ack_no = (uint32_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 24) | ((uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK] << 16) | ((uint64_t)buf[((off >> 3) + 2) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 3) & PK_BUF_MASK]);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.data_offset = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.res = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.ecn = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.window = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.checksum = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->customer_tcp.urgent_ptr = (uint16_t)(((uint64_t)buf[((off >> 3) + 0) & PK_BUF_MASK] << 8) | (uint64_t)buf[((off >> 3) + 1) & PK_BUF_MASK]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

uint64_t pk_entry(void *mem) {
  const uint8_t *m = (const uint8_t *)mem;
  uint64_t bit_len = 0;
  uint32_t i;
  for (i = 0; i < 8; i++) bit_len |= (uint64_t)m[i] << (8 * i);
  pk_dash_parser_result_t out = {0};
  pk_dash_parser_parse_core(m + 8, bit_len, &out);
  return ((uint64_t)out.outcome << 56) | ((uint64_t)out.reason << 48) | (out.consumed_bits & 0xFFFFFFFFFFFFULL);
}
