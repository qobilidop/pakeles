/* Generated by pakeles from `sai_parser`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

static uint64_t pk_read_bits(const uint8_t *buf, uint64_t avail, uint64_t off, uint32_t n) {
  (void)avail;
  uint64_t v = 0;
  uint32_t i;
  for (i = 0; i < n; i++) {
    uint64_t pos = off + i;
    v = (v << 1) | (uint64_t)((buf[pos >> 3] >> (7 - (pos & 7))) & 1);
  }
  return v;
}

#define PK_S_PARSE_ETHERNET 0
#define PK_S_PARSE_VLAN 1
#define PK_S_PARSE_IPV4 2
#define PK_S_PARSE_IPV6 3
#define PK_S_PARSE_ARP 4
#define PK_S_PARSE_ICMP 5
#define PK_S_PARSE_TCP 6
#define PK_S_PARSE_UDP 7

static int pk_sai_parser_parse_core(const uint8_t *buf, uint64_t bit_len, pk_sai_parser_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_ETHERNET;
  uint32_t depth;
  for (depth = 0; depth < 6u; depth++) {
    switch (state) {
    case PK_S_PARSE_ETHERNET: {
      out->ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.dst = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.src = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->ethernet.ether_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VLAN: {
      out->vlan_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.dei = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.vid = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan.ether_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IPV4: {
      out->ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ihl = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dscp = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ecn = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.total_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.identification = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.frag_offset = (uint16_t)pk_read_bits(buf, bit_len, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.protocol = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.header_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.src_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dst_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      uint64_t key0 = (uint64_t)out->ipv4.protocol;
      if (key0 == 1ULL) {
        state = PK_S_PARSE_ICMP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_UDP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IPV6: {
      out->ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.traffic_class = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.flow_label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.payload_length = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.next_header = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.hop_limit = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.src_addr_bit_off = off;
        out->ipv6.src_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.dst_addr_bit_off = off;
        out->ipv6.dst_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6.next_header;
      if (key0 == 58ULL) {
        state = PK_S_PARSE_ICMP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_UDP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ARP: {
      out->arp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.hw_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.proto_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.hw_addr_len = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.proto_addr_len = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.opcode = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.sender_hw = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.sender_proto = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.target_hw = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->arp.target_proto = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_ICMP: {
      out->icmp_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.type = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.code = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.rest = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_TCP: {
      out->tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.seq = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ack = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.data_offset = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.flags = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.window = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.urgent = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_UDP: {
      out->udp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.length = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_sai_parser_parse(const uint8_t *buf, uint64_t bit_len, pk_sai_parser_result_t *out) {
  pk_sai_parser_result_t zero = {0};
  *out = zero;
  return pk_sai_parser_parse_core(buf, bit_len, out);
}

const char *pk_sai_parser_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  case 4: return "out of region bounds";
  case 5: return "region out of bounds";
  case 6: return "region not exhausted";
  default: return "";
  }
}
