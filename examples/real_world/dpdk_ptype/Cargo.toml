[package]
name = "pakeles-example-dpdk-ptype"
version = "0.1.0"
edition = "2021"
publish = false
description = "Incumbent-agreement example: (ptype, hdr_lens) vs DPDK rte_net_get_ptype (projection + gate)"

[dependencies]
pakeles = { path = "../../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

[dev-dependencies]
pakeles-testkit = { path = "../../../pakeles-testkit" }
