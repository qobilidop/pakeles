-- Generated by pakeles from `eth_ipvx_l4` (ir 0.2.0).
-- Do not edit: regenerate with `pakeles gen lua`.
-- Requires Lua 5.2 (bit32); length arithmetic is signed —
-- negative computed lengths are treated as out of bounds,
-- matching the reference interpreter's outcome.
local p = Proto("pakeles_eth_ipvx_l4", "Pakeles eth_ipvx_l4")

local ef_error = ProtoExpert.new("pakeles_eth_ipvx_l4.malformed", "Malformed", expert.group.MALFORMED, expert.severity.ERROR)
local ef_info = ProtoExpert.new("pakeles_eth_ipvx_l4.boundary", "Payload boundary", expert.group.PROTOCOL, expert.severity.NOTE)
p.experts = { ef_error, ef_info }

local pf = {}
pf.f_hdr_ethernet = ProtoField.none("pakeles_eth_ipvx_l4.ethernet", "ethernet")
pf.f_ethernet_dst = ProtoField.ether("pakeles_eth_ipvx_l4.ethernet.dst", "Destination")
pf.f_ethernet_src = ProtoField.ether("pakeles_eth_ipvx_l4.ethernet.src", "Source")
pf.f_ethernet_ethertype = ProtoField.uint16("pakeles_eth_ipvx_l4.ethernet.ethertype", "Type", base.HEX, { [2048] = "IPv4", [2054] = "ARP", [33024] = "802.1Q VLAN", [34525] = "IPv6" })
pf.f_hdr_ipv4 = ProtoField.none("pakeles_eth_ipvx_l4.ipv4", "ipv4")
pf.f_ipv4_version = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv4.version", "Version", base.DEC)
pf.f_ipv4_ihl = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv4.ihl", "Header Length", base.DEC)
pf.f_ipv4_dscp = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv4.dscp", "DSCP", base.DEC)
pf.f_ipv4_ecn = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv4.ecn", "ECN", base.DEC)
pf.f_ipv4_total_len = ProtoField.uint16("pakeles_eth_ipvx_l4.ipv4.total_len", "Total Length", base.DEC)
pf.f_ipv4_id = ProtoField.uint16("pakeles_eth_ipvx_l4.ipv4.id", "Identification", base.HEX)
pf.f_ipv4_flags = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv4.flags", "Flags", base.HEX)
pf.f_ipv4_frag_offset = ProtoField.uint16("pakeles_eth_ipvx_l4.ipv4.frag_offset", "Fragment Offset", base.DEC)
pf.f_ipv4_ttl = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv4.ttl", "Time to Live", base.DEC)
pf.f_ipv4_protocol = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv4.protocol", "Protocol", base.DEC, { [1] = "ICMP", [6] = "TCP", [17] = "UDP" })
pf.f_ipv4_checksum = ProtoField.uint16("pakeles_eth_ipvx_l4.ipv4.checksum", "Header Checksum", base.HEX)
pf.f_ipv4_src = ProtoField.ipv4("pakeles_eth_ipvx_l4.ipv4.src", "Source Address")
pf.f_ipv4_dst = ProtoField.ipv4("pakeles_eth_ipvx_l4.ipv4.dst", "Destination Address")
pf.f_ipv4_options = ProtoField.bytes("pakeles_eth_ipvx_l4.ipv4.options", "options")
pf.f_hdr_ipv6 = ProtoField.none("pakeles_eth_ipvx_l4.ipv6", "ipv6")
pf.f_ipv6_version = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv6.version", "Version", base.DEC)
pf.f_ipv6_traffic_class = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv6.traffic_class", "Traffic Class", base.HEX)
pf.f_ipv6_flow_label = ProtoField.uint24("pakeles_eth_ipvx_l4.ipv6.flow_label", "Flow Label", base.HEX)
pf.f_ipv6_payload_length = ProtoField.uint16("pakeles_eth_ipvx_l4.ipv6.payload_length", "Payload Length", base.DEC)
pf.f_ipv6_next_header = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv6.next_header", "Next Header", base.DEC, { [1] = "ICMP", [6] = "TCP", [17] = "UDP" })
pf.f_ipv6_hop_limit = ProtoField.uint8("pakeles_eth_ipvx_l4.ipv6.hop_limit", "Hop Limit", base.DEC)
pf.f_ipv6_src = ProtoField.bytes("pakeles_eth_ipvx_l4.ipv6.src", "src")
pf.f_ipv6_dst = ProtoField.bytes("pakeles_eth_ipvx_l4.ipv6.dst", "dst")
pf.f_hdr_tcp = ProtoField.none("pakeles_eth_ipvx_l4.tcp", "tcp")
pf.f_tcp_sport = ProtoField.uint16("pakeles_eth_ipvx_l4.tcp.sport", "Source Port", base.DEC)
pf.f_tcp_dport = ProtoField.uint16("pakeles_eth_ipvx_l4.tcp.dport", "Destination Port", base.DEC)
pf.f_tcp_seq = ProtoField.uint32("pakeles_eth_ipvx_l4.tcp.seq", "Sequence Number", base.DEC)
pf.f_tcp_ack = ProtoField.uint32("pakeles_eth_ipvx_l4.tcp.ack", "Acknowledgment Number", base.DEC)
pf.f_tcp_data_offset = ProtoField.uint8("pakeles_eth_ipvx_l4.tcp.data_offset", "Data Offset", base.DEC)
pf.f_tcp_reserved = ProtoField.uint8("pakeles_eth_ipvx_l4.tcp.reserved", "Reserved", base.HEX)
pf.f_tcp_flags = ProtoField.uint8("pakeles_eth_ipvx_l4.tcp.flags", "Flags", base.HEX)
pf.f_tcp_window = ProtoField.uint16("pakeles_eth_ipvx_l4.tcp.window", "Window", base.DEC)
pf.f_tcp_checksum = ProtoField.uint16("pakeles_eth_ipvx_l4.tcp.checksum", "Checksum", base.HEX)
pf.f_tcp_urgent = ProtoField.uint16("pakeles_eth_ipvx_l4.tcp.urgent", "Urgent Pointer", base.DEC)
pf.f_hdr_udp = ProtoField.none("pakeles_eth_ipvx_l4.udp", "udp")
pf.f_udp_sport = ProtoField.uint16("pakeles_eth_ipvx_l4.udp.sport", "Source Port", base.DEC)
pf.f_udp_dport = ProtoField.uint16("pakeles_eth_ipvx_l4.udp.dport", "Destination Port", base.DEC)
pf.f_udp_length = ProtoField.uint16("pakeles_eth_ipvx_l4.udp.length", "Length", base.DEC)
pf.f_udp_checksum = ProtoField.uint16("pakeles_eth_ipvx_l4.udp.checksum", "Checksum", base.HEX)
pf.f_payload = ProtoField.bytes("pakeles_eth_ipvx_l4.payload", "Payload")
p.fields = { pf.f_hdr_ethernet, pf.f_ethernet_dst, pf.f_ethernet_src, pf.f_ethernet_ethertype, pf.f_hdr_ipv4, pf.f_ipv4_version, pf.f_ipv4_ihl, pf.f_ipv4_dscp, pf.f_ipv4_ecn, pf.f_ipv4_total_len, pf.f_ipv4_id, pf.f_ipv4_flags, pf.f_ipv4_frag_offset, pf.f_ipv4_ttl, pf.f_ipv4_protocol, pf.f_ipv4_checksum, pf.f_ipv4_src, pf.f_ipv4_dst, pf.f_ipv4_options, pf.f_hdr_ipv6, pf.f_ipv6_version, pf.f_ipv6_traffic_class, pf.f_ipv6_flow_label, pf.f_ipv6_payload_length, pf.f_ipv6_next_header, pf.f_ipv6_hop_limit, pf.f_ipv6_src, pf.f_ipv6_dst, pf.f_hdr_tcp, pf.f_tcp_sport, pf.f_tcp_dport, pf.f_tcp_seq, pf.f_tcp_ack, pf.f_tcp_data_offset, pf.f_tcp_reserved, pf.f_tcp_flags, pf.f_tcp_window, pf.f_tcp_checksum, pf.f_tcp_urgent, pf.f_hdr_udp, pf.f_udp_sport, pf.f_udp_dport, pf.f_udp_length, pf.f_udp_checksum, pf.f_payload }

local states = {}

local v = {}

local function add_payload(buf, tree, off)
  if off < buf:len() * 8 then
    tree:add(pf.f_payload, buf(math.floor(off / 8)))
  end
end

function states.parse_ethernet(buf, pinfo, tree, off, depth)
  depth = depth + 1
  if depth > 4 then
    tree:add_proto_expert_info(ef_error, "max depth exceeded")
    return off
  end
  local avail = buf:len() * 8
  local hdr_ethernet = tree:add(pf.f_hdr_ethernet, buf(math.floor(off / 8)))
  if off + 48 > avail then
    hdr_ethernet:add_proto_expert_info(ef_error, "out of bounds in ethernet.dst")
    return off
  end
  hdr_ethernet:add(pf.f_ethernet_dst, buf(math.floor(off / 8), 6))
  off = off + 48
  if off + 48 > avail then
    hdr_ethernet:add_proto_expert_info(ef_error, "out of bounds in ethernet.src")
    return off
  end
  hdr_ethernet:add(pf.f_ethernet_src, buf(math.floor(off / 8), 6))
  off = off + 48
  if off + 16 > avail then
    hdr_ethernet:add_proto_expert_info(ef_error, "out of bounds in ethernet.ethertype")
    return off
  end
  v.v_ethernet_ethertype = buf():bitfield(off, 16)
  hdr_ethernet:add(pf.f_ethernet_ethertype, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), v.v_ethernet_ethertype)
  off = off + 16
  if v.v_ethernet_ethertype == 2048 then
    return states.parse_ipv4(buf, pinfo, tree, off, depth)
  elseif v.v_ethernet_ethertype == 34525 then
    return states.parse_ipv6(buf, pinfo, tree, off, depth)
  else
    add_payload(buf, tree, off)
    tree:add_proto_expert_info(ef_info, "unsupported ethertype")
    return off
  end
end

function states.parse_ipv4(buf, pinfo, tree, off, depth)
  depth = depth + 1
  if depth > 4 then
    tree:add_proto_expert_info(ef_error, "max depth exceeded")
    return off
  end
  local avail = buf:len() * 8
  local hdr_ipv4 = tree:add(pf.f_hdr_ipv4, buf(math.floor(off / 8)))
  if off + 4 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.version")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_version, buf(math.floor(off / 8), math.floor((off % 8 + 4 + 7) / 8)), buf():bitfield(off, 4))
  off = off + 4
  if off + 4 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.ihl")
    return off
  end
  v.v_ipv4_ihl = buf():bitfield(off, 4)
  hdr_ipv4:add(pf.f_ipv4_ihl, buf(math.floor(off / 8), math.floor((off % 8 + 4 + 7) / 8)), v.v_ipv4_ihl)
  off = off + 4
  if off + 6 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.dscp")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_dscp, buf(math.floor(off / 8), math.floor((off % 8 + 6 + 7) / 8)), buf():bitfield(off, 6))
  off = off + 6
  if off + 2 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.ecn")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_ecn, buf(math.floor(off / 8), math.floor((off % 8 + 2 + 7) / 8)), buf():bitfield(off, 2))
  off = off + 2
  if off + 16 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.total_len")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_total_len, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 16 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.id")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_id, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 3 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.flags")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_flags, buf(math.floor(off / 8), math.floor((off % 8 + 3 + 7) / 8)), buf():bitfield(off, 3))
  off = off + 3
  if off + 13 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.frag_offset")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_frag_offset, buf(math.floor(off / 8), math.floor((off % 8 + 13 + 7) / 8)), buf():bitfield(off, 13))
  off = off + 13
  if off + 8 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.ttl")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_ttl, buf(math.floor(off / 8), math.floor((off % 8 + 8 + 7) / 8)), buf():bitfield(off, 8))
  off = off + 8
  if off + 8 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.protocol")
    return off
  end
  v.v_ipv4_protocol = buf():bitfield(off, 8)
  hdr_ipv4:add(pf.f_ipv4_protocol, buf(math.floor(off / 8), math.floor((off % 8 + 8 + 7) / 8)), v.v_ipv4_protocol)
  off = off + 8
  if off + 16 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.checksum")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_checksum, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 32 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.src")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_src, buf(math.floor(off / 8), 4))
  off = off + 32
  if off + 32 > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.dst")
    return off
  end
  hdr_ipv4:add(pf.f_ipv4_dst, buf(math.floor(off / 8), 4))
  off = off + 32
  local len_ipv4_options = (((v.v_ipv4_ihl * 4) - 20) * 8)
  if len_ipv4_options < 0 or off + len_ipv4_options > avail then
    hdr_ipv4:add_proto_expert_info(ef_error, "out of bounds in ipv4.options")
    return off
  end
  if len_ipv4_options > 0 then
    hdr_ipv4:add(pf.f_ipv4_options, buf(math.floor(off / 8), math.floor(len_ipv4_options / 8)))
  end
  off = off + len_ipv4_options
  if v.v_ipv4_protocol == 6 then
    return states.parse_tcp(buf, pinfo, tree, off, depth)
  elseif v.v_ipv4_protocol == 17 then
    return states.parse_udp(buf, pinfo, tree, off, depth)
  else
    add_payload(buf, tree, off)
    tree:add_proto_expert_info(ef_info, "unsupported ip protocol")
    return off
  end
end

function states.parse_ipv6(buf, pinfo, tree, off, depth)
  depth = depth + 1
  if depth > 4 then
    tree:add_proto_expert_info(ef_error, "max depth exceeded")
    return off
  end
  local avail = buf:len() * 8
  local hdr_ipv6 = tree:add(pf.f_hdr_ipv6, buf(math.floor(off / 8)))
  if off + 4 > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.version")
    return off
  end
  hdr_ipv6:add(pf.f_ipv6_version, buf(math.floor(off / 8), math.floor((off % 8 + 4 + 7) / 8)), buf():bitfield(off, 4))
  off = off + 4
  if off + 8 > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.traffic_class")
    return off
  end
  hdr_ipv6:add(pf.f_ipv6_traffic_class, buf(math.floor(off / 8), math.floor((off % 8 + 8 + 7) / 8)), buf():bitfield(off, 8))
  off = off + 8
  if off + 20 > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.flow_label")
    return off
  end
  hdr_ipv6:add(pf.f_ipv6_flow_label, buf(math.floor(off / 8), math.floor((off % 8 + 20 + 7) / 8)), buf():bitfield(off, 20))
  off = off + 20
  if off + 16 > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.payload_length")
    return off
  end
  hdr_ipv6:add(pf.f_ipv6_payload_length, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 8 > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.next_header")
    return off
  end
  v.v_ipv6_next_header = buf():bitfield(off, 8)
  hdr_ipv6:add(pf.f_ipv6_next_header, buf(math.floor(off / 8), math.floor((off % 8 + 8 + 7) / 8)), v.v_ipv6_next_header)
  off = off + 8
  if off + 8 > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.hop_limit")
    return off
  end
  hdr_ipv6:add(pf.f_ipv6_hop_limit, buf(math.floor(off / 8), math.floor((off % 8 + 8 + 7) / 8)), buf():bitfield(off, 8))
  off = off + 8
  local len_ipv6_src = (16 * 8)
  if len_ipv6_src < 0 or off + len_ipv6_src > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.src")
    return off
  end
  if len_ipv6_src > 0 then
    hdr_ipv6:add(pf.f_ipv6_src, buf(math.floor(off / 8), math.floor(len_ipv6_src / 8)))
  end
  off = off + len_ipv6_src
  local len_ipv6_dst = (16 * 8)
  if len_ipv6_dst < 0 or off + len_ipv6_dst > avail then
    hdr_ipv6:add_proto_expert_info(ef_error, "out of bounds in ipv6.dst")
    return off
  end
  if len_ipv6_dst > 0 then
    hdr_ipv6:add(pf.f_ipv6_dst, buf(math.floor(off / 8), math.floor(len_ipv6_dst / 8)))
  end
  off = off + len_ipv6_dst
  if v.v_ipv6_next_header == 6 then
    return states.parse_tcp(buf, pinfo, tree, off, depth)
  elseif v.v_ipv6_next_header == 17 then
    return states.parse_udp(buf, pinfo, tree, off, depth)
  else
    add_payload(buf, tree, off)
    tree:add_proto_expert_info(ef_info, "unsupported ip protocol")
    return off
  end
end

function states.parse_tcp(buf, pinfo, tree, off, depth)
  depth = depth + 1
  if depth > 4 then
    tree:add_proto_expert_info(ef_error, "max depth exceeded")
    return off
  end
  local avail = buf:len() * 8
  local hdr_tcp = tree:add(pf.f_hdr_tcp, buf(math.floor(off / 8)))
  if off + 16 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.sport")
    return off
  end
  hdr_tcp:add(pf.f_tcp_sport, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 16 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.dport")
    return off
  end
  hdr_tcp:add(pf.f_tcp_dport, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 32 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.seq")
    return off
  end
  hdr_tcp:add(pf.f_tcp_seq, buf(math.floor(off / 8), math.floor((off % 8 + 32 + 7) / 8)), buf():bitfield(off, 32))
  off = off + 32
  if off + 32 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.ack")
    return off
  end
  hdr_tcp:add(pf.f_tcp_ack, buf(math.floor(off / 8), math.floor((off % 8 + 32 + 7) / 8)), buf():bitfield(off, 32))
  off = off + 32
  if off + 4 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.data_offset")
    return off
  end
  hdr_tcp:add(pf.f_tcp_data_offset, buf(math.floor(off / 8), math.floor((off % 8 + 4 + 7) / 8)), buf():bitfield(off, 4))
  off = off + 4
  if off + 4 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.reserved")
    return off
  end
  hdr_tcp:add(pf.f_tcp_reserved, buf(math.floor(off / 8), math.floor((off % 8 + 4 + 7) / 8)), buf():bitfield(off, 4))
  off = off + 4
  if off + 8 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.flags")
    return off
  end
  hdr_tcp:add(pf.f_tcp_flags, buf(math.floor(off / 8), math.floor((off % 8 + 8 + 7) / 8)), buf():bitfield(off, 8))
  off = off + 8
  if off + 16 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.window")
    return off
  end
  hdr_tcp:add(pf.f_tcp_window, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 16 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.checksum")
    return off
  end
  hdr_tcp:add(pf.f_tcp_checksum, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 16 > avail then
    hdr_tcp:add_proto_expert_info(ef_error, "out of bounds in tcp.urgent")
    return off
  end
  hdr_tcp:add(pf.f_tcp_urgent, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  add_payload(buf, tree, off)
  return off
end

function states.parse_udp(buf, pinfo, tree, off, depth)
  depth = depth + 1
  if depth > 4 then
    tree:add_proto_expert_info(ef_error, "max depth exceeded")
    return off
  end
  local avail = buf:len() * 8
  local hdr_udp = tree:add(pf.f_hdr_udp, buf(math.floor(off / 8)))
  if off + 16 > avail then
    hdr_udp:add_proto_expert_info(ef_error, "out of bounds in udp.sport")
    return off
  end
  hdr_udp:add(pf.f_udp_sport, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 16 > avail then
    hdr_udp:add_proto_expert_info(ef_error, "out of bounds in udp.dport")
    return off
  end
  hdr_udp:add(pf.f_udp_dport, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 16 > avail then
    hdr_udp:add_proto_expert_info(ef_error, "out of bounds in udp.length")
    return off
  end
  hdr_udp:add(pf.f_udp_length, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  if off + 16 > avail then
    hdr_udp:add_proto_expert_info(ef_error, "out of bounds in udp.checksum")
    return off
  end
  hdr_udp:add(pf.f_udp_checksum, buf(math.floor(off / 8), math.floor((off % 8 + 16 + 7) / 8)), buf():bitfield(off, 16))
  off = off + 16
  add_payload(buf, tree, off)
  return off
end

function p.dissector(buf, pinfo, tree)
  local root = tree:add(p, buf())
  states.parse_ethernet(buf, pinfo, root, 0, 0)
end

-- Postdissector: runs on every frame after built-in dissection,
-- so our tree appears alongside Wireshark's own — side-by-side
-- comparison for free, no dissector-table precedence fights.
register_postdissector(p)
