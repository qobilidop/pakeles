# `p4lang_switch_parser`

Generated by `pakeles doc` from IR version 0.1.0. Parse depth bound: 43.

The parser of classic switch.p4: `p4lang/switch` @ 7874f565,
as-shipped feature defaults (p4features.h) + __TARGET_BMV2__.
Two layers (outer + inner_* twins after tunnel decap), the
fabric-header family, INT over VXLAN-GPE, and sFlow.

## Header `ethernet`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `dst_addr` | 48 | MAC | Destination |  |  |
| `src_addr` | 48 | MAC | Source |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 8939=ERSPAN type III, 25944=Transparent Ethernet Bridging, 33024=802.1Q, 34525=IPv6, 34825=Slow Protocols (source: LACP), 34887=MPLS (unicast), 35020=LLDP, 36864=internal fabric header (source: BF_FABRIC), 37120=QinQ (legacy 0x9100) |  |

## Header `llc_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `dsap` | 8 | hex | DSAP |  |  |
| `ssap` | 8 | hex | SSAP |  |  |
| `control_` | 8 | hex | Control |  |  |

## Header `snap_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `oui` | 24 | hex | OUI |  |  |
| `type_` | 16 | hex | Type | 2048=IPv4, 2054=ARP, 8939=ERSPAN type III, 25944=Transparent Ethernet Bridging, 33024=802.1Q, 34525=IPv6, 34825=Slow Protocols (source: LACP), 34887=MPLS (unicast), 35020=LLDP, 36864=internal fabric header (source: BF_FABRIC), 37120=QinQ (legacy 0x9100) |  |

## Header `roce_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `ib_grh_0` | 64 | hex | IB GRH (bits 0-63) |  |  |
| `ib_grh_1` | 64 | hex | IB GRH (bits 64-127) |  |  |
| `ib_grh_2` | 64 | hex | IB GRH (bits 128-191) |  |  |
| `ib_grh_3` | 64 | hex | IB GRH (bits 192-255) |  |  |
| `ib_grh_4` | 64 | hex | IB GRH (bits 256-319) |  |  |
| `ib_bth_0` | 64 | hex | IB BTH (bits 0-63) |  |  |
| `ib_bth_1` | 32 | hex | IB BTH (bits 64-95) |  |  |

## Header `fcoe_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `type_` | 4 |  | Type |  |  |
| `sof` | 8 | hex | SOF |  |  |
| `rsvd1` | 32 |  | Reserved |  |  |
| `ts_upper` | 32 | dec | Timestamp (upper) |  |  |
| `ts_lower` | 32 | dec | Timestamp (lower) |  |  |
| `size_` | 32 | dec | Size |  |  |
| `eof` | 8 | hex | EOF |  |  |
| `rsvd2` | 24 |  | Reserved |  |  |

## Header `vlan_tag`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `pcp` | 3 |  | PCP |  |  |
| `cfi` | 1 |  | CFI |  |  |
| `vid` | 12 | dec | VLAN ID |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 8939=ERSPAN type III, 25944=Transparent Ethernet Bridging, 33024=802.1Q, 34525=IPv6, 34825=Slow Protocols (source: LACP), 34887=MPLS (unicast), 35020=LLDP, 36864=internal fabric header (source: BF_FABRIC), 37120=QinQ (legacy 0x9100) |  |

## Header `mpls`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `label` | 20 | dec | Label |  |  |
| `exp` | 3 |  | EXP |  |  |
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `ttl` | 8 | dec | TTL |  |  |

## Header `ip_version_nibble`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `v` | 4 | dec | Version Nibble | 4=IPv4, 6=IPv6 |  |

## Header `inner_ipv4_rest`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `ihl` | 4 | dec | IHL |  |  |
| `diffserv` | 8 | hex | DiffServ |  |  |
| `total_len` | 16 | dec | Total Length |  |  |
| `identification` | 16 | hex | Identification |  |  |
| `flags` | 3 |  | Flags |  |  |
| `frag_offset` | 13 | dec | Fragment Offset |  |  |
| `ttl` | 8 | dec | TTL |  |  |
| `protocol` | 8 | dec | Protocol | 1=ICMP, 2=IGMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 41=IPv6-in-IP, 47=GRE, 58=ICMPv6, 88=EIGRP, 89=OSPF, 103=PIM, 112=VRRP |  |
| `hdr_checksum` | 16 | hex | Header Checksum |  |  |
| `src_addr` | 32 | hex | Source |  |  |
| `dst_addr` | 32 | hex | Destination |  |  |

## Header `inner_ipv6_rest`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `traffic_class` | 8 | hex | Traffic Class |  |  |
| `flow_label` | 20 | hex | Flow Label |  |  |
| `payload_len` | 16 | dec | Payload Length |  |  |
| `next_hdr` | 8 | dec | Next Header | 1=ICMP, 2=IGMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 41=IPv6-in-IP, 47=GRE, 58=ICMPv6, 88=EIGRP, 89=OSPF, 103=PIM, 112=VRRP |  |
| `hop_limit` | 8 | dec | Hop Limit |  |  |
| `src_addr` | `16` bytes |  |  |  |  |
| `dst_addr` | `16` bytes |  |  |  |  |

## Header `ipv4`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `ihl` | 4 | dec | IHL |  |  |
| `diffserv` | 8 | hex | DiffServ |  |  |
| `total_len` | 16 | dec | Total Length |  |  |
| `identification` | 16 | hex | Identification |  |  |
| `flags` | 3 |  | Flags |  |  |
| `frag_offset` | 13 | dec | Fragment Offset |  |  |
| `ttl` | 8 | dec | TTL |  |  |
| `protocol` | 8 | dec | Protocol | 1=ICMP, 2=IGMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 41=IPv6-in-IP, 47=GRE, 58=ICMPv6, 88=EIGRP, 89=OSPF, 103=PIM, 112=VRRP |  |
| `hdr_checksum` | 16 | hex | Header Checksum |  |  |
| `src_addr` | 32 | hex | Source |  |  |
| `dst_addr` | 32 | hex | Destination |  |  |

## Header `udp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port | 67=DHCP (server), 68=DHCP (client), 520=RIP, 521=RIPng, 546=DHCPv6 (client), 547=DHCPv6 (server), 1985=HSRP, 4789=VXLAN, 4790=VXLAN-GPE, 6081=Geneve, 6343=sFlow |  |
| `length_` | 16 | dec | Length |  |  |
| `checksum` | 16 | hex | Checksum |  |  |

## Header `gre`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `c` | 1 |  | C (Checksum Present) |  |  |
| `r` | 1 |  | R (Routing Present) |  |  |
| `k` | 1 |  | K (Key Present) |  |  |
| `s` | 1 |  | S (Sequence Present) |  |  |
| `strict` | 1 |  | s (Strict Source Route) |  |  |
| `recurse` | 3 |  | Recursion Control |  |  |
| `flags` | 5 |  | Flags |  |  |
| `ver` | 3 |  | Version |  |  |
| `proto` | 16 | hex | Protocol Type | 2048=IPv4, 8939=ERSPAN type III, 25944=Transparent Ethernet Bridging, 34525=IPv6 |  |

## Header `ipv6`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `traffic_class` | 8 | hex | Traffic Class |  |  |
| `flow_label` | 20 | hex | Flow Label |  |  |
| `payload_len` | 16 | dec | Payload Length |  |  |
| `next_hdr` | 8 | dec | Next Header | 1=ICMP, 2=IGMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 41=IPv6-in-IP, 47=GRE, 58=ICMPv6, 88=EIGRP, 89=OSPF, 103=PIM, 112=VRRP |  |
| `hop_limit` | 8 | dec | Hop Limit |  |  |
| `src_addr` | `16` bytes |  |  |  |  |
| `dst_addr` | `16` bytes |  |  |  |  |

## Header `icmp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `type_code` | 16 | hex | Type/Code |  |  |
| `hdr_checksum` | 16 | hex | Checksum |  |  |

## Header `tcp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port | 179=BGP, 639=MSDP |  |
| `seq_no` | 32 | dec | Sequence Number |  |  |
| `ack_no` | 32 | dec | Ack Number |  |  |
| `data_offset` | 4 | dec | Data Offset |  |  |
| `res` | 4 |  | Reserved |  |  |
| `flags` | 8 | hex | Flags |  |  |
| `window` | 16 | dec | Window |  |  |
| `checksum` | 16 | hex | Checksum |  |  |
| `urgent_ptr` | 16 | dec | Urgent Pointer |  |  |

## Header `roce_v2_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `ib_bth_0` | 64 | hex | IB BTH (bits 0-63) |  |  |
| `ib_bth_1` | 32 | hex | IB BTH (bits 64-95) |  |  |

## Header `vxlan_gpe_int_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `int_type` | 8 | hex | INT Type |  |  |
| `rsvd` | 8 |  | Reserved |  |  |
| `len` | 8 | dec | Length |  |  |
| `next_proto` | 8 | hex | Next Protocol |  |  |

## Header `int_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `ver` | 2 |  | Version |  |  |
| `rep` | 2 |  | Replication |  |  |
| `c` | 1 |  | Copy |  |  |
| `e` | 1 |  | Max Hop Count Exceeded |  |  |
| `rsvd1` | 5 |  | Reserved |  |  |
| `ins_cnt` | 5 | dec | Instruction Count |  |  |
| `max_hop_cnt` | 8 | dec | Max Hop Count |  |  |
| `total_hop_cnt` | 8 | dec | Total Hop Count |  |  |
| `instruction_mask_0003` | 4 |  | Instruction Mask 0-3 |  |  |
| `instruction_mask_0407` | 4 |  | Instruction Mask 4-7 |  |  |
| `instruction_mask_0811` | 4 |  | Instruction Mask 8-11 |  |  |
| `instruction_mask_1215` | 4 |  | Instruction Mask 12-15 |  |  |
| `rsvd2` | 16 |  | Reserved |  |  |

## Header `int_value`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `val` | 31 | hex | Value |  |  |

## Header `int_switch_id_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `switch_id` | 31 | dec | Switch ID |  |  |

## Header `int_ingress_port_id_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `ingress_port_id_1` | 15 | dec | Ingress Port ID (high) |  |  |
| `ingress_port_id_0` | 16 | dec | Ingress Port ID (low) |  |  |

## Header `int_hop_latency_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `hop_latency` | 31 | dec | Hop Latency |  |  |

## Header `int_q_occupancy_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `q_occupancy1` | 7 | dec | Queue Occupancy (high) |  |  |
| `q_occupancy0` | 24 | dec | Queue Occupancy (low) |  |  |

## Header `int_ingress_tstamp_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `ingress_tstamp` | 31 | dec | Ingress Timestamp |  |  |

## Header `int_egress_port_id_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `egress_port_id` | 31 | dec | Egress Port ID |  |  |

## Header `int_q_congestion_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `q_congestion` | 31 | dec | Queue Congestion |  |  |

## Header `int_egress_port_tx_utilization_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `bos` | 1 | dec | Bottom of Stack |  |  |
| `egress_port_tx_utilization` | 31 | dec | Egress Port TX Utilization |  |  |

## Header `sctp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `src_port` | 16 | dec | Source Port |  |  |
| `dst_port` | 16 | dec | Destination Port |  |  |
| `verif_tag` | 32 | hex | Verification Tag |  |  |
| `checksum` | 32 | hex | Checksum |  |  |

## Header `nvgre`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `tni` | 24 | dec | TNI |  |  |
| `flow_id` | 8 | hex | Flow ID |  |  |

## Header `erspan_header_t3`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `vlan` | 12 | dec | VLAN |  |  |
| `priority` | 6 |  | Priority |  |  |
| `span_id` | 10 | dec | Span ID |  |  |
| `timestamp` | 32 | dec | Timestamp |  |  |
| `sgt` | 16 | hex | SGT |  |  |
| `ft_d_other` | 16 | hex | Ft/D/Other |  |  |

## Header `inner_ethernet_rest`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `dst_addr_rest` | 44 | hex | Destination (low 44 bits) |  |  |
| `src_addr` | 48 | MAC | Source |  |  |
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 8939=ERSPAN type III, 25944=Transparent Ethernet Bridging, 33024=802.1Q, 34525=IPv6, 34825=Slow Protocols (source: LACP), 34887=MPLS (unicast), 35020=LLDP, 36864=internal fabric header (source: BF_FABRIC), 37120=QinQ (legacy 0x9100) |  |

## Header `vxlan`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `flags` | 8 | hex | Flags |  |  |
| `reserved` | 24 |  | Reserved |  |  |
| `vni` | 24 | dec | VNI |  |  |
| `reserved2` | 8 |  | Reserved |  |  |

## Header `vxlan_gpe`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `flags` | 8 | hex | Flags |  |  |
| `reserved` | 16 |  | Reserved |  |  |
| `next_proto` | 8 | hex | Next Protocol |  |  |
| `vni` | 24 | dec | VNI |  |  |
| `reserved2` | 8 |  | Reserved |  |  |

## Header `genv`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `ver` | 2 |  | Version |  |  |
| `opt_len` | 6 | dec | Option Length |  |  |
| `oam` | 1 |  | OAM |  |  |
| `critical` | 1 |  | Critical |  |  |
| `reserved` | 6 |  | Reserved |  |  |
| `proto_type` | 16 | hex | Protocol Type | 25944=Transparent Ethernet Bridging |  |
| `vni` | 24 | dec | VNI |  |  |
| `reserved2` | 8 |  | Reserved |  |  |

## Header `nsh`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `oam` | 1 |  | OAM |  |  |
| `context` | 1 |  | Context |  |  |
| `flags` | 6 |  | Flags |  |  |
| `reserved` | 8 |  | Reserved |  |  |
| `proto_type` | 16 | hex | Protocol Type | 2048=IPv4, 25944=Transparent Ethernet Bridging, 34525=IPv6 |  |
| `spath` | 24 | dec | Service Path |  |  |
| `sindex` | 8 | dec | Service Index |  |  |

## Header `nsh_context`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `network_platform` | 32 | hex | Network Platform |  |  |
| `network_shared` | 32 | hex | Network Shared |  |  |
| `service_platform` | 32 | hex | Service Platform |  |  |
| `service_shared` | 32 | hex | Service Shared |  |  |

## Header `lisp`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `flags` | 8 | hex | Flags |  |  |
| `nonce` | 24 | hex | Nonce |  |  |
| `lsbs_instance_id` | 32 | hex | LSBs/Instance ID |  |  |

## Header `inner_ipv6`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 4 |  | Version |  |  |
| `traffic_class` | 8 | hex | Traffic Class |  |  |
| `flow_label` | 20 | hex | Flow Label |  |  |
| `payload_len` | 16 | dec | Payload Length |  |  |
| `next_hdr` | 8 | dec | Next Header | 1=ICMP, 2=IGMP, 4=IPv4-in-IP, 6=TCP, 17=UDP, 41=IPv6-in-IP, 47=GRE, 58=ICMPv6, 88=EIGRP, 89=OSPF, 103=PIM, 112=VRRP |  |
| `hop_limit` | 8 | dec | Hop Limit |  |  |
| `src_addr` | `16` bytes |  |  |  |  |
| `dst_addr` | `16` bytes |  |  |  |  |

## Header `trill`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 2 |  | Version |  |  |
| `reserved` | 2 |  | Reserved |  |  |
| `multi_destination` | 1 |  | Multi-Destination |  |  |
| `opt_length` | 5 | dec | Option Length |  |  |
| `hop_count` | 6 | dec | Hop Count |  |  |
| `egress_rbridge` | 16 | dec | Egress RBridge |  |  |
| `ingress_rbridge` | 16 | dec | Ingress RBridge |  |  |

## Header `vntag`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `direction` | 1 |  | Direction |  |  |
| `pointer` | 1 |  | Pointer |  |  |
| `dest_vif` | 14 | dec | Destination VIF |  |  |
| `looped` | 1 |  | Looped |  |  |
| `reserved` | 1 |  | Reserved |  |  |
| `version` | 2 |  | Version |  |  |
| `src_vif` | 12 | dec | Source VIF |  |  |

## Header `bfd`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 3 |  | Version |  |  |
| `diag` | 5 |  | Diagnostic |  |  |
| `state` | 2 |  | State |  |  |
| `p` | 1 |  | P |  |  |
| `f` | 1 |  | F |  |  |
| `c` | 1 |  | C |  |  |
| `a` | 1 |  | A |  |  |
| `d` | 1 |  | D |  |  |
| `m` | 1 |  | M |  |  |
| `detect_mult` | 8 | dec | Detect Multiplier |  |  |
| `len` | 8 | dec | Length |  |  |
| `my_discriminator` | 32 | hex | My Discriminator |  |  |
| `your_discriminator` | 32 | hex | Your Discriminator |  |  |
| `desired_min_tx_interval` | 32 | dec | Desired Min TX Interval |  |  |
| `required_min_rx_interval` | 32 | dec | Required Min RX Interval |  |  |
| `required_min_echo_rx_interval` | 32 | dec | Required Min Echo RX Interval |  |  |

## Header `sflow_hdr`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `version` | 32 | dec | Version |  |  |
| `addr_type` | 32 | dec | Address Type |  |  |
| `ip_address` | 32 | hex | IP Address |  |  |
| `sub_agent_id` | 32 | dec | Sub-Agent ID |  |  |
| `seq_number` | 32 | dec | Sequence Number |  |  |
| `uptime` | 32 | dec | Uptime |  |  |
| `num_samples` | 32 | dec | Sample Count |  |  |

## Header `fabric_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `packet_type` | 3 | dec | Packet Type | 1=UNICAST, 2=MULTICAST, 3=MIRROR, 5=CPU |  |
| `header_version` | 2 |  | Header Version |  |  |
| `packet_version` | 2 |  | Packet Version |  |  |
| `pad1` | 1 |  | Pad |  |  |
| `fabric_color` | 3 |  | Fabric Color |  |  |
| `fabric_qos` | 5 |  | Fabric QoS |  |  |
| `dst_device` | 8 | dec | Destination Device |  |  |
| `dst_port_or_group` | 16 | dec | Destination Port/Group |  |  |

## Header `fabric_header_unicast`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `routed` | 1 |  | Routed |  |  |
| `outer_routed` | 1 |  | Outer Routed |  |  |
| `tunnel_terminate` | 1 |  | Tunnel Terminate |  |  |
| `ingress_tunnel_type` | 5 | dec | Ingress Tunnel Type |  |  |
| `nexthop_index` | 16 | dec | Nexthop Index |  |  |

## Header `fabric_header_multicast`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `routed` | 1 |  | Routed |  |  |
| `outer_routed` | 1 |  | Outer Routed |  |  |
| `tunnel_terminate` | 1 |  | Tunnel Terminate |  |  |
| `ingress_tunnel_type` | 5 | dec | Ingress Tunnel Type |  |  |
| `ingress_ifindex` | 16 | dec | Ingress IfIndex |  |  |
| `ingress_bd` | 16 | dec | Ingress BD |  |  |
| `mcast_grp` | 16 | dec | Multicast Group |  |  |

## Header `fabric_header_mirror`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `rewrite_index` | 16 | dec | Rewrite Index |  |  |
| `egress_port` | 10 | dec | Egress Port |  |  |
| `egress_queue` | 5 | dec | Egress Queue |  |  |
| `pad` | 1 |  | Pad |  |  |

## Header `fabric_header_cpu`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `egress_queue` | 5 | dec | Egress Queue |  |  |
| `tx_bypass` | 1 |  | TX Bypass |  |  |
| `reserved` | 2 |  | Reserved |  |  |
| `ingress_port` | 16 | dec | Ingress Port |  |  |
| `ingress_ifindex` | 16 | dec | Ingress IfIndex |  |  |
| `ingress_bd` | 16 | dec | Ingress BD |  |  |
| `reason_code` | 16 | hex | Reason Code | 4=sFlow sample |  |
| `mcast_grp` | 16 | dec | Multicast Group |  |  |

## Header `fabric_header_sflow`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `sflow_session_id` | 16 | dec | sFlow Session ID |  |  |
| `sflow_egress_ifindex` | 16 | dec | sFlow Egress IfIndex |  |  |

## Header `fabric_payload_header`

| Field | Bits | Format | Name | Labels | Notes |
|---|---|---|---|---|---|
| `ether_type` | 16 | hex | EtherType | 2048=IPv4, 2054=ARP, 8939=ERSPAN type III, 25944=Transparent Ethernet Bridging, 33024=802.1Q, 34525=IPv6, 34825=Slow Protocols (source: LACP), 34887=MPLS (unicast), 35020=LLDP, 36864=internal fabric header (source: BF_FABRIC), 37120=QinQ (legacy 0x9100) |  |

## Parse graph

Start state: `parse_ethernet`.

- **`parse_ethernet`** — extracts ethernet; selects on `ethernet.ether_type`:
  - ethernet.ether_type & 0xfe00 == 0x0 → `parse_llc_header`
  - ethernet.ether_type & 0xfa00 == 0x0 → `parse_llc_header`
  - ethernet.ether_type == 0x9000 → `parse_fabric_header`
  - ethernet.ether_type == 0x8100 → `parse_vlan`
  - ethernet.ether_type == 0x9100 → `parse_qinq`
  - ethernet.ether_type == 0x8847 → `parse_mpls`
  - ethernet.ether_type == 0x800 → `parse_ipv4`
  - ethernet.ether_type == 0x86dd → `parse_ipv6`
  - ethernet.ether_type == 0x806 → `parse_arp_rarp`
  - ethernet.ether_type == 0x88cc → `parse_set_prio_high`
  - ethernet.ether_type == 0x8809 → `parse_set_prio_high`
  - otherwise → **accept**
  > The entry state. The source's `start` is a bare `return
  > parse_ethernet`; `start` is a reserved eDSL attribute, so the
  > entry lands here directly.
- **`parse_llc_header`** — extracts llc_header; selects on `llc_header.dsap`, `llc_header.ssap`:
  - llc_header.dsap == 0xaa && llc_header.ssap == 0xaa → `parse_snap_header`
  - llc_header.dsap == 0xfe && llc_header.ssap == 0xfe → `parse_set_prio_med`
  - otherwise → **accept**
  > The source selects on (dsap, ssap) with the concatenated
  > literals 0xAAAA (SNAP) and 0xFEFE (CLNS/IS-IS), split here
  > per 8-bit key.
- **`parse_snap_header`** — extracts snap_header; selects on `snap_header.type_`:
  - snap_header.type_ == 0x8100 → `parse_vlan`
  - snap_header.type_ == 0x9100 → `parse_qinq`
  - snap_header.type_ == 0x8847 → `parse_mpls`
  - snap_header.type_ == 0x800 → `parse_ipv4`
  - snap_header.type_ == 0x86dd → `parse_ipv6`
  - snap_header.type_ == 0x806 → `parse_arp_rarp`
  - snap_header.type_ == 0x88cc → `parse_set_prio_high`
  - snap_header.type_ == 0x8809 → `parse_set_prio_high`
  - otherwise → **accept**
- **`parse_roce`** — extracts roce; then **accept**
  > Unreachable under shipped defaults (its EtherType arm is
  > compiled out); transcribed as the source keeps it.
- **`parse_fcoe`** — extracts fcoe; then **accept**
  > Unreachable under shipped defaults (see parse_roce).
- **`parse_vlan`** — extracts vlan_tag_; selects on `vlan_tag_.ether_type`:
  - vlan_tag_.ether_type == 0x8847 → `parse_mpls`
  - vlan_tag_.ether_type == 0x800 → `parse_ipv4`
  - vlan_tag_.ether_type == 0x86dd → `parse_ipv6`
  - vlan_tag_.ether_type == 0x806 → `parse_arp_rarp`
  - vlan_tag_.ether_type == 0x88cc → `parse_set_prio_high`
  - vlan_tag_.ether_type == 0x8809 → `parse_set_prio_high`
  - otherwise → **accept**
  > First 802.1Q tag: extracts vlan_tag_[0]. All three VLAN
  > states share one `vlan_tag_` instance (the gibb stack
  > pattern: a later extract overwrites).
- **`parse_qinq`** — extracts vlan_tag_; selects on `vlan_tag_.ether_type`:
  - vlan_tag_.ether_type == 0x8100 → `parse_qinq_vlan`
  - otherwise → **accept**
- **`parse_qinq_vlan`** — extracts vlan_tag_; selects on `vlan_tag_.ether_type`:
  - vlan_tag_.ether_type == 0x8847 → `parse_mpls`
  - vlan_tag_.ether_type == 0x800 → `parse_ipv4`
  - vlan_tag_.ether_type == 0x86dd → `parse_ipv6`
  - vlan_tag_.ether_type == 0x806 → `parse_arp_rarp`
  - vlan_tag_.ether_type == 0x88cc → `parse_set_prio_high`
  - vlan_tag_.ether_type == 0x8809 → `parse_set_prio_high`
  - otherwise → **accept**
  > The inner 0x8100 tag of a QinQ pair: vlan_tag_[1] in the
  > source, the shared `vlan_tag_` instance here.
- **`parse_mpls`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls_2`
  - mpls.bos == 0x1 → `parse_mpls_bos`
  - otherwise → **accept**
  > First label of the source's `mpls[3]` stack. The source is
  > one self-looping state extracting `mpls[next]`; the 3-entry
  > stack bound is transcribed by unrolling (parse_mpls_2 and
  > parse_mpls_3 are unroll-invented names).
- **`parse_mpls_2`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → `parse_mpls_3`
  - mpls.bos == 0x1 → `parse_mpls_bos`
  - otherwise → **accept**
- **`parse_mpls_3`** — extracts mpls; selects on `mpls.bos`:
  - mpls.bos == 0x0 → **reject** (*a fourth MPLS label overflows the source's mpls[3] stack*)
  - mpls.bos == 0x1 → `parse_mpls_bos`
  - otherwise → **accept**
  > Third label: the stack is full. A fourth label (bos=0)
  > would overflow `mpls[3]` — a P4_14 parse exception,
  > transcribed as an explicit reject.
- **`parse_mpls_bos`** — extracts ip_version_nibble; selects on `ip_version_nibble.v`:
  - ip_version_nibble.v == 0x4 → `parse_mpls_inner_ipv4`
  - ip_version_nibble.v == 0x6 → `parse_mpls_inner_ipv6`
  - otherwise → `parse_eompls`
  > Bottom of stack: the source peeks `current(0, 4)`;
  > transcribed as a real 4-bit nibble extract whose
  > continuations are defined minus their leading nibble.
- **`parse_mpls_inner_ipv4`** — extracts inner_ipv4_rest; selects on `inner_ipv4_rest.frag_offset`, `inner_ipv4_rest.ihl`, `inner_ipv4_rest.protocol`:
  - inner_ipv4_rest.frag_offset == 0x0 && inner_ipv4_rest.ihl == 0x5 && inner_ipv4_rest.protocol == 0x1 → `parse_inner_icmp`
  - inner_ipv4_rest.frag_offset == 0x0 && inner_ipv4_rest.ihl == 0x5 && inner_ipv4_rest.protocol == 0x6 → `parse_inner_tcp`
  - inner_ipv4_rest.frag_offset == 0x0 && inner_ipv4_rest.ihl == 0x5 && inner_ipv4_rest.protocol == 0x11 → `parse_inner_udp`
  - otherwise → **accept**
  > Metadata-only in the source (tunnel type 9 = MPLS L3VPN),
  > then parse_inner_ipv4; here it extracts the nibble-less
  > `InnerIpv4Rest` and carries parse_inner_ipv4's dispatch.
- **`parse_mpls_inner_ipv6`** — extracts inner_ipv6_rest; selects on `inner_ipv6_rest.next_hdr`:
  - inner_ipv6_rest.next_hdr == 0x3a → `parse_inner_icmp`
  - inner_ipv6_rest.next_hdr == 0x6 → `parse_inner_tcp`
  - inner_ipv6_rest.next_hdr == 0x11 → `parse_inner_udp`
  - otherwise → **accept**
- **`parse_vpls`**; then **accept**
  > Unreachable, extract-less `return ingress` in the source.
- **`parse_pw`**; then **accept**
  > Unreachable, extract-less `return ingress` in the source.
- **`parse_ipv4`** — extracts ipv4; selects on `ipv4.frag_offset`, `ipv4.ihl`, `ipv4.protocol`:
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x5 && ipv4.protocol == 0x1 → `parse_icmp`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x5 && ipv4.protocol == 0x6 → `parse_tcp`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x5 && ipv4.protocol == 0x11 → `parse_udp`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x5 && ipv4.protocol == 0x2f → `parse_gre`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x5 && ipv4.protocol == 0x4 → `parse_ipv4_in_ip`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x5 && ipv4.protocol == 0x29 → `parse_ipv6_in_ip`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x0 && ipv4.protocol == 0x2 → `parse_set_prio_med`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x0 && ipv4.protocol == 0x58 → `parse_set_prio_med`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x0 && ipv4.protocol == 0x59 → `parse_set_prio_med`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x0 && ipv4.protocol == 0x67 → `parse_set_prio_med`
  - ipv4.frag_offset == 0x0 && ipv4.ihl == 0x0 && ipv4.protocol == 0x70 → `parse_set_prio_med`
  - otherwise → **accept**
  > The source keys on (fragOffset, ihl, protocol) with
  > concatenated literals (0x501 = frag 0 ++ ihl 5 ++ proto 1),
  > split here per key width. The bare literals 2/88/89/103/112
  > decompose to ihl=0 — the routing-protocol arms match only
  > ihl==0 packets (a known switch.p4 quirk, kept). IPv4 is a
  > fixed 20-byte extract: options are never consumed.
- **`parse_ipv4_in_ip`**; then `parse_inner_ipv4`
  > Metadata-only in the source (tunnel type 3 = IP-in-IP).
- **`parse_ipv6_in_ip`**; then `parse_inner_ipv6`
- **`parse_udp_v6`** — extracts udp; selects on `udp.dst_port`:
  - udp.dst_port == 0x43 → `parse_set_prio_med`
  - udp.dst_port == 0x44 → `parse_set_prio_med`
  - udp.dst_port == 0x222 → `parse_set_prio_med`
  - udp.dst_port == 0x223 → `parse_set_prio_med`
  - udp.dst_port == 0x208 → `parse_set_prio_med`
  - udp.dst_port == 0x209 → `parse_set_prio_med`
  - udp.dst_port == 0x7c1 → `parse_set_prio_med`
  - otherwise → **accept**
  > Unreachable under shipped defaults (parse_ipv6 dispatches
  > UDP to parse_udp); extracts into the shared `udp` instance,
  > as the source does.
- **`parse_gre_v6`** — extracts gre; selects on `gre.c`, `gre.r`, `gre.k`, `gre.s`, `gre.strict`, `gre.recurse`, `gre.flags`, `gre.ver`, `gre.proto`:
  - gre.c == 0x0 && gre.r == 0x0 && gre.k == 0x0 && gre.s == 0x0 && gre.strict == 0x0 && gre.recurse == 0x0 && gre.flags == 0x0 && gre.ver == 0x0 && gre.proto == 0x800 → `parse_gre_ipv4`
  - otherwise → **accept**
  > Unreachable twin of parse_gre with a single IPv4 arm.
- **`parse_ipv6`** — extracts ipv6; selects on `ipv6.next_hdr`:
  - ipv6.next_hdr == 0x3a → `parse_icmp`
  - ipv6.next_hdr == 0x6 → `parse_tcp`
  - ipv6.next_hdr == 0x4 → `parse_ipv4_in_ip`
  - ipv6.next_hdr == 0x11 → `parse_udp`
  - ipv6.next_hdr == 0x2f → `parse_gre`
  - ipv6.next_hdr == 0x29 → `parse_ipv6_in_ip`
  - ipv6.next_hdr == 0x58 → `parse_set_prio_med`
  - ipv6.next_hdr == 0x59 → `parse_set_prio_med`
  - ipv6.next_hdr == 0x67 → `parse_set_prio_med`
  - ipv6.next_hdr == 0x70 → `parse_set_prio_med`
  - otherwise → **accept**
- **`parse_icmp`** — extracts icmp; selects on `icmp.type_code`:
  - icmp.type_code & 0xfe00 == 0x8200 → `parse_set_prio_med`
  - icmp.type_code & 0xfc00 == 0x8400 → `parse_set_prio_med`
  - icmp.type_code & 0xff00 == 0x8800 → `parse_set_prio_med`
  - otherwise → **accept**
  > Shared by IPv4 (proto 1) and IPv6 (proto 58). The masked
  > typeCode arms raise priority for ICMPv6 MLD and neighbor
  > discovery.
- **`parse_tcp`** — extracts tcp; selects on `tcp.dst_port`:
  - tcp.dst_port == 0xb3 → `parse_set_prio_med`
  - tcp.dst_port == 0x27f → `parse_set_prio_med`
  - otherwise → **accept**
  > A fixed 20-byte extract: dataOffset is read but no options
  > are consumed (source behavior).
- **`parse_roce_v2`** — extracts roce_v2; then **accept**
  > Unreachable under shipped defaults (no 4791 arm in
  > parse_udp).
- **`parse_udp`** — extracts udp; selects on `udp.dst_port`:
  - udp.dst_port == 0x12b5 → `parse_vxlan`
  - udp.dst_port == 0x17c1 → `parse_geneve`
  - udp.dst_port == 0x12b6 → `parse_vxlan_gpe`
  - udp.dst_port == 0x43 → `parse_set_prio_med`
  - udp.dst_port == 0x44 → `parse_set_prio_med`
  - udp.dst_port == 0x222 → `parse_set_prio_med`
  - udp.dst_port == 0x223 → `parse_set_prio_med`
  - udp.dst_port == 0x208 → `parse_set_prio_med`
  - udp.dst_port == 0x209 → `parse_set_prio_med`
  - udp.dst_port == 0x7c1 → `parse_set_prio_med`
  - udp.dst_port == 0x18c7 → `parse_sflow`
  - otherwise → **accept**
- **`parse_gpe_int_header`** — extracts vxlan_gpe_int_header; then `parse_int_header`
- **`parse_int_header`** — extracts int_header; selects on `int_header.rsvd1`, `int_header.total_hop_cnt`:
  - int_header.rsvd1 == 0x0 && int_header.total_hop_cnt == 0x0 → **accept**
  - int_header.rsvd1 & 0xf == 0x0 && int_header.total_hop_cnt & 0x0 == 0x0 → `parse_int_val`
  - int_header.rsvd1 & 0x0 == 0x0 && int_header.total_hop_cnt & 0x0 == 0x0 → **accept**
  - otherwise → `parse_all_int_meta_value_heders`
  > The source keys on (rsvd1, total_hop_cnt): exact 0x000
  > accepts (no hops added yet), `0x000 mask 0xf00` (= rsvd1's
  > low four bits zero) enters the value stack, and a `0 mask 0`
  > catch-all accepts — deliberately shadowing the default, whose
  > target parse_all_int_meta_value_heders the source keeps as a
  > deparser-graph aid ("never transition to the following
  > state").
- **`parse_int_val`** — extracts int_val; selects on `int_val.bos`:
  - int_val.bos == 0x0 → `parse_int_val`
  - int_val.bos == 0x1 → `parse_inner_ethernet`
  - otherwise → **accept**
  > The source's `int_val[24]` stack loop, kept as one cyclic
  > state: `max_depth` (not the stack size) bounds it here, so
  > the 24-entry cap is a depth budget, not a hard count.
- **`parse_all_int_meta_value_heders`** — extracts int_switch_id_header, int_ingress_port_id_header, int_hop_latency_header, int_q_occupancy_header, int_ingress_tstamp_header, int_egress_port_id_header, int_q_congestion_header, int_egress_port_tx_utilization_header; then `parse_int_val`
  > Semantically unreachable (see parse_int_header); the state
  > name keeps the source's spelling, typo included.
- **`parse_sctp`** — extracts sctp; then **accept**
  > Unreachable under shipped defaults (no proto-132 arm).
- **`parse_gre`** — extracts gre; selects on `gre.c`, `gre.r`, `gre.k`, `gre.s`, `gre.strict`, `gre.recurse`, `gre.flags`, `gre.ver`, `gre.proto`:
  - gre.c == 0x0 && gre.r == 0x0 && gre.k == 0x1 && gre.s == 0x0 && gre.strict == 0x0 && gre.recurse == 0x0 && gre.flags == 0x0 && gre.ver == 0x0 && gre.proto == 0x6558 → `parse_nvgre`
  - gre.c == 0x0 && gre.r == 0x0 && gre.k == 0x0 && gre.s == 0x0 && gre.strict == 0x0 && gre.recurse == 0x0 && gre.flags == 0x0 && gre.ver == 0x0 && gre.proto == 0x800 → `parse_gre_ipv4`
  - gre.c == 0x0 && gre.r == 0x0 && gre.k == 0x0 && gre.s == 0x0 && gre.strict == 0x0 && gre.recurse == 0x0 && gre.flags == 0x0 && gre.ver == 0x0 && gre.proto == 0x86dd → `parse_gre_ipv6`
  - gre.c == 0x0 && gre.r == 0x0 && gre.k == 0x0 && gre.s == 0x0 && gre.strict == 0x0 && gre.recurse == 0x0 && gre.flags == 0x0 && gre.ver == 0x0 && gre.proto == 0x22eb → `parse_erspan_t3`
  - otherwise → **accept**
  > The source selects on all nine GRE fields concatenated
  > (32 bits); 0x20006558 splits to K=1 ++ proto 0x6558 (NVGRE),
  > the other arms are flagless proto values.
- **`parse_gre_ipv4`**; then `parse_inner_ipv4`
  > Metadata-only in the source (tunnel type 2 = GRE).
- **`parse_gre_ipv6`**; then `parse_inner_ipv6`
- **`parse_nvgre`** — extracts nvgre; then `parse_inner_ethernet`
- **`parse_erspan_t3`** — extracts erspan_t3_header; then `parse_inner_ethernet`
- **`parse_arp_rarp`**; then `parse_set_prio_med`
  > Extract-less in this configuration: ARP raises priority
  > and parsing ends (the ARP header extract is compiled out).
- **`parse_eompls`** — extracts inner_ethernet_rest; selects on `inner_ethernet_rest.ether_type`:
  - inner_ethernet_rest.ether_type == 0x800 → `parse_inner_ipv4`
  - inner_ethernet_rest.ether_type == 0x86dd → `parse_inner_ipv6`
  - otherwise → **accept**
  > The source's eompls extract is commented out; the state
  > jumps straight to parse_inner_ethernet. Since the transcribed
  > nibble is already consumed, this state extracts the
  > nibble-less `InnerEthernetRest` and carries
  > parse_inner_ethernet's dispatch.
- **`parse_vxlan`** — extracts vxlan; then `parse_inner_ethernet`
- **`parse_vxlan_gpe`** — extracts vxlan_gpe; selects on `vxlan_gpe.next_proto`:
  - vxlan_gpe.next_proto & 0xff == 0x5 → `parse_gpe_int_header`
  - otherwise → `parse_inner_ethernet`
  > next_proto 5 (INT shim) is written `0x05 mask 0xff` in the
  > source; kept as a masked arm.
- **`parse_geneve`** — extracts genv; selects on `genv.ver`, `genv.opt_len`, `genv.proto_type`:
  - genv.ver == 0x0 && genv.opt_len == 0x0 && genv.proto_type == 0x6558 → `parse_inner_ethernet`
  - otherwise → **reject** (*genv: only ver=0, optLen=0, protoType=TEB is parseable*)
  > The source keys on (ver, optLen, protoType) with the single
  > concatenated arm 0x6558 (= 0 ++ 0 ++ TEB) and NO default: any
  > other value is a P4_14 parse exception, transcribed as an
  > explicit reject.
- **`parse_nsh`** — extracts nsh, nsh_context; selects on `nsh.proto_type`:
  - nsh.proto_type == 0x800 → `parse_inner_ipv4`
  - nsh.proto_type == 0x86dd → `parse_inner_ipv6`
  - nsh.proto_type == 0x6558 → `parse_inner_ethernet`
  - otherwise → **accept**
  > Unreachable under shipped defaults; extracts the NSH base
  > and context headers back to back, as the source does.
- **`parse_lisp`** — extracts lisp, ip_version_nibble; selects on `ip_version_nibble.v`:
  - ip_version_nibble.v == 0x4 → `parse_mpls_inner_ipv4`
  - ip_version_nibble.v == 0x6 → `parse_mpls_inner_ipv6`
  - otherwise → **accept**
  > Unreachable under shipped defaults. The source's second
  > `current(0, 4)` site: here the nibble arms route through
  > parse_mpls_inner_ipv4/6, which own the nibble-less inner-IP
  > continuations both lookahead sites share.
- **`parse_inner_ipv4`** — extracts inner_ipv4; selects on `inner_ipv4.frag_offset`, `inner_ipv4.ihl`, `inner_ipv4.protocol`:
  - inner_ipv4.frag_offset == 0x0 && inner_ipv4.ihl == 0x5 && inner_ipv4.protocol == 0x1 → `parse_inner_icmp`
  - inner_ipv4.frag_offset == 0x0 && inner_ipv4.ihl == 0x5 && inner_ipv4.protocol == 0x6 → `parse_inner_tcp`
  - inner_ipv4.frag_offset == 0x0 && inner_ipv4.ihl == 0x5 && inner_ipv4.protocol == 0x11 → `parse_inner_udp`
  - otherwise → **accept**
- **`parse_inner_icmp`** — extracts inner_icmp; then **accept**
- **`parse_inner_tcp`** — extracts inner_tcp; then **accept**
- **`parse_inner_udp`** — extracts inner_udp; then **accept**
- **`parse_inner_sctp`** — extracts inner_sctp; then **accept**
  > Unreachable under shipped defaults (see parse_sctp).
- **`parse_inner_ipv6`** — extracts inner_ipv6; selects on `inner_ipv6.next_hdr`:
  - inner_ipv6.next_hdr == 0x3a → `parse_inner_icmp`
  - inner_ipv6.next_hdr == 0x6 → `parse_inner_tcp`
  - inner_ipv6.next_hdr == 0x11 → `parse_inner_udp`
  - otherwise → **accept**
- **`parse_inner_ethernet`** — extracts inner_ethernet; selects on `inner_ethernet.ether_type`:
  - inner_ethernet.ether_type == 0x800 → `parse_inner_ipv4`
  - inner_ethernet.ether_type == 0x86dd → `parse_inner_ipv6`
  - otherwise → **accept**
- **`parse_trill`** — extracts trill; then `parse_inner_ethernet`
  > Unreachable under shipped defaults.
- **`parse_vntag`** — extracts vntag; then `parse_inner_ethernet`
  > Unreachable under shipped defaults.
- **`parse_bfd`** — extracts bfd; then `parse_set_prio_max`
  > Unreachable under shipped defaults.
- **`parse_sflow`** — extracts sflow; then **accept**
- **`parse_fabric_header`** — extracts fabric_header; selects on `fabric_header.packet_type`:
  - fabric_header.packet_type == 0x1 → `parse_fabric_header_unicast`
  - fabric_header.packet_type == 0x2 → `parse_fabric_header_multicast`
  - fabric_header.packet_type == 0x3 → `parse_fabric_header_mirror`
  - fabric_header.packet_type == 0x5 → `parse_fabric_header_cpu`
  - otherwise → **accept**
- **`parse_fabric_header_unicast`** — extracts fabric_header_unicast; then `parse_fabric_payload_header`
- **`parse_fabric_header_multicast`** — extracts fabric_header_multicast; then `parse_fabric_payload_header`
- **`parse_fabric_header_mirror`** — extracts fabric_header_mirror; then `parse_fabric_payload_header`
- **`parse_fabric_header_cpu`** — extracts fabric_header_cpu; selects on `fabric_header_cpu.reason_code`:
  - fabric_header_cpu.reason_code == 0x4 → `parse_fabric_sflow_header`
  - otherwise → `parse_fabric_payload_header`
- **`parse_fabric_sflow_header`** — extracts fabric_header_sflow; then `parse_fabric_payload_header`
- **`parse_fabric_payload_header`** — extracts fabric_payload_header; selects on `fabric_payload_header.ether_type`:
  - fabric_payload_header.ether_type & 0xfe00 == 0x0 → `parse_llc_header`
  - fabric_payload_header.ether_type & 0xfa00 == 0x0 → `parse_llc_header`
  - fabric_payload_header.ether_type == 0x8100 → `parse_vlan`
  - fabric_payload_header.ether_type == 0x9100 → `parse_qinq`
  - fabric_payload_header.ether_type == 0x8847 → `parse_mpls`
  - fabric_payload_header.ether_type == 0x800 → `parse_ipv4`
  - fabric_payload_header.ether_type == 0x86dd → `parse_ipv6`
  - fabric_payload_header.ether_type == 0x806 → `parse_arp_rarp`
  - fabric_payload_header.ether_type == 0x88cc → `parse_set_prio_high`
  - fabric_payload_header.ether_type == 0x8809 → `parse_set_prio_high`
  - otherwise → **accept**
  > The fabric payload re-enters the L2 dispatch — the same
  > map as parse_ethernet minus the fabric arm (no nesting).
- **`parse_set_prio_med`**; then **accept**
  > `intrinsic_metadata.priority = 3` in the source, then
  > ingress: parsing ends.
- **`parse_set_prio_high`**; then **accept**
  > `intrinsic_metadata.priority = 5` in the source.
- **`parse_set_prio_max`**; then **accept**
  > `intrinsic_metadata.priority = 7` in the source.
