/* Generated by pakeles from `p4lang_switch_parser`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

static uint64_t pk_read_bits(const uint8_t *buf, uint64_t avail, uint64_t off, uint32_t n) {
  (void)avail;
  uint64_t v = 0;
  uint32_t i;
  for (i = 0; i < n; i++) {
    uint64_t pos = off + i;
    v = (v << 1) | (uint64_t)((buf[pos >> 3] >> (7 - (pos & 7))) & 1);
  }
  return v;
}

#define PK_S_PARSE_ETHERNET 0
#define PK_S_PARSE_LLC_HEADER 1
#define PK_S_PARSE_SNAP_HEADER 2
#define PK_S_PARSE_ROCE 3
#define PK_S_PARSE_FCOE 4
#define PK_S_PARSE_VLAN 5
#define PK_S_PARSE_QINQ 6
#define PK_S_PARSE_QINQ_VLAN 7
#define PK_S_PARSE_MPLS 8
#define PK_S_PARSE_MPLS_2 9
#define PK_S_PARSE_MPLS_3 10
#define PK_S_PARSE_MPLS_BOS 11
#define PK_S_PARSE_MPLS_INNER_IPV4 12
#define PK_S_PARSE_MPLS_INNER_IPV6 13
#define PK_S_PARSE_VPLS 14
#define PK_S_PARSE_PW 15
#define PK_S_PARSE_IPV4 16
#define PK_S_PARSE_IPV4_IN_IP 17
#define PK_S_PARSE_IPV6_IN_IP 18
#define PK_S_PARSE_UDP_V6 19
#define PK_S_PARSE_GRE_V6 20
#define PK_S_PARSE_IPV6 21
#define PK_S_PARSE_ICMP 22
#define PK_S_PARSE_TCP 23
#define PK_S_PARSE_ROCE_V2 24
#define PK_S_PARSE_UDP 25
#define PK_S_PARSE_GPE_INT_HEADER 26
#define PK_S_PARSE_INT_HEADER 27
#define PK_S_PARSE_INT_VAL 28
#define PK_S_PARSE_ALL_INT_META_VALUE_HEDERS 29
#define PK_S_PARSE_SCTP 30
#define PK_S_PARSE_GRE 31
#define PK_S_PARSE_GRE_IPV4 32
#define PK_S_PARSE_GRE_IPV6 33
#define PK_S_PARSE_NVGRE 34
#define PK_S_PARSE_ERSPAN_T3 35
#define PK_S_PARSE_ARP_RARP 36
#define PK_S_PARSE_EOMPLS 37
#define PK_S_PARSE_VXLAN 38
#define PK_S_PARSE_VXLAN_GPE 39
#define PK_S_PARSE_GENEVE 40
#define PK_S_PARSE_NSH 41
#define PK_S_PARSE_LISP 42
#define PK_S_PARSE_INNER_IPV4 43
#define PK_S_PARSE_INNER_ICMP 44
#define PK_S_PARSE_INNER_TCP 45
#define PK_S_PARSE_INNER_UDP 46
#define PK_S_PARSE_INNER_SCTP 47
#define PK_S_PARSE_INNER_IPV6 48
#define PK_S_PARSE_INNER_ETHERNET 49
#define PK_S_PARSE_TRILL 50
#define PK_S_PARSE_VNTAG 51
#define PK_S_PARSE_BFD 52
#define PK_S_PARSE_SFLOW 53
#define PK_S_PARSE_FABRIC_HEADER 54
#define PK_S_PARSE_FABRIC_HEADER_UNICAST 55
#define PK_S_PARSE_FABRIC_HEADER_MULTICAST 56
#define PK_S_PARSE_FABRIC_HEADER_MIRROR 57
#define PK_S_PARSE_FABRIC_HEADER_CPU 58
#define PK_S_PARSE_FABRIC_SFLOW_HEADER 59
#define PK_S_PARSE_FABRIC_PAYLOAD_HEADER 60
#define PK_S_PARSE_SET_PRIO_MED 61
#define PK_S_PARSE_SET_PRIO_HIGH 62
#define PK_S_PARSE_SET_PRIO_MAX 63

static int pk_p4lang_switch_parser_parse_core(const uint8_t *buf, uint64_t bit_len, pk_p4lang_switch_parser_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_ETHERNET;
  uint32_t depth;
  for (depth = 0; depth < 43u; depth++) {
    switch (state) {
    case PK_S_PARSE_ETHERNET: {
      out->ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.dst_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.src_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ethernet.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->ethernet.ether_type;
      if ((key0 & 65024ULL) == 0ULL) {
        state = PK_S_PARSE_LLC_HEADER;
        continue;
      } else if ((key0 & 64000ULL) == 0ULL) {
        state = PK_S_PARSE_LLC_HEADER;
        continue;
      } else if (key0 == 36864ULL) {
        state = PK_S_PARSE_FABRIC_HEADER;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN;
        continue;
      } else if (key0 == 37120ULL) {
        state = PK_S_PARSE_QINQ;
        continue;
      } else if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 35020ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else if (key0 == 34825ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_LLC_HEADER: {
      out->llc_header_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->llc_header.dsap = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->llc_header.ssap = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->llc_header.control_ = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->llc_header.dsap;
      uint64_t key1 = (uint64_t)out->llc_header.ssap;
      if (key0 == 170ULL && key1 == 170ULL) {
        state = PK_S_PARSE_SNAP_HEADER;
        continue;
      } else if (key0 == 254ULL && key1 == 254ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_SNAP_HEADER: {
      out->snap_header_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->snap_header.oui = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->snap_header.type_ = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->snap_header.type_;
      if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN;
        continue;
      } else if (key0 == 37120ULL) {
        state = PK_S_PARSE_QINQ;
        continue;
      } else if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 35020ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else if (key0 == 34825ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ROCE: {
      out->roce_present = 1;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce.ib_grh_0 = (uint64_t)pk_read_bits(buf, bit_len, off, 64);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce.ib_grh_1 = (uint64_t)pk_read_bits(buf, bit_len, off, 64);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce.ib_grh_2 = (uint64_t)pk_read_bits(buf, bit_len, off, 64);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce.ib_grh_3 = (uint64_t)pk_read_bits(buf, bit_len, off, 64);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce.ib_grh_4 = (uint64_t)pk_read_bits(buf, bit_len, off, 64);
      off += 64;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce.ib_bth_0 = (uint64_t)pk_read_bits(buf, bit_len, off, 64);
      off += 64;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce.ib_bth_1 = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_FCOE: {
      out->fcoe_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.type_ = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.sof = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.rsvd1 = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.ts_upper = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.ts_lower = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.size_ = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.eof = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fcoe.rsvd2 = (uint32_t)pk_read_bits(buf, bit_len, off, 24);
      off += 24;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_VLAN: {
      out->vlan_tag__present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.cfi = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.vid = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan_tag_.ether_type;
      if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 35020ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else if (key0 == 34825ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_QINQ: {
      out->vlan_tag__present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.cfi = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.vid = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan_tag_.ether_type;
      if (key0 == 33024ULL) {
        state = PK_S_PARSE_QINQ_VLAN;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_QINQ_VLAN: {
      out->vlan_tag__present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.pcp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.cfi = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.vid = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vlan_tag_.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->vlan_tag_.ether_type;
      if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 35020ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else if (key0 == 34825ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.exp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_MPLS_2;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_BOS;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS_2: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.exp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_MPLS_3;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_BOS;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS_3: {
      out->mpls_present = 1;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.exp = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->mpls.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->mpls.bos;
      if (key0 == 0ULL) {
        out->outcome = 1;
        out->reason = PK_R_A_FOURTH_MPLS_LABEL_OVERFLOWS_THE_SOURCE_S_MPLS_3__STACK;
        out->consumed_bits = off;
        return 1;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_MPLS_BOS;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS_BOS: {
      out->ip_version_nibble_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ip_version_nibble.v = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      uint64_t key0 = (uint64_t)out->ip_version_nibble.v;
      if (key0 == 4ULL) {
        state = PK_S_PARSE_MPLS_INNER_IPV4;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_MPLS_INNER_IPV6;
        continue;
      } else {
        state = PK_S_PARSE_EOMPLS;
        continue;
      }
    }
    case PK_S_PARSE_MPLS_INNER_IPV4: {
      out->inner_ipv4_rest_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.ihl = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.diffserv = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.total_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.identification = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.frag_offset = (uint16_t)pk_read_bits(buf, bit_len, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.protocol = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.src_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4_rest.dst_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      uint64_t key0 = (uint64_t)out->inner_ipv4_rest.frag_offset;
      uint64_t key1 = (uint64_t)out->inner_ipv4_rest.ihl;
      uint64_t key2 = (uint64_t)out->inner_ipv4_rest.protocol;
      if (key0 == 0ULL && key1 == 5ULL && key2 == 1ULL) {
        state = PK_S_PARSE_INNER_ICMP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 17ULL) {
        state = PK_S_PARSE_INNER_UDP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_MPLS_INNER_IPV6: {
      out->inner_ipv6_rest_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6_rest.traffic_class = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6_rest.flow_label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6_rest.payload_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6_rest.next_hdr = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6_rest.hop_limit = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->inner_ipv6_rest.src_addr_bit_off = off;
        out->inner_ipv6_rest.src_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->inner_ipv6_rest.dst_addr_bit_off = off;
        out->inner_ipv6_rest.dst_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->inner_ipv6_rest.next_hdr;
      if (key0 == 58ULL) {
        state = PK_S_PARSE_INNER_ICMP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_INNER_UDP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VPLS: {
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_PW: {
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_IPV4: {
      out->ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ihl = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.diffserv = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.total_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.identification = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.frag_offset = (uint16_t)pk_read_bits(buf, bit_len, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.protocol = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.src_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv4.dst_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      uint64_t key0 = (uint64_t)out->ipv4.frag_offset;
      uint64_t key1 = (uint64_t)out->ipv4.ihl;
      uint64_t key2 = (uint64_t)out->ipv4.protocol;
      if (key0 == 0ULL && key1 == 5ULL && key2 == 1ULL) {
        state = PK_S_PARSE_ICMP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 17ULL) {
        state = PK_S_PARSE_UDP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 4ULL) {
        state = PK_S_PARSE_IPV4_IN_IP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 41ULL) {
        state = PK_S_PARSE_IPV6_IN_IP;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 2ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 88ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 89ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 103ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 112ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IPV4_IN_IP: {
      state = PK_S_PARSE_INNER_IPV4;
      continue;
    }
    case PK_S_PARSE_IPV6_IN_IP: {
      state = PK_S_PARSE_INNER_IPV6;
      continue;
    }
    case PK_S_PARSE_UDP_V6: {
      out->udp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.src_port = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.dst_port = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.length_ = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.checksum = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->udp.dst_port;
      if (key0 == 67ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 68ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 546ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 547ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 520ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 521ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 1985ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_GRE_V6: {
      out->gre_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.c = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.r = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.k = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.s = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.strict = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.recurse = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.ver = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.proto = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      uint64_t key0 = (uint64_t)out->gre.c;
      uint64_t key1 = (uint64_t)out->gre.r;
      uint64_t key2 = (uint64_t)out->gre.k;
      uint64_t key3 = (uint64_t)out->gre.s;
      uint64_t key4 = (uint64_t)out->gre.strict;
      uint64_t key5 = (uint64_t)out->gre.recurse;
      uint64_t key6 = (uint64_t)out->gre.flags;
      uint64_t key7 = (uint64_t)out->gre.ver;
      uint64_t key8 = (uint64_t)out->gre.proto;
      if (key0 == 0ULL && key1 == 0ULL && key2 == 0ULL && key3 == 0ULL && key4 == 0ULL && key5 == 0ULL && key6 == 0ULL && key7 == 0ULL && key8 == 2048ULL) {
        state = PK_S_PARSE_GRE_IPV4;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_IPV6: {
      out->ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.traffic_class = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.flow_label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.payload_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.next_hdr = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ipv6.hop_limit = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.src_addr_bit_off = off;
        out->ipv6.src_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->ipv6.dst_addr_bit_off = off;
        out->ipv6.dst_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->ipv6.next_hdr;
      if (key0 == 58ULL) {
        state = PK_S_PARSE_ICMP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_TCP;
        continue;
      } else if (key0 == 4ULL) {
        state = PK_S_PARSE_IPV4_IN_IP;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_UDP;
        continue;
      } else if (key0 == 47ULL) {
        state = PK_S_PARSE_GRE;
        continue;
      } else if (key0 == 41ULL) {
        state = PK_S_PARSE_IPV6_IN_IP;
        continue;
      } else if (key0 == 88ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 89ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 103ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 112ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ICMP: {
      out->icmp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.type_code = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->icmp.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->icmp.type_code;
      if ((key0 & 65024ULL) == 33280ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if ((key0 & 64512ULL) == 33792ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if ((key0 & 65280ULL) == 34816ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_TCP: {
      out->tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.seq_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.ack_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.data_offset = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.res = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.flags = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.window = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->tcp.urgent_ptr = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->tcp.dst_port;
      if (key0 == 179ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 639ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ROCE_V2: {
      out->roce_v2_present = 1;
      if (off + 64 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce_v2.ib_bth_0 = (uint64_t)pk_read_bits(buf, bit_len, off, 64);
      off += 64;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->roce_v2.ib_bth_1 = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_UDP: {
      out->udp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.length_ = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->udp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->udp.dst_port;
      if (key0 == 4789ULL) {
        state = PK_S_PARSE_VXLAN;
        continue;
      } else if (key0 == 6081ULL) {
        state = PK_S_PARSE_GENEVE;
        continue;
      } else if (key0 == 4790ULL) {
        state = PK_S_PARSE_VXLAN_GPE;
        continue;
      } else if (key0 == 67ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 68ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 546ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 547ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 520ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 521ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 1985ULL) {
        state = PK_S_PARSE_SET_PRIO_MED;
        continue;
      } else if (key0 == 6343ULL) {
        state = PK_S_PARSE_SFLOW;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_GPE_INT_HEADER: {
      out->vxlan_gpe_int_header_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe_int_header.int_type = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe_int_header.rsvd = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe_int_header.len = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe_int_header.next_proto = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      state = PK_S_PARSE_INT_HEADER;
      continue;
    }
    case PK_S_PARSE_INT_HEADER: {
      out->int_header_present = 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.ver = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.rep = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.c = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.e = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.rsvd1 = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.ins_cnt = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.max_hop_cnt = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.total_hop_cnt = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.instruction_mask_0003 = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.instruction_mask_0407 = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.instruction_mask_0811 = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.instruction_mask_1215 = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_header.rsvd2 = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->int_header.rsvd1;
      uint64_t key1 = (uint64_t)out->int_header.total_hop_cnt;
      if (key0 == 0ULL && key1 == 0ULL) {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      } else if ((key0 & 15ULL) == 0ULL && (key1 & 0ULL) == 0ULL) {
        state = PK_S_PARSE_INT_VAL;
        continue;
      } else if ((key0 & 0ULL) == 0ULL && (key1 & 0ULL) == 0ULL) {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      } else {
        state = PK_S_PARSE_ALL_INT_META_VALUE_HEDERS;
        continue;
      }
    }
    case PK_S_PARSE_INT_VAL: {
      out->int_val_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_val.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 31 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_val.val = (uint32_t)pk_read_bits(buf, bit_len, off, 31);
      off += 31;
      uint64_t key0 = (uint64_t)out->int_val.bos;
      if (key0 == 0ULL) {
        state = PK_S_PARSE_INT_VAL;
        continue;
      } else if (key0 == 1ULL) {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_ALL_INT_META_VALUE_HEDERS: {
      out->int_switch_id_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_switch_id_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 31 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_switch_id_header.switch_id = (uint32_t)pk_read_bits(buf, bit_len, off, 31);
      off += 31;
      out->int_ingress_port_id_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_ingress_port_id_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 15 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_ingress_port_id_header.ingress_port_id_1 = (uint16_t)pk_read_bits(buf, bit_len, off, 15);
      off += 15;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_ingress_port_id_header.ingress_port_id_0 = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->int_hop_latency_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_hop_latency_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 31 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_hop_latency_header.hop_latency = (uint32_t)pk_read_bits(buf, bit_len, off, 31);
      off += 31;
      out->int_q_occupancy_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_q_occupancy_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 7 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_q_occupancy_header.q_occupancy1 = (uint8_t)pk_read_bits(buf, bit_len, off, 7);
      off += 7;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_q_occupancy_header.q_occupancy0 = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      out->int_ingress_tstamp_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_ingress_tstamp_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 31 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_ingress_tstamp_header.ingress_tstamp = (uint32_t)pk_read_bits(buf, bit_len, off, 31);
      off += 31;
      out->int_egress_port_id_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_egress_port_id_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 31 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_egress_port_id_header.egress_port_id = (uint32_t)pk_read_bits(buf, bit_len, off, 31);
      off += 31;
      out->int_q_congestion_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_q_congestion_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 31 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_q_congestion_header.q_congestion = (uint32_t)pk_read_bits(buf, bit_len, off, 31);
      off += 31;
      out->int_egress_port_tx_utilization_header_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_egress_port_tx_utilization_header.bos = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 31 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->int_egress_port_tx_utilization_header.egress_port_tx_utilization = (uint32_t)pk_read_bits(buf, bit_len, off, 31);
      off += 31;
      state = PK_S_PARSE_INT_VAL;
      continue;
    }
    case PK_S_PARSE_SCTP: {
      out->sctp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.src_port = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.dst_port = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.verif_tag = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sctp.checksum = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_GRE: {
      out->gre_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.c = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.r = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.k = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.s = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.strict = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.recurse = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.ver = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->gre.proto = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->gre.c;
      uint64_t key1 = (uint64_t)out->gre.r;
      uint64_t key2 = (uint64_t)out->gre.k;
      uint64_t key3 = (uint64_t)out->gre.s;
      uint64_t key4 = (uint64_t)out->gre.strict;
      uint64_t key5 = (uint64_t)out->gre.recurse;
      uint64_t key6 = (uint64_t)out->gre.flags;
      uint64_t key7 = (uint64_t)out->gre.ver;
      uint64_t key8 = (uint64_t)out->gre.proto;
      if (key0 == 0ULL && key1 == 0ULL && key2 == 1ULL && key3 == 0ULL && key4 == 0ULL && key5 == 0ULL && key6 == 0ULL && key7 == 0ULL && key8 == 25944ULL) {
        state = PK_S_PARSE_NVGRE;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 0ULL && key3 == 0ULL && key4 == 0ULL && key5 == 0ULL && key6 == 0ULL && key7 == 0ULL && key8 == 2048ULL) {
        state = PK_S_PARSE_GRE_IPV4;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 0ULL && key3 == 0ULL && key4 == 0ULL && key5 == 0ULL && key6 == 0ULL && key7 == 0ULL && key8 == 34525ULL) {
        state = PK_S_PARSE_GRE_IPV6;
        continue;
      } else if (key0 == 0ULL && key1 == 0ULL && key2 == 0ULL && key3 == 0ULL && key4 == 0ULL && key5 == 0ULL && key6 == 0ULL && key7 == 0ULL && key8 == 8939ULL) {
        state = PK_S_PARSE_ERSPAN_T3;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_GRE_IPV4: {
      state = PK_S_PARSE_INNER_IPV4;
      continue;
    }
    case PK_S_PARSE_GRE_IPV6: {
      state = PK_S_PARSE_INNER_IPV6;
      continue;
    }
    case PK_S_PARSE_NVGRE: {
      out->nvgre_present = 1;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nvgre.tni = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nvgre.flow_id = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      state = PK_S_PARSE_INNER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_ERSPAN_T3: {
      out->erspan_t3_header_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->erspan_t3_header.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->erspan_t3_header.vlan = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->erspan_t3_header.priority = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 10 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->erspan_t3_header.span_id = (uint16_t)pk_read_bits(buf, bit_len, off, 10);
      off += 10;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->erspan_t3_header.timestamp = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->erspan_t3_header.sgt = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->erspan_t3_header.ft_d_other = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      state = PK_S_PARSE_INNER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_ARP_RARP: {
      state = PK_S_PARSE_SET_PRIO_MED;
      continue;
    }
    case PK_S_PARSE_EOMPLS: {
      out->inner_ethernet_rest_present = 1;
      if (off + 44 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ethernet_rest.dst_addr_rest = (uint64_t)pk_read_bits(buf, bit_len, off, 44);
      off += 44;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ethernet_rest.src_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ethernet_rest.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->inner_ethernet_rest.ether_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_VXLAN: {
      out->vxlan_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.flags = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.reserved = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.vni = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan.reserved2 = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      state = PK_S_PARSE_INNER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_VXLAN_GPE: {
      out->vxlan_gpe_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe.flags = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe.reserved = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe.next_proto = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe.vni = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vxlan_gpe.reserved2 = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->vxlan_gpe.next_proto;
      if ((key0 & 255ULL) == 5ULL) {
        state = PK_S_PARSE_GPE_INT_HEADER;
        continue;
      } else {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      }
    }
    case PK_S_PARSE_GENEVE: {
      out->genv_present = 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.ver = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.opt_len = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.oam = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.critical = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.proto_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.vni = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 16) | ((uint64_t)buf[(off >> 3) + 1] << 8) | (uint64_t)buf[(off >> 3) + 2]);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->genv.reserved2 = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      uint64_t key0 = (uint64_t)out->genv.ver;
      uint64_t key1 = (uint64_t)out->genv.opt_len;
      uint64_t key2 = (uint64_t)out->genv.proto_type;
      if (key0 == 0ULL && key1 == 0ULL && key2 == 25944ULL) {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      } else {
        out->outcome = 1;
        out->reason = PK_R_GENV__ONLY_VER_0__OPTLEN_0__PROTOTYPE_TEB_IS_PARSEABLE;
        out->consumed_bits = off;
        return 1;
      }
    }
    case PK_S_PARSE_NSH: {
      out->nsh_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh.oam = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh.context = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh.proto_type = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh.spath = (uint32_t)pk_read_bits(buf, bit_len, off, 24);
      off += 24;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh.sindex = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      out->nsh_context_present = 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh_context.network_platform = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh_context.network_shared = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh_context.service_platform = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->nsh_context.service_shared = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      uint64_t key0 = (uint64_t)out->nsh.proto_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else if (key0 == 25944ULL) {
        state = PK_S_PARSE_INNER_ETHERNET;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_LISP: {
      out->lisp_present = 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->lisp.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 24 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->lisp.nonce = (uint32_t)pk_read_bits(buf, bit_len, off, 24);
      off += 24;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->lisp.lsbs_instance_id = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      out->ip_version_nibble_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->ip_version_nibble.v = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      uint64_t key0 = (uint64_t)out->ip_version_nibble.v;
      if (key0 == 4ULL) {
        state = PK_S_PARSE_MPLS_INNER_IPV4;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_MPLS_INNER_IPV6;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_IPV4: {
      out->inner_ipv4_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.ihl = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.diffserv = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.total_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.identification = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.flags = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 13 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.frag_offset = (uint16_t)pk_read_bits(buf, bit_len, off, 13);
      off += 13;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.ttl = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.protocol = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.src_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv4.dst_addr = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      uint64_t key0 = (uint64_t)out->inner_ipv4.frag_offset;
      uint64_t key1 = (uint64_t)out->inner_ipv4.ihl;
      uint64_t key2 = (uint64_t)out->inner_ipv4.protocol;
      if (key0 == 0ULL && key1 == 5ULL && key2 == 1ULL) {
        state = PK_S_PARSE_INNER_ICMP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else if (key0 == 0ULL && key1 == 5ULL && key2 == 17ULL) {
        state = PK_S_PARSE_INNER_UDP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_ICMP: {
      out->inner_icmp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_icmp.type_code = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_icmp.hdr_checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_INNER_TCP: {
      out->inner_tcp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.seq_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.ack_no = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.data_offset = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.res = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.flags = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.window = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_tcp.urgent_ptr = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_INNER_UDP: {
      out->inner_udp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_udp.src_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_udp.dst_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_udp.length_ = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_udp.checksum = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_INNER_SCTP: {
      out->inner_sctp_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_sctp.src_port = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_sctp.dst_port = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_sctp.verif_tag = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_sctp.checksum = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_INNER_IPV6: {
      out->inner_ipv6_present = 1;
      if (off + 4 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6.version = (uint8_t)pk_read_bits(buf, bit_len, off, 4);
      off += 4;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6.traffic_class = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 20 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6.flow_label = (uint32_t)pk_read_bits(buf, bit_len, off, 20);
      off += 20;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6.payload_len = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6.next_hdr = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ipv6.hop_limit = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->inner_ipv6.src_addr_bit_off = off;
        out->inner_ipv6.src_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      {
        uint64_t vlen = 16ULL;
        if (vlen > (bit_len - off) / 8) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->inner_ipv6.dst_addr_bit_off = off;
        out->inner_ipv6.dst_addr_bit_len = vlen * 8;
        off += vlen * 8;
      }
      uint64_t key0 = (uint64_t)out->inner_ipv6.next_hdr;
      if (key0 == 58ULL) {
        state = PK_S_PARSE_INNER_ICMP;
        continue;
      } else if (key0 == 6ULL) {
        state = PK_S_PARSE_INNER_TCP;
        continue;
      } else if (key0 == 17ULL) {
        state = PK_S_PARSE_INNER_UDP;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_INNER_ETHERNET: {
      out->inner_ethernet_present = 1;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ethernet.dst_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 48 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ethernet.src_addr = (uint64_t)(((uint64_t)buf[(off >> 3) + 0] << 40) | ((uint64_t)buf[(off >> 3) + 1] << 32) | ((uint64_t)buf[(off >> 3) + 2] << 24) | ((uint64_t)buf[(off >> 3) + 3] << 16) | ((uint64_t)buf[(off >> 3) + 4] << 8) | (uint64_t)buf[(off >> 3) + 5]);
      off += 48;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->inner_ethernet.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->inner_ethernet.ether_type;
      if (key0 == 2048ULL) {
        state = PK_S_PARSE_INNER_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_INNER_IPV6;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_TRILL: {
      out->trill_present = 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->trill.version = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->trill.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->trill.multi_destination = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->trill.opt_length = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 6 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->trill.hop_count = (uint8_t)pk_read_bits(buf, bit_len, off, 6);
      off += 6;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->trill.egress_rbridge = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->trill.ingress_rbridge = (uint16_t)pk_read_bits(buf, bit_len, off, 16);
      off += 16;
      state = PK_S_PARSE_INNER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_VNTAG: {
      out->vntag_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vntag.direction = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vntag.pointer = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 14 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vntag.dest_vif = (uint16_t)pk_read_bits(buf, bit_len, off, 14);
      off += 14;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vntag.looped = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vntag.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vntag.version = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 12 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->vntag.src_vif = (uint16_t)pk_read_bits(buf, bit_len, off, 12);
      off += 12;
      state = PK_S_PARSE_INNER_ETHERNET;
      continue;
    }
    case PK_S_PARSE_BFD: {
      out->bfd_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.version = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.diag = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.state = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.p = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.f = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.c = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.a = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.d = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.m = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.detect_mult = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.len = (uint8_t)pk_read_bits(buf, bit_len, off, 8);
      off += 8;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.my_discriminator = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.your_discriminator = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.desired_min_tx_interval = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.required_min_rx_interval = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->bfd.required_min_echo_rx_interval = (uint32_t)pk_read_bits(buf, bit_len, off, 32);
      off += 32;
      state = PK_S_PARSE_SET_PRIO_MAX;
      continue;
    }
    case PK_S_PARSE_SFLOW: {
      out->sflow_present = 1;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sflow.version = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sflow.addr_type = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sflow.ip_address = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sflow.sub_agent_id = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sflow.seq_number = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sflow.uptime = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      if (off + 32 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->sflow.num_samples = (uint32_t)(((uint64_t)buf[(off >> 3) + 0] << 24) | ((uint64_t)buf[(off >> 3) + 1] << 16) | ((uint64_t)buf[(off >> 3) + 2] << 8) | (uint64_t)buf[(off >> 3) + 3]);
      off += 32;
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_FABRIC_HEADER: {
      out->fabric_header_present = 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.packet_type = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.header_version = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.packet_version = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.pad1 = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 3 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.fabric_color = (uint8_t)pk_read_bits(buf, bit_len, off, 3);
      off += 3;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.fabric_qos = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.dst_device = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header.dst_port_or_group = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->fabric_header.packet_type;
      if (key0 == 1ULL) {
        state = PK_S_PARSE_FABRIC_HEADER_UNICAST;
        continue;
      } else if (key0 == 2ULL) {
        state = PK_S_PARSE_FABRIC_HEADER_MULTICAST;
        continue;
      } else if (key0 == 3ULL) {
        state = PK_S_PARSE_FABRIC_HEADER_MIRROR;
        continue;
      } else if (key0 == 5ULL) {
        state = PK_S_PARSE_FABRIC_HEADER_CPU;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_FABRIC_HEADER_UNICAST: {
      out->fabric_header_unicast_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_unicast.routed = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_unicast.outer_routed = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_unicast.tunnel_terminate = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_unicast.ingress_tunnel_type = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_unicast.nexthop_index = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      state = PK_S_PARSE_FABRIC_PAYLOAD_HEADER;
      continue;
    }
    case PK_S_PARSE_FABRIC_HEADER_MULTICAST: {
      out->fabric_header_multicast_present = 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_multicast.routed = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_multicast.outer_routed = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_multicast.tunnel_terminate = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_multicast.ingress_tunnel_type = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_multicast.ingress_ifindex = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_multicast.ingress_bd = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_multicast.mcast_grp = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      state = PK_S_PARSE_FABRIC_PAYLOAD_HEADER;
      continue;
    }
    case PK_S_PARSE_FABRIC_HEADER_MIRROR: {
      out->fabric_header_mirror_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_mirror.rewrite_index = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 10 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_mirror.egress_port = (uint16_t)pk_read_bits(buf, bit_len, off, 10);
      off += 10;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_mirror.egress_queue = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_mirror.pad = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      state = PK_S_PARSE_FABRIC_PAYLOAD_HEADER;
      continue;
    }
    case PK_S_PARSE_FABRIC_HEADER_CPU: {
      out->fabric_header_cpu_present = 1;
      if (off + 5 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.egress_queue = (uint8_t)pk_read_bits(buf, bit_len, off, 5);
      off += 5;
      if (off + 1 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.tx_bypass = (uint8_t)pk_read_bits(buf, bit_len, off, 1);
      off += 1;
      if (off + 2 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.reserved = (uint8_t)pk_read_bits(buf, bit_len, off, 2);
      off += 2;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.ingress_port = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.ingress_ifindex = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.ingress_bd = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.reason_code = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_cpu.mcast_grp = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->fabric_header_cpu.reason_code;
      if (key0 == 4ULL) {
        state = PK_S_PARSE_FABRIC_SFLOW_HEADER;
        continue;
      } else {
        state = PK_S_PARSE_FABRIC_PAYLOAD_HEADER;
        continue;
      }
    }
    case PK_S_PARSE_FABRIC_SFLOW_HEADER: {
      out->fabric_header_sflow_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_sflow.sflow_session_id = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_header_sflow.sflow_egress_ifindex = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      state = PK_S_PARSE_FABRIC_PAYLOAD_HEADER;
      continue;
    }
    case PK_S_PARSE_FABRIC_PAYLOAD_HEADER: {
      out->fabric_payload_header_present = 1;
      if (off + 16 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->fabric_payload_header.ether_type = (uint16_t)(((uint64_t)buf[(off >> 3) + 0] << 8) | (uint64_t)buf[(off >> 3) + 1]);
      off += 16;
      uint64_t key0 = (uint64_t)out->fabric_payload_header.ether_type;
      if ((key0 & 65024ULL) == 0ULL) {
        state = PK_S_PARSE_LLC_HEADER;
        continue;
      } else if ((key0 & 64000ULL) == 0ULL) {
        state = PK_S_PARSE_LLC_HEADER;
        continue;
      } else if (key0 == 33024ULL) {
        state = PK_S_PARSE_VLAN;
        continue;
      } else if (key0 == 37120ULL) {
        state = PK_S_PARSE_QINQ;
        continue;
      } else if (key0 == 34887ULL) {
        state = PK_S_PARSE_MPLS;
        continue;
      } else if (key0 == 2048ULL) {
        state = PK_S_PARSE_IPV4;
        continue;
      } else if (key0 == 34525ULL) {
        state = PK_S_PARSE_IPV6;
        continue;
      } else if (key0 == 2054ULL) {
        state = PK_S_PARSE_ARP_RARP;
        continue;
      } else if (key0 == 35020ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else if (key0 == 34825ULL) {
        state = PK_S_PARSE_SET_PRIO_HIGH;
        continue;
      } else {
        out->outcome = 0;
        out->reason = PK_R_NONE;
        out->consumed_bits = off;
        return 0;
      }
    }
    case PK_S_PARSE_SET_PRIO_MED: {
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_SET_PRIO_HIGH: {
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    case PK_S_PARSE_SET_PRIO_MAX: {
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_p4lang_switch_parser_parse(const uint8_t *buf, uint64_t bit_len, pk_p4lang_switch_parser_result_t *out) {
  pk_p4lang_switch_parser_result_t zero = {0};
  *out = zero;
  return pk_p4lang_switch_parser_parse_core(buf, bit_len, out);
}

const char *pk_p4lang_switch_parser_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  case 4: return "out of region bounds";
  case 5: return "region out of bounds";
  case 6: return "region not exhausted";
  case 16: return "a fourth MPLS label overflows the source's mpls[3] stack";
  case 17: return "genv: only ver=0, optLen=0, protoType=TEB is parseable";
  default: return "";
  }
}
