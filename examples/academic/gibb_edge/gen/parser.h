/* Generated by pakeles from `gibb_edge`. Do not edit:
 * regenerate with `pakeles gen c`. */
#ifndef PK_GIBB_EDGE_H
#define PK_GIBB_EDGE_H

#include <stdint.h>
#include <stddef.h>

enum { PK_GIBB_EDGE_ACCEPT = 0, PK_GIBB_EDGE_REJECT = 1 };

typedef enum {
  PK_R_NONE = 0,
  PK_R_OUT_OF_BOUNDS = 1, /* "out of bounds" */
  PK_R_MAX_DEPTH_EXCEEDED = 2, /* "max depth exceeded" */
  PK_R_NO_MATCHING_SELECT_ARM = 3, /* "no matching select arm" */
  PK_R_OUT_OF_REGION_BOUNDS = 4, /* "out of region bounds" */
  PK_R_REGION_OUT_OF_BOUNDS = 5, /* "region out of bounds" */
  PK_R_REGION_NOT_EXHAUSTED = 6, /* "region not exhausted" */
} pk_gibb_edge_reason_t;

typedef struct {
  uint64_t dst_addr;
  uint64_t src_addr;
  uint16_t ether_type;
} pk_gibb_edge_ethernet_t;

typedef struct {
  uint32_t label;
  uint8_t tc;
  uint8_t bos;
  uint8_t ttl;
} pk_gibb_edge_mpls_t;

typedef struct {
  uint8_t v;
} pk_gibb_edge_mpls_payload_nibble_t;

typedef struct {
  uint8_t version;
  uint8_t ihl;
  uint8_t diffserv;
  uint16_t total_len;
  uint16_t identification;
  uint8_t flags;
  uint16_t frag_offset;
  uint8_t ttl;
  uint8_t protocol;
  uint16_t hdr_checksum;
  uint32_t src_addr;
  uint32_t dst_addr;
  uint64_t options_bit_off;
  uint64_t options_bit_len;
} pk_gibb_edge_ipv4_t;

typedef struct {
  uint8_t ihl;
  uint8_t diffserv;
  uint16_t total_len;
  uint16_t identification;
  uint8_t flags;
  uint16_t frag_offset;
  uint8_t ttl;
  uint8_t protocol;
  uint16_t hdr_checksum;
  uint32_t src_addr;
  uint32_t dst_addr;
  uint64_t options_bit_off;
  uint64_t options_bit_len;
} pk_gibb_edge_ipv4_rest_t;

typedef struct {
  uint8_t version;
  uint8_t traffic_class;
  uint32_t flow_label;
  uint16_t payload_len;
  uint8_t next_hdr;
  uint8_t hop_limit;
  uint64_t src_addr_bit_off;
  uint64_t src_addr_bit_len;
  uint64_t dst_addr_bit_off;
  uint64_t dst_addr_bit_len;
} pk_gibb_edge_ipv6_t;

typedef struct {
  uint8_t traffic_class;
  uint32_t flow_label;
  uint16_t payload_len;
  uint8_t next_hdr;
  uint8_t hop_limit;
  uint64_t src_addr_bit_off;
  uint64_t src_addr_bit_len;
  uint64_t dst_addr_bit_off;
  uint64_t dst_addr_bit_len;
} pk_gibb_edge_ipv6_rest_t;

typedef struct {
  uint16_t reserved;
  uint16_t seq_no;
} pk_gibb_edge_eompls_rest_t;

typedef struct {
  uint64_t dst_addr;
  uint64_t src_addr;
  uint16_t ether_type;
} pk_gibb_edge_ethernet2_t;

typedef struct {
  uint8_t outcome; /* 0 = accept, 1 = reject */
  uint16_t reason; /* pk_gibb_edge_reason */
  uint64_t consumed_bits;
  uint8_t ethernet_present;
  pk_gibb_edge_ethernet_t ethernet;
  uint8_t mpls_present;
  pk_gibb_edge_mpls_t mpls;
  uint8_t mpls_payload_nibble_present;
  pk_gibb_edge_mpls_payload_nibble_t mpls_payload_nibble;
  uint8_t ipv4_present;
  pk_gibb_edge_ipv4_t ipv4;
  uint8_t ipv4_rest_present;
  pk_gibb_edge_ipv4_rest_t ipv4_rest;
  uint8_t ipv6_present;
  pk_gibb_edge_ipv6_t ipv6;
  uint8_t ipv6_rest_present;
  pk_gibb_edge_ipv6_rest_t ipv6_rest;
  uint8_t eompls_rest_present;
  pk_gibb_edge_eompls_rest_t eompls_rest;
  uint8_t ethernet2_present;
  pk_gibb_edge_ethernet2_t ethernet2;
} pk_gibb_edge_result_t;

/* Parse `bit_len` bits of `buf` (reject mode). Returns outcome. */
int pk_gibb_edge_parse(const uint8_t *buf, uint64_t bit_len, pk_gibb_edge_result_t *out);
const char *pk_gibb_edge_reason_str(uint16_t reason);

#endif /* PK_GIBB_EDGE_H */
