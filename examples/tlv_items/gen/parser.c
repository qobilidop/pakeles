/* Generated by pakeles from `tlv_items`. Do not edit:
 * regenerate with `pakeles gen c`. */
#include "parser.h"

#define PK_S_PARSE_TOTAL 0
#define PK_S_TLV_LOOP 1
#define PK_S_PARSE_ITEM 2
#define PK_S_CLOSE 3

static int pk_tlv_items_parse_core(const uint8_t *buf, uint64_t bit_len, pk_tlv_items_result_t *out) {
  uint64_t off = 0;
  uint32_t state = PK_S_PARSE_TOTAL;
  uint32_t depth;
#define PK_RMASK 0u
  uint64_t pk_region_end[1];
  uint32_t pk_rsp = 0;
  for (depth = 0; depth < 8u; depth++) {
    switch (state) {
    case PK_S_PARSE_TOTAL: {
      out->total_len_present = 1;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->total_len.total = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t rlen = ((uint64_t)out->total_len.total * 8ULL);
        if (pk_rsp) {
          if (rlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
            out->outcome = 1;
            out->reason = PK_R_REGION_OUT_OF_BOUNDS;
            out->consumed_bits = off;
            return 1;
          }
        } else if (rlen > 0xffffffffffffffffULL - off) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (pk_rsp > PK_RMASK) {
          out->outcome = 1;
          out->reason = PK_R_REGION_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        pk_region_end[pk_rsp & PK_RMASK] = off + rlen;
        pk_rsp++;
      }
      state = PK_S_TLV_LOOP;
      continue;
    }
    case PK_S_TLV_LOOP: {
      uint64_t key0 = ((pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) >> 3ULL);
      if (key0 == 0ULL) {
        state = PK_S_CLOSE;
        continue;
      } else {
        state = PK_S_PARSE_ITEM;
        continue;
      }
    }
    case PK_S_PARSE_ITEM: {
      out->item_present = 1;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->item.kind = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      if (pk_rsp && off + 8 > pk_region_end[(pk_rsp - 1u) & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_REGION_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      if (off + 8 > bit_len) {
        out->outcome = 1;
        out->reason = PK_R_OUT_OF_BOUNDS;
        out->consumed_bits = off;
        return 1;
      }
      out->item.ln = (uint8_t)((uint64_t)buf[(off >> 3) + 0]);
      off += 8;
      {
        uint64_t vlen = ((uint64_t)out->item.ln * 8ULL);
        if (pk_rsp && vlen > pk_region_end[(pk_rsp - 1u) & PK_RMASK] - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_REGION_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        if (vlen > bit_len - off) {
          out->outcome = 1;
          out->reason = PK_R_OUT_OF_BOUNDS;
          out->consumed_bits = off;
          return 1;
        }
        out->item.val_bit_off = off;
        out->item.val_bit_len = vlen;
        off += vlen;
      }
      state = PK_S_TLV_LOOP;
      continue;
    }
    case PK_S_CLOSE: {
      if (!pk_rsp) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      pk_rsp--;
      if (off < pk_region_end[pk_rsp & PK_RMASK]) {
        out->outcome = 1;
        out->reason = PK_R_REGION_NOT_EXHAUSTED;
        out->consumed_bits = off;
        return 1;
      }
      out->outcome = 0;
      out->reason = PK_R_NONE;
      out->consumed_bits = off;
      return 0;
    }
    }
  }
  out->outcome = 1;
  out->reason = PK_R_MAX_DEPTH_EXCEEDED;
  out->consumed_bits = off;
  return 1;
}

int pk_tlv_items_parse(const uint8_t *buf, uint64_t bit_len, pk_tlv_items_result_t *out) {
  pk_tlv_items_result_t zero = {0};
  *out = zero;
  return pk_tlv_items_parse_core(buf, bit_len, out);
}

const char *pk_tlv_items_reason_str(uint16_t reason) {
  switch (reason) {
  case 1: return "out of bounds";
  case 2: return "max depth exceeded";
  case 3: return "no matching select arm";
  case 4: return "out of region bounds";
  case 5: return "region out of bounds";
  case 6: return "region not exhausted";
  default: return "";
  }
}
