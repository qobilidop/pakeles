/* Generated by pakeles from `tlv_items`. Do not edit:
 * regenerate with `pakeles gen c`. */
#ifndef PK_TLV_ITEMS_H
#define PK_TLV_ITEMS_H

#include <stdint.h>
#include <stddef.h>

enum { PK_TLV_ITEMS_ACCEPT = 0, PK_TLV_ITEMS_REJECT = 1 };

typedef enum {
  PK_R_NONE = 0,
  PK_R_OUT_OF_BOUNDS = 1, /* "out of bounds" */
  PK_R_MAX_DEPTH_EXCEEDED = 2, /* "max depth exceeded" */
  PK_R_NO_MATCHING_SELECT_ARM = 3, /* "no matching select arm" */
  PK_R_OUT_OF_REGION_BOUNDS = 4, /* "out of region bounds" */
  PK_R_REGION_OUT_OF_BOUNDS = 5, /* "region out of bounds" */
  PK_R_REGION_NOT_EXHAUSTED = 6, /* "region not exhausted" */
} pk_tlv_items_reason_t;

typedef struct {
  uint8_t total;
} pk_tlv_items_total_len_t;

typedef struct {
  uint8_t kind;
  uint8_t ln;
  uint64_t val_bit_off;
  uint64_t val_bit_len;
} pk_tlv_items_item_t;

typedef struct {
  uint8_t outcome; /* 0 = accept, 1 = reject */
  uint16_t reason; /* pk_tlv_items_reason */
  uint64_t consumed_bits;
  uint8_t total_len_present;
  pk_tlv_items_total_len_t total_len;
  uint8_t item_present;
  pk_tlv_items_item_t item;
} pk_tlv_items_result_t;

/* Parse `bit_len` bits of `buf` (reject mode). Returns outcome. */
int pk_tlv_items_parse(const uint8_t *buf, uint64_t bit_len, pk_tlv_items_result_t *out);
const char *pk_tlv_items_reason_str(uint16_t reason);

#endif /* PK_TLV_ITEMS_H */
